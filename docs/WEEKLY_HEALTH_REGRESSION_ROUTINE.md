# Weekly Health + Regression + Autonomous Bug-Fix — Claude Code routine

> Runs **weekly**, **global coverage** (both repos, all test types). Mirrors the structure of `DAILY_SECURITY_CHECK_ROUTINE.md`.
> Deterministic test/build runner: `scripts/weekly-health-regression.sh`. The agent does triage, autonomous fixes (on `develop` via `claude/*` PRs), Jira logging, and a **release rehearsal that stops at the manual production gate**.

This routine checks Lyra is healthy, runs the full regression + E2E + build suite across `luisa-sys/lyra` and `luisa-sys/lyra-mcp-server`, **fixes the bugs it can on a fully autonomous basis**, logs everything in Jira, and **rehearses the release pipeline** — stopping before the one step the project forbids automating.

## 🚦 Hard boundaries (do not cross — encoded in the prompt)

1. **Production promote — AUTO for FIXES, MANUAL for FEATURES (owner-authorized 2026-06-21).** The routine MAY auto-promote to production, but **only when *every* change pending on `develop` ahead of `main` is a bug-FIX** (a BUGS/SEC defect, `fix:`-type; no new feature / user-facing capability / route / table / MCP tool / migration) — **never a feature**, and only after the full suite is green through staging + beta. If *any* pending change is a feature, or fix-vs-feature is ambiguous → **STOP and require manual sign-off**. It always promotes via `promote-to-production.yml` (built-in smoke + auto-rollback), never a direct push to `main/beta/staging`. (Supersedes the prior "never automated" default for this routine — see `CLAUDE.md` → Deployment Pipeline.)
2. **Test Integrity Policy.** A failing test means the *code* is wrong, not the test. The routine must **never** modify, weaken, skip, or delete a test to go green. If a fix would require a test change, it **STOPS and reports** for sign-off.
3. **Autonomy with a stop-line.** Fix what's unambiguous and low-risk on a `claude/*` branch → PR to `develop`. For anything ambiguous, architectural, security-sensitive, data-model/RLS/migration, or requiring a test change → **STOP and report to the user**. (Mirrors your instruction: "Stop where you are unsure … otherwise act autonomously.")
4. **No self-merge to protected branches.** Open PRs; the routine may merge its own fix PR to `develop` **only** when CI is green and the change is low-risk — never to `staging/beta/main`.

## How the run is split

| Layer | Driven by | Covers |
|---|---|---|
| Tests + build | `scripts/weekly-health-regression.sh` | lint, type-check, unit, scripts, integration, E2E, build — honest PASS/FAIL/UNVERIFIED |
| Live health | agent (curl / connectors) | `*/api/health`, MCP `/health`, deploy SHAs, CI status |
| Triage + fix | agent | open `claude/*` PR per fix, log Jira (BUGS/SEC), respect the boundaries above |
| Release | agent (GitHub connector) | full chain develop→staging→beta→prod **for fix-only releases**; STOP + manual sign-off if any feature is pending |
| Self-update | agent | propose improvements to THIS routine's script/prompt/doc via a PR (never silent) |

## Prerequisite (same as the other routines)

Routines clone the **default branch (`main`)**; this tooling lives on `develop`. The prompt's first lines check out `develop` (and STOP if the script isn't there).

## Setup

- **Connectors:** GitHub + Atlassian (+ Supabase/Cloudflare/Vercel if you want the agent to read deploy/runtime state). Remove the rest.
- **Setup script** (this routine *does* need one — it runs the suite):
  ```bash
  npm ci
  npx playwright install --with-deps   # for E2E
  ```
  Repeat for `lyra-mcp-server` if covering it in the same run (or use a second routine).
- **Network:** **Custom** allowlist with `*.checklyra.com`, `mcp.checklyra.com`, `mcp-dev.checklyra.com` (for live health + E2E targets) + the default package registries (for `npm ci`).
- **Schedule:** **Weekly** (e.g. Monday 06:00 local — before the workweek; note the Sunday 23:00 UTC develop→staging auto-promote already runs).
- **Permissions:** leave **"Allow unrestricted branch pushes" OFF** (so it can only push `claude/*` — this is what keeps it off `main/beta/staging`). No prod tokens in the env.

## The routine prompt

```
FIRST run: git fetch origin develop && git checkout develop
Then verify scripts/weekly-health-regression.sh exists; if not, STOP and say the
checkout failed — do NOT improvise.

Read the repo for context first: CLAUDE.md, docs/RUNBOOK.md, docs/RELEASE_POLICY.md,
docs/ARCHITECTURE.md. Obey them. Two rules are absolute:
  (A) PRODUCTION PROMOTE IS MANUAL — never run promote-to-production.yml, never push
      to main/beta/staging, never vercel-promote to prod. You prepare + report only.
  (B) TEST INTEGRITY — never modify/skip/weaken/delete a test to make it pass. If a fix
      needs a test change, STOP and report for sign-off.

GOAL: confirm Lyra is healthy, run full regression + E2E, fix what you safely can,
log everything, and rehearse the release pipeline up to (not through) the prod gate.

1. HEALTH: curl https://checklyra.com/api/health and https://mcp.checklyra.com/health;
   check the latest CI run on develop is green (GitHub connector). Note anything red.
2. REGRESSION + E2E: run  RUN_E2E=1 bash scripts/weekly-health-regression.sh  and capture
   the PASS/FAIL/UNVERIFIED lines. (For lyra-mcp-server coverage, clone/build/test it too.)
3. TRIAGE + FIX (autonomous, bounded):
   - For each FAIL, find the ROOT CAUSE in the application code and fix it on a new
     claude/ branch. Re-run the suite to confirm green WITHOUT touching any test.
   - Open a PR to develop. If CI is green and the fix is low-risk, you may merge it to
     develop. NEVER merge to staging/beta/main.
   - STOP and report (do not fix) anything ambiguous, architectural, security/RLS/auth,
     migration/data-model, or that would require a test change. List it for the user.
   - Log every bug + fix in Jira the usual way: BUGS (6-section) for defects, SEC for
     security findings, with the failing output + your diagnosis + the PR link.
4. RESIDUAL BUGS: also scan for residual/known issues (open BUGS/SEC tickets, TODO/FIXME,
   flaky tests, Dependabot/CodeQL alerts) and fix the safe ones under the same rules.
5. RELEASE (auto for FIX-ONLY releases; manual for features — see boundary 1):
   - Determine what is pending on develop ahead of main (the changes that WOULD ship to prod):
     git log --oneline origin/main..origin/develop. CLASSIFY every commit.
   - If ALL pending changes are bug-FIXES (BUGS/SEC defects, fix: commits; NO new feature,
     user-facing capability, route, table, MCP tool, or migration) -> you are AUTHORISED to ship:
       a. Bump package.json: npm version patch --no-git-tag-version (per KAN-166), commit on a claude/ PR.
       b. Promote the chain: promote-to-staging.yml -> verify staging green + healthy ->
          promote-staging-to-beta.yml -> verify beta ->
          promote-to-production.yml -f confirm=PRODUCTION -f promote_mode=auto-fix-only.
          The `-f promote_mode=auto-fix-only` flag is REQUIRED for the auto path: the workflow then
          runs scripts/check-fix-only-promote.sh and HARD-FAILS the promote if any commit in
          origin/main..origin/beta is a feature / breaking / ambiguous change (SEC-77 — the fix-only
          gate is now machine-enforced, not just your judgement). If it fails, treat as "release holds
          a feature" -> STOP and require manual sign-off (see the ambiguous branch below).
       c. The prod workflow runs smoke tests + AUTO-ROLLBACK. If it rolls back, treat as FAIL,
          open a Highest-priority BUGS ticket, and STOP.
       d. Verify prod after: curl https://checklyra.com/api/health and https://mcp.checklyra.com/health.
   - If ANY pending change is a feature, OR you are unsure whether something is a fix vs a feature
     -> DO NOT promote past staging. STOP and report: "release holds a feature / is ambiguous —
     needs manual sign-off: gh workflow run promote-to-production.yml -f confirm=PRODUCTION".
6. SELF-UPDATE: if you found a way this routine (script/prompt/doc) should improve, open a
   PR proposing it — do NOT silently change your own behaviour.
7. REPORT + LOG: append a run-log row to this doc (date, PASS/FAIL counts, bugs fixed +
   tickets, what you STOPPED on, release-readiness) via a claude/ PR. If anything is red
   or you stopped on something, put a clear "ACTION NEEDED:" summary at the top of your reply.
```

## Run log

| Date | Runner | Suite result | Bugs fixed → tickets/PRs | Stopped-on (needs user) | Release-ready? |
|---|---|---|---|---|---|
| 2026-06-21 | _(initial — routine authored; fix-only auto-promote authorised 2026-06-21)_ | — | — | — | auto for fix-only; features manual |
| 2026-06-24 | Claude Code (Opus) | lyra PASS=5 (lint/type-check/unit/scripts/build); integration+e2e = env gaps → **UNVERIFIED** (no `tests/integration/`; Playwright browsers rev 1194 vs required 1223, CDN blocked) — _not_ product FAILs. lyra-mcp-server: tsc + 565/565 tests green. Health: checklyra.com + mcp.checklyra.com `/health` OK; develop CI (Deploy/Lint&Unit/CodeQL) green. | qs moderate DoS (GHSA-q8mj-m7cp-5q26) in lyra-mcp-server → SEC-35 + draft PR #91 (lockfile qs 6.15.0→6.15.2). BUGS-51 false-FAIL reproduced (PR #352 still pending) → confirmation comment only. | **STOP (sign-off):** PR #352 (BUGS-51 runner-honesty) + PR #91 (SEC-35 qs bump) are draft, awaiting merge. Open HIGH SEC items (SEC-29/30/31/3/23/2) untouched — security/RLS, out of autonomous bounds. integration+e2e need env fix (#352) or browser/CDN allowlist to verify. | **Nothing to release** — develop is 0 commits ahead of main (chain fully flushed). No promotion run. |
| 2026-06-25 | Claude Code (weekly routine) | lyra: lint/type-check/unit (1803/139)/scripts/build **PASS**; e2e + integration **UNVERIFIED** (env gaps — Playwright browsers rev 1194 vs required 1223 + webkit 2287, CDN network-blocked; no `tests/integration/` dir). mcp-server: tsc clean + **583/583** green. Live health green (checklyra `ok`, mcp `ok`); develop HEAD 5a74607 CI + CodeQL green. | None to fix — both FAIL lines are the known **BUGS-51** env-gap false-positives; fix already in **PR #352** (draft, awaiting sign-off — not self-merged per boundary 6). Posted weekly-confirmation comment on BUGS-51. | **BUGS-51** false-FAIL persists → recommend merging **#352**. **BUGS-57** (High): prod MCP OAuth auth rejected — auth/architectural, out of routine scope. npm audit: prod 0, dev 21 moderate. SEC-35 (qs DoS in mcp-server) tracked. | **NO auto-promote** — 12+ `feat()` pending on develop→main (Convene, admin back-office, /status, age-verify, RS256, per-user entitlements, KAN-326). Release holds features → **manual prod sign-off required**: `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-06-27 | weekly health/regression routine (Claude Code, remote) | **Real-green.** lyra: lint/type-check/unit/scripts/build PASS; E2E (chromium, against local build with CI dummy-Supabase env) 17 passed / 0 failed / 5 skipped. mcp-server: build OK, 525/525 tests pass. Two script-level **false-FAILs** from `RUN_E2E=1` — both environment/harness gaps, not code defects: (a) `integration` — no `tests/integration` dir exists so jest exits 1 on 0 matches; (b) `e2e` — webkit/`mobile-safari` browser binary absent (download 403 by network policy) **and** the E2E phase runs `npm run dev` with no Supabase env, so `requireEnv` throws on `/waitlist` + `[slug]` 404. Supplying CI's dummy Supabase env + the installed chromium binary → all pass. Tracked: BUGS-51 / BUGS-58. | None — no application defect found; **no test modified/skipped/weakened** (Test Integrity Policy). Health GREEN: checklyra.com/api/health + mcp.checklyra.com/health both ok; latest develop CI green. | Release HELD at the prod gate per this run's **absolute** boundary "production promote is manual — prepare + report only" (GOAL: rehearse *up to, not through* the gate). No push to staging/beta/main, no promote workflow run. | **YES — fix-only, ready for manual promote.** Pending on develop ahead of main = 1 commit: `fix(SEC-40)` cookie-domain scoping (src/lib/cookie-domain.ts + its unit test; no feature/route/table/MCP tool/migration). ACTION NEEDED → manual: `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-06-29 | Claude Code | lyra: lint/type-check/unit/scripts/build **PASS**; integration + e2e **UNVERIFIED** (env gap, not code — BUGS-51). lyra-mcp-server: **585 tests PASS** (38 suites). Health: checklyra.com + mcp.checklyra.com **OK**; latest CI green. | BUGS-51 self-update: `weekly-health-regression.sh` now classifies missing-Playwright-browser / no-tests-found as UNVERIFIED not FAIL (+5 unit tests) — draft PR on `claude/nice-meitner-z1zc16`. qs DoS bump (SEC-35/41) — draft PR on lyra-mcp-server `claude/sharp-faraday-z1zc16`. | **No code regressions.** E2E unverifiable in-sandbox: pre-baked browsers (rev 1194) out of lockstep with `@playwright/test@1.60.0` (rev 1223 + webkit 2287) and the Playwright CDN is network-blocked (403). Needs infra: bake matching browsers or allowlist the CDN (BUGS-51 §5). | **No release pending** — develop is 0 commits ahead of main (chain fully flushed); nothing to promote. |
| 2026-06-30 | Weekly Health+Regression (Claude Code) | Health green (`checklyra.com/api/health` ok prod; `mcp.checklyra.com/health` ok; develop HEAD `6cfe7a2` CI + CodeQL green). lyra **PASS=5** (lint/type-check/unit/scripts/build); `e2e`+`integration` are env-gap **UNVERIFIED** not FAIL (BUGS-51 — see note). lyra-mcp-server: `tsc` clean + **585/585** (38 suites) PASS. | **No product bug.** Both raw-runner FAILs are the BUGS-51 false-positive: Playwright browsers rev `1194` ≠ `@playwright/test@1.60.0`'s required `1223`+webkit `2287` (CDN blocked) → every e2e fail is `Executable doesn't exist`, never an assertion; and no `tests/integration/` dir → jest `No tests found`. Fix already merged as **#404** (BUGS-51, +5 unit tests) this session. qs DoS (SEC-35) already in draft PR #91. | **Merge ONE BUGS-51 fix (#404) and close the dup drafts** (#352, #392) — done this session. BUGS-57 (High — prod MCP OAuth clients rejected) still open: auth/architectural → report-only. | **Nothing to release** — `origin/main..origin/develop` = **0 commits** (chain already flushed to main). No promotion run. |
| 2026-07-07 | Weekly Health+Regression (Claude Code) | **No product FAIL.** lyra (develop `ed319b3`): lint/type-check/unit/scripts/build **PASS=5**, FAIL=0; `integration`+`e2e` **UNVERIFIED** — known **BUGS-51** env gaps (no `tests/integration/` dir → jest 0-match; only chromium rev `1194` baked, no webkit/firefox + `@playwright/test@1.61.1` CDN-blocked → 10 e2e skipped). lyra-mcp-server (`origin/main e5445311`): `tsc` clean + **637/637** (41 suites) PASS. Health **GREEN**: checklyra `/api/health` prod `ok`, mcp `/health` `ok`; develop HEAD `ed319b3` CI (CodeQL + Deploy) green. npm audit prod **0/0** both repos (SEC-35 `qs` DoS now **cleared** on mcp main via #97). | None — no application defect surfaced; **no test modified/skipped/weakened** (Test Integrity Policy). No new BUGS/SEC filed. | Both UNVERIFIED lines are env-only (BUGS-51) — need baked matching browsers / CDN allowlist + a `tests/integration/` path to verify in-sandbox; not code. Citing (owned by Daily Security, not re-derived): **SEC-79** watchdog FAIL — `health-check.yml` + `weekly-report.yml` `disabled_manually`. Large open security/RLS backlog (SEC-43/31/30/23/3 High + others) all report-only, out of autonomous scope. | **HELD — release holds features + a migration.** 43 commits `develop→main` include `feat(dashboard/profile/ux)` (#418/#416/#408), the KAN-404 profile-editor epic, and the SECURITY DEFINER/RLS migration batch (#443) → **manual prod sign-off required**. Drift 🔴 **RED** (43 ≥ 15 threshold). ACTION NEEDED → manual: `promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-08 | Weekly Health+Regression (Claude Code, remote) | Health **green**: `checklyra.com/api/health` ok (prod); `mcp.checklyra.com/health` ok; develop HEAD `adf5e4a` — CodeQL + Deploy-to-Development + E2E-gate + PR-Checks all green. lyra **PASS=5** (lint/type-check/unit/scripts/build), FAIL=0; `integration`+`e2e` **UNVERIFIED** (BUGS-51 env gap — see note). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. | **No product bug** — no test modified/skipped/weakened (Test Integrity Policy). The two UNVERIFIED lines are the tracked BUGS-51 env gap: no `tests/integration/` dir → jest "0 matches"; and pre-baked Playwright browser rev `1194` ≠ `@playwright/test`'s now-required rev **`1228`** (`chromium_headless_shell-1228` missing), CDN download **403/network-blocked** — confirmed by attempting `npx playwright install chromium` (fails) and running `--project=chromium` locally (every fail is `Executable doesn't exist`, never an assertion). npm audit: lyra prod **0**, dev 3 moderate; mcp-server **0**. | **STOP — release HELD (needs manual sign-off).** Pending `main..develop` = 2 commits: `fix(SEC-76)` DCR anti-phishing **carries a DB migration** (`oauth_clients.is_first_party`, applied dev+staging, **NOT prod**) + consent-screen/route changes → **disqualified from fix-only auto-promote** (boundary 1 excludes migrations/tables). Prod Supabase must be migrated before/with any promote or gated OAuth reads break. E2E still needs infra fix (bake rev-1228 browsers or allowlist Playwright CDN — BUGS-51). | **NO auto-promote.** Release holds a **migration** (data-model change) → manual prod sign-off required: apply the KAN/SEC-76 migration to prod Supabase, then `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-09 | Weekly Health+Regression (Claude Code, remote) | Health **GREEN** (`checklyra.com/api/health` ok prod; `mcp.checklyra.com/health` ok; develop HEAD `adf5e4a` Deploy+CodeQL green). lyra **PASS=5** (lint/type-check/unit/scripts/build), FAIL=0; `integration`+`e2e` **UNVERIFIED** (BUGS-51 env gap — no `tests/integration/` dir; Playwright browsers rev `1194` ≠ v1.61.1's required `1228`, CDN download blocked). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. npm audit: lyra prod **0** / 3 moderate dev-only; mcp-server **0**. | **None — no product FAIL; no test modified/skipped/weakened** (Test Integrity Policy). Both UNVERIFIED lines are the known BUGS-51 infra gap, not code defects. | **BUGS-51** e2e/integration env gap persists (infra: bake matching Playwright browsers or allowlist the CDN). Open **High** SEC items (SEC-43/3/32/31/30/23) untouched — security/RLS/auth/DR, out of autonomous bounds → report-only. | **HELD — NOT fix-only.** `origin/main..origin/develop` = **2 commits**: `fix(SEC-76)` DCR anti-phishing **includes a DB migration** (`oauth_clients.is_first_party`) + user-facing OAuth consent-screen changes, plus `docs(KAN-283)` (docs-only). Migration + user-facing capability ⇒ **outside** the fix-only auto-promote boundary → **manual sign-off required**: `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-10 | Weekly Health+Regression (Claude Code, remote) | **All-green, zero product failures.** Health: `checklyra.com/api/health` ok (prod); `mcp.checklyra.com/health` ok; develop HEAD `adf5e4a` CI green (Deploy to Development + CodeQL both `success`). lyra suite (`RUN_E2E=1`): **PASS=5** (lint/type-check/unit/scripts/build), FAIL=0, **UNVERIFIED=2** — `integration` (no `tests/integration/` dir) + `e2e` (BUGS-51 browser-rev gap). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. | **None — no application defect found; no test modified/skipped/weakened** (Test Integrity Policy). Every E2E "failure" is the BUGS-51 env gap: `@playwright/test@1.61.1` needs `chromium_headless_shell` rev **1228** but the sandbox has rev **1194**, and the Playwright CDN is network-blocked (`npx playwright install` → Download failure) — errors fire at `browserType.launch` (2ms) before any assertion, verified by direct chromium-only run against live prod. | E2E remains unverifiable in-sandbox (BUGS-51 §5 infra: bake matching browsers or allowlist the CDN). npm audit: lyra prod **0**, dev **3 moderate** (js-yaml + uuid, both via `@lhci` Lighthouse-CI tooling — dev-only, non-shipping; uuid fix is breaking) — flagged, not auto-fixed. Open HIGH SEC (SEC-43/3/32/31/30/23) all security/RLS/backup — out of autonomous bounds, tracked. | **HELD for manual sign-off.** `origin/main..origin/develop` = **2 commits**: `docs(KAN-283)` #475 (docs-only) + `fix(SEC-76)` #476 — but #476 **carries a DB migration** (`…sec76_oauth_clients_is_first_party.sql`, adds `oauth_clients.is_first_party`). A pending migration disqualifies fix-only auto-promote per boundary 1, **and** this run's absolute (A) makes prod promote manual regardless. ACTION NEEDED → manual, in lockstep with the migration reaching prod Supabase: `promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-11 | Weekly Health+Regression (Claude Code) | Health green (`checklyra.com/api/health` ok prod; `mcp.checklyra.com/health` ok, build_sha `e5445311…` = `lyra-mcp-server@main` HEAD; develop HEAD `adf5e4a` Deploy+CodeQL green). lyra **PASS=5** (lint/type-check/unit/scripts/build), **FAIL=0**; `integration`+`e2e` = env-gap **UNVERIFIED** not FAIL (BUGS-51 — no `tests/integration/` dir; Playwright browsers unavailable → **10 skipped**, error at launch never an assertion). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. | **None — no product FAIL.** Both UNVERIFIED lines are the known BUGS-51 env gaps; **no test modified/skipped/weakened** (Test Integrity Policy). npm audit: lyra prod **0** / 3 moderate dev-only (js-yaml via coverage tooling, uuid via @lhci/cli — Dependabot-tracked); mcp-server **0**. | **Report-only (out of autonomous bounds):** open High SEC items (SEC-43/3/32/31/30/23) + the Medium SEC/BUGS batch (SEC-76/62/61/60/…, BUGS-61/62) untouched — all security/RLS/auth/data-model/architectural. E2E unverifiable in-sandbox (BUGS-51). | **HELD — manual sign-off.** `origin/main..origin/develop` = **2 commits**: `docs(KAN-283)` #475 (docs-only) + `fix(SEC-76)` #476 which carries a **DB migration** (`…sec76_oauth_clients_is_first_party.sql`, adds `oauth_clients.is_first_party`) **+ user-facing OAuth consent-screen changes** → outside fix-only auto-promote (boundary 1). No promotion run. ACTION NEEDED → manual: `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-12 | Weekly Health+Regression (Claude Code, remote) | Health **GREEN** (`checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `{status:ok, version:1.0.0}`; develop HEAD `adf5e4a`, latest develop CI — PR Checks + CodeQL + E2E-PR-gate — all **success**). lyra **PASS=5** (lint/type-check/unit/scripts/build), **FAIL=0**, `integration`+`e2e` **UNVERIFIED** (BUGS-51 env gaps — no `tests/integration/` dir; Playwright 1.61.1 vs pre-baked chromium rev `1194` + no webkit for `mobile-safari`, 10 skipped). Note: real E2E DID run **green in develop CI** ("E2E Tests (PR gate) success"), so E2E is verified upstream, only unreproducible in-sandbox. lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. | **No product bug — nothing to autonomously fix.** No test modified/skipped/weakened (Test Integrity Policy). npm audit: both repos **0 prod** vulns; lyra has 3 **dev-only** moderates (js-yaml/uuid via `@lhci/cli` Lighthouse tooling — uuid fix needs a breaking `--force` downgrade, not low-risk → report-only). | **STOPPED — release is NOT fix-only → manual sign-off.** Pending `origin/main..origin/develop` = 2 commits: `docs(KAN-283)` #475 (docs-only) **and** `fix(SEC-76)` #476 which carries a **DB migration** (`oauth_clients.is_first_party`) + user-facing `/oauth/authorize` consent-screen changes. Per boundary #1, a pending migration/data-model change ⇒ not auto-promotable. **Also: the SEC-76 migration is applied dev+staging but NOT prod** — code must not ship ahead of the prod column or gated OAuth reads break (KAN-327-style ordering). Open SEC/BUGS backlog (SEC-43 High, SEC-44/57/62/76 etc.) all security/RLS/auth → report-only, out of routine bounds. | **HOLD — manual prod sign-off required.** Migration must be applied to **prod Supabase** first, then: `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. No promotion run this session (boundary A). |
| 2026-07-13 | Weekly Health+Regression (Claude Code) | Health green (`checklyra.com/api/health` ok prod; `mcp.checklyra.com/health` ok; develop CI — PR Checks / E2E gate / CodeQL — all `success`). lyra **PASS=5** (lint/type-check/unit/scripts/build); `integration`+`e2e` env-gap **UNVERIFIED** not FAIL (BUGS-51 — see note). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. npm audit: lyra prod **0** / dev **3 moderate** (dev-only); mcp-server **0**. | **No product bug; no test modified/skipped/weakened.** Both raw-runner FAILs are the BUGS-51 env gap: no `tests/integration/` dir → jest `No tests found`; and pre-baked Playwright chromium rev `1194` ≠ required rev `1228` (`chromium_headless_shell-1228` absent, install CDN 403) → every e2e launch is `Executable doesn't exist`, never an assertion. Bounded chromium-only retry confirmed the same launch error. | **STOP — release holds a migration + user-facing change → manual sign-off.** develop is **2 commits** ahead of main: `docs(KAN-283)` #475 (compliance docs only) + **`fix(SEC-76)` #476** which, though a security fix, **carries a DB migration** (`oauth_clients.is_first_party`, `20260707193000`) **and a user-facing OAuth consent-screen UI change** — outside the fix-only auto-promote boundary. **Prod Supabase does NOT yet have that migration** (latest prod = `kan349_404_promote_batch_prod`); promoting #476 code without applying it first would break the OAuth consent flow. | **HELD at staging gate — NOT auto-promoted.** Manual sequence required: apply the SEC-76 migration to prod Supabase, then `promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-14 | Weekly Health+Regression (Claude Code) | Health **GREEN**: `checklyra.com/api/health` ok (prod); `mcp.checklyra.com/health` ok; mcp `build_sha e544531` **== `origin/main` HEAD** (Railway in sync); develop CI (PR Checks / E2E gate / CodeQL / Scheduled Health) all green. lyra **PASS=5** (lint/type-check/unit/scripts/build), **FAIL=0**, **UNVERIFIED=2** — `integration` (no `tests/integration/` dir) + `e2e` (BUGS-51 env gap: sandbox chromium **rev 1194** ≠ `@playwright/test@1.61.1` required **rev 1228**, webkit absent, download CDN blocked → `Executable doesn't exist`, never an assertion). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS (well above 217 floor). | **None — zero product FAILs.** No test modified/skipped/weakened (Test Integrity Policy). | **Release HELD — manual sign-off (see next col).** E2E remains env-UNVERIFIED (BUGS-51 — infra browser-lockstep, out of code scope; classifier already correct). Open HIGH SEC (SEC-3/23/30/31/32/43) + Medium SEC/BUGS untouched — security/RLS/auth/architectural, out of autonomous bounds. | **NO auto-promote — NOT fix-only.** `origin/main..origin/develop` = **2 commits**: `docs(KAN-283)` (docs-only, inert) **+** `fix(SEC-76)` DCR anti-phishing — which carries a Supabase **migration** (`oauth_clients.is_first_party`, applied dev+staging, **NOT prod**) **and** new user-facing OAuth consent-screen UI (verified/unverified badges, redirect-host). Migration + user-facing capability → excluded from the fix-only auto-promote path. **ACTION NEEDED (manual):** apply the SEC-76 migration to prod Supabase, then `promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-15 | Weekly Health+Regression (Claude Code) | Health **GREEN** (`checklyra.com/api/health` ok prod; `mcp.checklyra.com/health` ok; `beta.checklyra.com` health ok + `/dashboard` 307→`/login`). develop HEAD `adf5e4a` CI: PR Checks + E2E gate + CodeQL **green**. lyra **PASS=5** (lint/type-check/unit/scripts/build), FAIL=0; `integration`+`e2e` **UNVERIFIED** (BUGS-51 env gap — no `tests/integration/` dir; Playwright browsers rev `1194` ≠ now-required `1228`, CDN blocked). A manual chromium bypass reproduced the exact rev-mismatch (`chromium_headless_shell-1228` missing) — env, not a regression; **no test modified** (Test Integrity Policy intact). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. | **None — no product FAIL, no fix required.** | **Beta gate smoke** scheduled run (main, 07:45Z) failed once at step `/dashboard auth-gate` — transient CF-challenge-class flake (KAN-279 class); live beta verified correct now (307→`/login`, health ok). npm audit: lyra prod **0** / dev **3 moderate** (js-yaml, uuid — dev-tooling only; only fix is a breaking `@lhci/cli` downgrade → left); mcp-server **0/0**. Open BUGS/SEC backlog all security/RLS/auth/architectural (e.g. SEC-83/3/43/32/31/30/23; BUGS-61/62) → report-only, out of autonomous bounds. | **NO auto-promote — STOP at manual prod gate.** Pending develop→main = **2 commits**: `docs(KAN-283)` (docs-only) + `fix(SEC-76)` DCR anti-phishing — the latter, despite the `fix:` prefix, ships a **migration** (`oauth_clients.is_first_party` column), a **user-facing OAuth consent-screen** change, and is **auth/security**-sensitive → outside the fix-only auto-promote lane. Needs manual sign-off (verify the SEC-76 migration is applied to prod Supabase first): `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-16 | Weekly Health+Regression (Claude Code, Opus) | Health **GREEN** (checklyra.com/api/health ok prod; mcp.checklyra.com/health ok — prod MCP on latest main `e544531`; develop CI + CodeQL green). lyra: lint/type-check/unit/scripts/build **PASS=5, FAIL=0**; integration + e2e **UNVERIFIED** (BUGS-51 env gaps — no `tests/integration/` dir; Playwright browsers unavailable, 10 e2e skipped) — not code FAILs. lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS; npm audit 0. | **None** — zero product FAILs; no test modified/skipped/weakened (Test Integrity Policy). | **Release HELD — manual sign-off (see last col).** Open HIGH SEC (SEC-83/3/43/32/31/30/23) + medium SEC/BUGS untouched — security/RLS/auth/DR/migration, out of autonomous bounds. lyra dev-only npm audit: 3 moderate (js-yaml, uuid) in CI tooling (lighthouse/istanbul), not prod-facing. | **NO auto-promote — NOT fix-only.** `origin/main..origin/develop` = **2 commits**: `fix(SEC-76)` DCR anti-phishing **carries a migration** (`oauth_clients.is_first_party` new column) + is OAuth-security-sensitive; `docs(KAN-283)` privacy intake. Migration + security change → **manual gate**; prod Supabase migration must be applied by web team first. ACTION NEEDED (manual): `promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-17 | Weekly Health+Regression (Claude Code, remote) | Health **GREEN**: `checklyra.com/api/health` ok (prod, non-beta); `mcp.checklyra.com/health` ok; MCP live `build_sha` == main HEAD `e544531` (deploy in lockstep); recent develop CI (PR Checks / CodeQL / **E2E Tests PR-gate**) all `success`. lyra suite **PASS=5** (lint/type-check/unit/scripts/build); `integration`+`e2e` **UNVERIFIED** env-gaps (BUGS-51 — no `tests/integration/` dir; pre-baked Playwright chromium rev `1194` ≠ `@playwright/test@1.61.1`'s v`1228`, download CDN network-blocked). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. | **No product FAIL — nothing to fix**; suite green, no test touched (Test Integrity honoured). Prod `npm audit` **0/0** both repos; dev-only: lyra 3 moderate, mcp 0. TODO/FIXME scan: 2 hits, both ticket-tracked (KAN-184). | **Report-only (out of autonomous bounds — security/RLS/auth/migration/gov):** SEC-83 (MCP suspended-actor write bypass), SEC-43 (admin RPC `authenticated` EXECUTE), SEC-3 (branch-protection SoD), SEC-30/31/23 (backup/DR hardening), SEC-32 (admin-MCP sign-off) — all already open/In-Progress in Jira. **ACTION NEEDED:** `fix(SEC-76)` (anti-phishing) has sat unpromoted on develop **10 days**. | **HELD — manual sign-off (NOT auto-shipped).** `origin/main..origin/develop` = **2 commits**: `fix(SEC-76)` DCR anti-phishing on OAuth consent — **carries a migration** (`…is_first_party.sql` + `oauth_clients.is_first_party` column) **and** an OAuth consent-route change — plus `docs(KAN-283)`. Migration + security-route change **disqualify the fix-only auto-promote path** → not shipped. Manual chain: `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-18 | Weekly Health+Regression (Claude Code, remote) | Health **green**: `checklyra.com/api/health` ok (prod), `mcp.checklyra.com/health` ok. develop HEAD `adf5e4a` (from green merged PR #476; no red CI). lyra **PASS=5** (lint/type-check/unit/scripts/build), **FAIL=0**; `e2e`+`integration` **UNVERIFIED** (BUGS-51 env gaps — see note). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. npm audit: lyra prod **0** / dev **3 moderate**; mcp-server **0**. | **No product bug** — no FAIL; **no test modified/skipped/weakened** (Test Integrity Policy). Both UNVERIFIED are the tracked BUGS-51 env gaps: pre-baked chromium rev `1194` ≠ `@playwright/test@1.61.1`'s required v`1228` and the Playwright CDN is network-blocked (`403 host not permitted`) → `10 skipped`; and no `tests/integration/` dir → jest `0 matches`. Neither is an assertion failure. | **STOP — release HELD (see next col).** Broad open SEC/BUGS backlog (SEC-83/43/31/30/23/3, BUGS-61/62, etc.) is security/RLS/auth/data-model/architectural → **out of autonomous scope, report-only**. **SEC-88** already tracks the develop merge pipeline being stalled ~11 days (40+ open draft PRs). | **NO auto-promote — manual sign-off required.** `origin/main..origin/develop` = **2 commits**: `fix(SEC-76)` DCR anti-phishing **+ `docs(KAN-283)`**. The SEC-76 change carries a **DB migration** (`oauth_clients.is_first_party`) **and a user-facing consent-screen change** → disqualified from fix-only auto-promote (boundary 1). Migration is applied dev+staging **but NOT prod**; prod code without it would break `getOauthClient()`. **Apply the prod migration first, then** `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-19 | Weekly Health+Regression (Claude Code, remote) | Health **GREEN**: `checklyra.com/api/health` `ok` (prod); `mcp.checklyra.com/health` `ok`; develop CI (PR Checks + E2E Tests gate + CodeQL) all **success**. lyra **PASS=5** (lint/type-check/unit/scripts/build), **FAIL=0**; `integration`+`e2e` **UNVERIFIED** (BUGS-51 env gaps — no `tests/integration/` dir; Playwright chromium rev `1194` present vs required `1228`, CDN network-blocked → browser launch = `Executable doesn't exist`, never an assertion). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. `npm audit --omit=dev`: **0 prod vulns** both repos. | **None** — no product FAIL; **no test modified/skipped/weakened** (Test Integrity Policy). No new defect surfaced by the suite. | **Open SEC/BUGS backlog is all out-of-bounds (security/RLS/auth/architectural → report-only):** SEC-88 (develop→main merge pipeline stalled ~11 days, 40+ open draft PRs incl. CI-green security fixes unshipped), SEC-87 (Daily Security routine's Confluence heartbeat silently stopped 2026-07-06), SEC-83/43/32/31/30/23 (High). BUGS-51 E2E env gap persists (needs matching Playwright browser baked into the runner image or the Playwright CDN allowlisted). | **NO auto-promote — MANUAL sign-off required.** `origin/main..origin/develop` = **2 commits**: `fix(SEC-76)` DCR anti-phishing on the OAuth consent screen **carries a migration** (`oauth_clients.is_first_party`) **+ new user-facing consent-screen UI + is auth-sensitive**, and `docs(KAN-283)` (docs-only). SEC-76 disqualifies fix-only auto-promote (migration + user-facing capability) → release **NOT fix-only** → held at prod gate. **ACTION NEEDED (manual):** apply the SEC-76 migration to prod Supabase, then `promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-20 | Weekly Health+Regression (Claude Code, remote) | Health **GREEN**: `checklyra.com/api/health` prod `ok`; `mcp.checklyra.com/health` `ok`; develop HEAD `adf5e4a` (PR #476) — Deploy-to-Development + CodeQL both `success`. lyra **PASS=5 / FAIL=0** (lint/type-check/unit **2141/174 suites**/scripts **90/8**/build); `e2e`+`integration` **UNVERIFIED** (known **BUGS-51** env gaps — no `tests/integration/` dir → jest `No tests found`; pre-baked Playwright chromium rev `1194` ≠ `@playwright/test@1.61.1` required v`1228`, CDN `403 host not permitted`). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS, npm audit **0**. | **None to fix — no product defect.** Both UNVERIFIED lines are the BUGS-51 infra false-positive (never an assertion failure); **no test modified/skipped/weakened** (Test Integrity honoured). npm audit prod **0/0** both repos (lyra dev-only: 3 moderate — js-yaml/uuid via `@lhci` Lighthouse-CI tooling; not autonomous-fixable without `--force` breaking `@lhci`). Open High/Med SEC + BUGS-61/62 all security/RLS/auth/architectural/data-model → **report-only, out of autonomous bounds**, all already tracked/In-Progress. | **Release HELD (manual sign-off).** Pending develop→main = `fix(SEC-76)` **[contains DB migration `20260707193000_sec76_oauth_clients_is_first_party.sql` + user-facing OAuth consent-screen change]** + `docs(KAN-283)`. NOT fix-only-no-migration → auto-promote gate **fails by design**. **Verified read-only:** prod Supabase does **NOT** yet have `oauth_clients.is_first_party` → the migration must be applied to prod **before** the code promotes or `getOauthClient()` breaks the consent screen. Citing **SEC-88**: develop→main pipeline stalled ~13 days (40+ open draft PRs). | **NO auto-promote — manual only.** Release holds a migration + user-facing change. ACTION NEEDED (human, in order): 1) apply SEC-76 migration to **prod** Supabase; 2) `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
