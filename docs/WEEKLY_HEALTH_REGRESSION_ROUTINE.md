# Weekly Health + Regression + Autonomous Bug-Fix — Claude Code routine

> Runs **weekly**, **global coverage** (both repos, all test types). Mirrors the structure of `DAILY_SECURITY_CHECK_ROUTINE.md`.
> Deterministic test/build runner: `scripts/weekly-health-regression.sh`. The agent does triage, autonomous fixes (on `develop` via `claude/*` PRs), Jira logging, and a **release rehearsal that stops at the manual production gate**.

This routine checks Lyra is healthy, runs the full regression + E2E + build suite across `luisa-sys/lyra` and `luisa-sys/lyra-mcp-server`, **fixes the bugs it can on a fully autonomous basis**, logs everything in Jira, and **rehearses the release pipeline** — stopping before the one step the project forbids automating.

## 🚦 Hard boundaries (do not cross — encoded in the prompt)

1. **Production promote — MANUAL ONLY. The fix-only auto-promote exception was WITHDRAWN 2026-07-23.** The routine prepares release-readiness (classify pending commits, may rehearse `develop → staging`) and **reports** — it must NOT run `promote-to-production.yml` or `promote-staging-to-beta.yml` under any condition, including an all-fixes pending set. This reverts to the project's default "production promote is always manual" rule (`CLAUDE.md` → Deployment Pipeline). Historical context: from 2026-06-21 to 2026-07-23 an owner-authorized exception let the routine auto-promote **only when every change pending on `develop` ahead of `main` was a bug-FIX** (a BUGS/SEC defect, `fix:`-type — never a feature) after the full suite was green through staging + beta, always via `promote-to-production.yml` (built-in smoke + auto-rollback). Luisa withdrew that authority on 2026-07-23; do not act on the historical text unless she reinstates it here in writing with a new date.
2. **Test Integrity Policy.** A failing test means the *code* is wrong, not the test. The routine must **never** modify, weaken, skip, or delete a test to go green. If a fix would require a test change, it **STOPS and reports** for sign-off.
3. **Look and text belong to Luisa.** The routine must never change how a Lyra webpage looks or reads. Off-limits: `src/app/**/*.tsx` (pages, layouts, colocated components), `src/components/**`, `src/app/globals.css` and any styling/design token, `postcss.config.mjs`, `public/` brand assets, page titles and metadata, and the user-visible copy modules (invite/SMS/email templates, affiliation/organise field labels). Still in scope: `src/app/api/**`, `route.ts` handlers, the rest of `src/lib/**`, `src/app/admin/**`, `src/middleware.ts`, `scripts/**`, `.github/**`, `docs/**`, `tests/**`, `supabase/**`. An auto-fix that would touch a page, component, stylesheet, brand asset, or user-visible string is NOT a fix-only change — even a one-word fix, an accessibility fix, or a test fix. If a test asserting page copy or design fails, the page is right and the change is wrong — report it, never relax the assertion. `scripts/check-ui-copy-ownership.sh` is the mechanical backstop; a red result is the rule working, not a bug to route around. (Filed as KAN-430 — this rule was present in the live scheduled prompt before this doc caught up.)
4. **Autonomy with a stop-line.** Fix what's unambiguous and low-risk on a `claude/*` branch → PR to `develop`. For anything ambiguous, architectural, security-sensitive, data-model/RLS/migration, UI/copy (rule 3), or requiring a test change → **STOP and report to the user**. (Mirrors your instruction: "Stop where you are unsure … otherwise act autonomously.")
5. **No self-merge to protected branches.** Open PRs; the routine may merge its own fix PR to `develop` **only** when CI is green and the change is low-risk — never to `staging/beta/main`.

## How the run is split

| Layer | Driven by | Covers |
|---|---|---|
| Tests + build | `scripts/weekly-health-regression.sh` | lint, type-check, unit, scripts, integration, E2E, build — honest PASS/FAIL/UNVERIFIED |
| Live health | agent (curl / connectors) | `*/api/health`, MCP `/health`, deploy SHAs, CI status |
| Triage + fix | agent | open `claude/*` PR per fix, log Jira (BUGS/SEC), respect the boundaries above |
| Release | agent (GitHub connector) | prepare + report only — may rehearse `develop → staging`, **never** promotes past staging (no auto-promote-to-production path exists, fix-only or otherwise, per the 2026-07-23 withdrawal — see boundary 1) |
| Self-update | agent | propose improvements to THIS routine's script/prompt/doc via a PR (never silent) |

## Prerequisite (same as the other routines)

Routines clone the **default branch (`main`)**; this tooling lives on `develop`. The prompt's first lines check out `develop` (and STOP if the script isn't there).

## Setup

- **Connectors:** GitHub + Atlassian (+ Supabase/Cloudflare/Vercel if you want the agent to read deploy/runtime state). Remove the rest.
- **Setup script** (this routine *does* need one — it runs the suite):
  ```bash
  npm ci
  npx playwright install --with-deps   # for E2E
  ```
  Repeat for `lyra-mcp-server` if covering it in the same run (or use a second routine).
- **Network:** **Custom** allowlist with `*.checklyra.com`, `mcp.checklyra.com`, `mcp-dev.checklyra.com` (for live health + E2E targets) + the default package registries (for `npm ci`).
- **Schedule:** **Weekly** (e.g. Monday 06:00 local — before the workweek; note the Sunday 23:00 UTC develop→staging auto-promote already runs).
- **Permissions:** leave **"Allow unrestricted branch pushes" OFF** (so it can only push `claude/*` — this is what keeps it off `main/beta/staging`). No prod tokens in the env.

## The routine prompt

```
FIRST run: git fetch origin develop && git checkout develop
Then verify scripts/weekly-health-regression.sh exists; if not, STOP and say the
checkout failed — do NOT improvise.

Read the repo for context first: CLAUDE.md, docs/RUNBOOK.md, docs/RELEASE_POLICY.md,
docs/ARCHITECTURE.md. Obey them. Three rules are absolute:
  (A) PRODUCTION PROMOTE IS MANUAL — never run promote-to-production.yml, never run
      promote-staging-to-beta.yml, never push to main/beta/staging, never vercel-promote
      to prod. You prepare + report only. Luisa runs the prod gate herself, always.
  (B) TEST INTEGRITY — never modify/skip/weaken/delete a test to make it pass. If a fix
      needs a test change, STOP and report for sign-off.
  (C) LOOK AND TEXT BELONG TO LUISA — you never change how a Lyra webpage looks or reads.
      Off-limits: src/app/**/*.tsx (pages, layouts, colocated components), src/components/**,
      src/app/globals.css and any styling or design token, postcss.config.mjs, public/ brand
      assets, page titles and metadata, and user-visible copy modules. Explicitly still yours:
      src/app/api/**, every route.ts handler, the rest of src/lib/**, src/app/admin/** (Luisa-
      only console behind Cloudflare Access), src/middleware.ts, scripts/**, .github/**,
      docs/**, tests/**, supabase/**. An auto-fix that would edit a page, component,
      stylesheet, brand asset or user-visible string is NOT a fix-only change — do not
      auto-fix it. STOP, name the exact files, and lead your report with ACTION NEEDED. This
      holds even for a one-word fix, an accessibility fix, or a test fix. If a test asserting
      page copy or design fails, the page is right and your change is wrong — report it,
      never relax the assertion. scripts/check-ui-copy-ownership.sh is the mechanical
      backstop — a red result is the rule working, not a bug to route around.

GOAL: confirm Lyra is healthy, run full regression + E2E, fix what you safely can,
log everything, and rehearse the release pipeline up to (not through) the prod gate.

1. HEALTH: curl https://checklyra.com/api/health and https://mcp.checklyra.com/health;
   check the latest CI run on develop is green (GitHub connector). Note anything red.
2. REGRESSION + E2E: run  RUN_E2E=1 bash scripts/weekly-health-regression.sh  and capture
   the PASS/FAIL/UNVERIFIED lines. (For lyra-mcp-server coverage, clone/build/test it too.)
3. TRIAGE + FIX (autonomous, bounded):
   - For each FAIL, find the ROOT CAUSE in the application code and fix it on a new
     claude/ branch. Re-run the suite to confirm green WITHOUT touching any test.
   - Open a PR to develop. If CI is green and the fix is low-risk, you may merge it to
     develop. NEVER merge to staging/beta/main.
   - STOP and report (do not fix) anything ambiguous, architectural, security/RLS/auth,
     migration/data-model, that touches look/text (rule C), or that would require a test
     change. List it for the user.
   - Log every bug + fix in Jira the usual way: BUGS (6-section) for defects, SEC for
     security findings, with the failing output + your diagnosis + the PR link.
4. RESIDUAL BUGS: also scan for residual/known issues (open BUGS/SEC tickets, TODO/FIXME,
   flaky tests, Dependabot/CodeQL alerts) and fix the safe ones under the same rules.
5. RELEASE (PREPARE ONLY — the promote gate is Luisa's; see boundary 1. The
   2026-06-21 fix-only auto-promote exception referenced by earlier revisions of
   this step was WITHDRAWN 2026-07-23 and has no live successor — do not run
   promote_mode=auto-fix-only or any variant regardless of what is pending):
   - Determine what is pending on develop ahead of main (the changes that WOULD ship to prod):
     git log --oneline origin/main..origin/develop. CLASSIFY every commit as fix vs feature,
     and flag any commit touching look/text under rule C (if the live prompt carries one).
   - You MAY promote as far as STAGING to rehearse the chain: promote-to-staging.yml, then
     verify staging is green + healthy. Do NOT go further.
   - NEVER run promote-staging-to-beta.yml or promote-to-production.yml, even when every
     pending change is a bug-fix. Luisa runs those herself, always.
   - Report release-readiness and end with the exact commands for Luisa to run herself:
       gh workflow run promote-staging-to-beta.yml -f confirm=promote
       gh workflow run promote-to-production.yml -f confirm=PRODUCTION
6. SELF-UPDATE: if you found a way this routine (script/prompt/doc) should improve, open a
   PR proposing it — do NOT silently change your own behaviour.
7. REPORT + LOG: append a run-log row to this doc (date, PASS/FAIL counts, bugs fixed +
   tickets, what you STOPPED on, release-readiness) via a claude/ PR. If anything is red
   or you stopped on something, put a clear "ACTION NEEDED:" summary at the top of your reply.
```

## Run log

| Date | Runner | Suite result | Bugs fixed → tickets/PRs | Stopped-on (needs user) | Release-ready? |
|---|---|---|---|---|---|
| 2026-06-21 | _(initial — routine authored; fix-only auto-promote authorised 2026-06-21)_ | — | — | — | auto for fix-only; features manual |
| 2026-06-24 | Claude Code (Opus) | lyra PASS=5 (lint/type-check/unit/scripts/build); integration+e2e = env gaps → **UNVERIFIED** (no `tests/integration/`; Playwright browsers rev 1194 vs required 1223, CDN blocked) — _not_ product FAILs. lyra-mcp-server: tsc + 565/565 tests green. Health: checklyra.com + mcp.checklyra.com `/health` OK; develop CI (Deploy/Lint&Unit/CodeQL) green. | qs moderate DoS (GHSA-q8mj-m7cp-5q26) in lyra-mcp-server → SEC-35 + draft PR #91 (lockfile qs 6.15.0→6.15.2). BUGS-51 false-FAIL reproduced (PR #352 still pending) → confirmation comment only. | **STOP (sign-off):** PR #352 (BUGS-51 runner-honesty) + PR #91 (SEC-35 qs bump) are draft, awaiting merge. Open HIGH SEC items (SEC-29/30/31/3/23/2) untouched — security/RLS, out of autonomous bounds. integration+e2e need env fix (#352) or browser/CDN allowlist to verify. | **Nothing to release** — develop is 0 commits ahead of main (chain fully flushed). No promotion run. |
| 2026-06-25 | Claude Code (weekly routine) | lyra: lint/type-check/unit (1803/139)/scripts/build **PASS**; e2e + integration **UNVERIFIED** (env gaps — Playwright browsers rev 1194 vs required 1223 + webkit 2287, CDN network-blocked; no `tests/integration/` dir). mcp-server: tsc clean + **583/583** green. Live health green (checklyra `ok`, mcp `ok`); develop HEAD 5a74607 CI + CodeQL green. | None to fix — both FAIL lines are the known **BUGS-51** env-gap false-positives; fix already in **PR #352** (draft, awaiting sign-off — not self-merged per boundary 6). Posted weekly-confirmation comment on BUGS-51. | **BUGS-51** false-FAIL persists → recommend merging **#352**. **BUGS-57** (High): prod MCP OAuth auth rejected — auth/architectural, out of routine scope. npm audit: prod 0, dev 21 moderate. SEC-35 (qs DoS in mcp-server) tracked. | **NO auto-promote** — 12+ `feat()` pending on develop→main (Convene, admin back-office, /status, age-verify, RS256, per-user entitlements, KAN-326). Release holds features → **manual prod sign-off required**: `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-06-27 | weekly health/regression routine (Claude Code, remote) | **Real-green.** lyra: lint/type-check/unit/scripts/build PASS; E2E (chromium, against local build with CI dummy-Supabase env) 17 passed / 0 failed / 5 skipped. mcp-server: build OK, 525/525 tests pass. Two script-level **false-FAILs** from `RUN_E2E=1` — both environment/harness gaps, not code defects: (a) `integration` — no `tests/integration` dir exists so jest exits 1 on 0 matches; (b) `e2e` — webkit/`mobile-safari` browser binary absent (download 403 by network policy) **and** the E2E phase runs `npm run dev` with no Supabase env, so `requireEnv` throws on `/waitlist` + `[slug]` 404. Supplying CI's dummy Supabase env + the installed chromium binary → all pass. Tracked: BUGS-51 / BUGS-58. | None — no application defect found; **no test modified/skipped/weakened** (Test Integrity Policy). Health GREEN: checklyra.com/api/health + mcp.checklyra.com/health both ok; latest develop CI green. | Release HELD at the prod gate per this run's **absolute** boundary "production promote is manual — prepare + report only" (GOAL: rehearse *up to, not through* the gate). No push to staging/beta/main, no promote workflow run. | **YES — fix-only, ready for manual promote.** Pending on develop ahead of main = 1 commit: `fix(SEC-40)` cookie-domain scoping (src/lib/cookie-domain.ts + its unit test; no feature/route/table/MCP tool/migration). ACTION NEEDED → manual: `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-06-29 | Claude Code | lyra: lint/type-check/unit/scripts/build **PASS**; integration + e2e **UNVERIFIED** (env gap, not code — BUGS-51). lyra-mcp-server: **585 tests PASS** (38 suites). Health: checklyra.com + mcp.checklyra.com **OK**; latest CI green. | BUGS-51 self-update: `weekly-health-regression.sh` now classifies missing-Playwright-browser / no-tests-found as UNVERIFIED not FAIL (+5 unit tests) — draft PR on `claude/nice-meitner-z1zc16`. qs DoS bump (SEC-35/41) — draft PR on lyra-mcp-server `claude/sharp-faraday-z1zc16`. | **No code regressions.** E2E unverifiable in-sandbox: pre-baked browsers (rev 1194) out of lockstep with `@playwright/test@1.60.0` (rev 1223 + webkit 2287) and the Playwright CDN is network-blocked (403). Needs infra: bake matching browsers or allowlist the CDN (BUGS-51 §5). | **No release pending** — develop is 0 commits ahead of main (chain fully flushed); nothing to promote. |
| 2026-06-30 | Weekly Health+Regression (Claude Code) | Health green (`checklyra.com/api/health` ok prod; `mcp.checklyra.com/health` ok; develop HEAD `6cfe7a2` CI + CodeQL green). lyra **PASS=5** (lint/type-check/unit/scripts/build); `e2e`+`integration` are env-gap **UNVERIFIED** not FAIL (BUGS-51 — see note). lyra-mcp-server: `tsc` clean + **585/585** (38 suites) PASS. | **No product bug.** Both raw-runner FAILs are the BUGS-51 false-positive: Playwright browsers rev `1194` ≠ `@playwright/test@1.60.0`'s required `1223`+webkit `2287` (CDN blocked) → every e2e fail is `Executable doesn't exist`, never an assertion; and no `tests/integration/` dir → jest `No tests found`. Fix already merged as **#404** (BUGS-51, +5 unit tests) this session. qs DoS (SEC-35) already in draft PR #91. | **Merge ONE BUGS-51 fix (#404) and close the dup drafts** (#352, #392) — done this session. BUGS-57 (High — prod MCP OAuth clients rejected) still open: auth/architectural → report-only. | **Nothing to release** — `origin/main..origin/develop` = **0 commits** (chain already flushed to main). No promotion run. |
| 2026-07-07 | Weekly Health+Regression (Claude Code) | **No product FAIL.** lyra (develop `ed319b3`): lint/type-check/unit/scripts/build **PASS=5**, FAIL=0; `integration`+`e2e` **UNVERIFIED** — known **BUGS-51** env gaps (no `tests/integration/` dir → jest 0-match; only chromium rev `1194` baked, no webkit/firefox + `@playwright/test@1.61.1` CDN-blocked → 10 e2e skipped). lyra-mcp-server (`origin/main e5445311`): `tsc` clean + **637/637** (41 suites) PASS. Health **GREEN**: checklyra `/api/health` prod `ok`, mcp `/health` `ok`; develop HEAD `ed319b3` CI (CodeQL + Deploy) green. npm audit prod **0/0** both repos (SEC-35 `qs` DoS now **cleared** on mcp main via #97). | None — no application defect surfaced; **no test modified/skipped/weakened** (Test Integrity Policy). No new BUGS/SEC filed. | Both UNVERIFIED lines are env-only (BUGS-51) — need baked matching browsers / CDN allowlist + a `tests/integration/` path to verify in-sandbox; not code. Citing (owned by Daily Security, not re-derived): **SEC-79** watchdog FAIL — `health-check.yml` + `weekly-report.yml` `disabled_manually`. Large open security/RLS backlog (SEC-43/31/30/23/3 High + others) all report-only, out of autonomous scope. | **HELD — release holds features + a migration.** 43 commits `develop→main` include `feat(dashboard/profile/ux)` (#418/#416/#408), the KAN-404 profile-editor epic, and the SECURITY DEFINER/RLS migration batch (#443) → **manual prod sign-off required**. Drift 🔴 **RED** (43 ≥ 15 threshold). ACTION NEEDED → manual: `promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-08 | Weekly Health+Regression (Claude Code, remote) | Health **green**: `checklyra.com/api/health` ok (prod); `mcp.checklyra.com/health` ok; develop HEAD `adf5e4a` — CodeQL + Deploy-to-Development + E2E-gate + PR-Checks all green. lyra **PASS=5** (lint/type-check/unit/scripts/build), FAIL=0; `integration`+`e2e` **UNVERIFIED** (BUGS-51 env gap — see note). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. | **No product bug** — no test modified/skipped/weakened (Test Integrity Policy). The two UNVERIFIED lines are the tracked BUGS-51 env gap: no `tests/integration/` dir → jest "0 matches"; and pre-baked Playwright browser rev `1194` ≠ `@playwright/test`'s now-required rev **`1228`** (`chromium_headless_shell-1228` missing), CDN download **403/network-blocked** — confirmed by attempting `npx playwright install chromium` (fails) and running `--project=chromium` locally (every fail is `Executable doesn't exist`, never an assertion). npm audit: lyra prod **0**, dev 3 moderate; mcp-server **0**. | **STOP — release HELD (needs manual sign-off).** Pending `main..develop` = 2 commits: `fix(SEC-76)` DCR anti-phishing **carries a DB migration** (`oauth_clients.is_first_party`, applied dev+staging, **NOT prod**) + consent-screen/route changes → **disqualified from fix-only auto-promote** (boundary 1 excludes migrations/tables). Prod Supabase must be migrated before/with any promote or gated OAuth reads break. E2E still needs infra fix (bake rev-1228 browsers or allowlist Playwright CDN — BUGS-51). | **NO auto-promote.** Release holds a **migration** (data-model change) → manual prod sign-off required: apply the KAN/SEC-76 migration to prod Supabase, then `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-09 | Weekly Health+Regression (Claude Code, remote) | Health **GREEN** (`checklyra.com/api/health` ok prod; `mcp.checklyra.com/health` ok; develop HEAD `adf5e4a` Deploy+CodeQL green). lyra **PASS=5** (lint/type-check/unit/scripts/build), FAIL=0; `integration`+`e2e` **UNVERIFIED** (BUGS-51 env gap — no `tests/integration/` dir; Playwright browsers rev `1194` ≠ v1.61.1's required `1228`, CDN download blocked). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. npm audit: lyra prod **0** / 3 moderate dev-only; mcp-server **0**. | **None — no product FAIL; no test modified/skipped/weakened** (Test Integrity Policy). Both UNVERIFIED lines are the known BUGS-51 infra gap, not code defects. | **BUGS-51** e2e/integration env gap persists (infra: bake matching Playwright browsers or allowlist the CDN). Open **High** SEC items (SEC-43/3/32/31/30/23) untouched — security/RLS/auth/DR, out of autonomous bounds → report-only. | **HELD — NOT fix-only.** `origin/main..origin/develop` = **2 commits**: `fix(SEC-76)` DCR anti-phishing **includes a DB migration** (`oauth_clients.is_first_party`) + user-facing OAuth consent-screen changes, plus `docs(KAN-283)` (docs-only). Migration + user-facing capability ⇒ **outside** the fix-only auto-promote boundary → **manual sign-off required**: `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-10 | Weekly Health+Regression (Claude Code, remote) | **All-green, zero product failures.** Health: `checklyra.com/api/health` ok (prod); `mcp.checklyra.com/health` ok; develop HEAD `adf5e4a` CI green (Deploy to Development + CodeQL both `success`). lyra suite (`RUN_E2E=1`): **PASS=5** (lint/type-check/unit/scripts/build), FAIL=0, **UNVERIFIED=2** — `integration` (no `tests/integration/` dir) + `e2e` (BUGS-51 browser-rev gap). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. | **None — no application defect found; no test modified/skipped/weakened** (Test Integrity Policy). Every E2E "failure" is the BUGS-51 env gap: `@playwright/test@1.61.1` needs `chromium_headless_shell` rev **1228** but the sandbox has rev **1194**, and the Playwright CDN is network-blocked (`npx playwright install` → Download failure) — errors fire at `browserType.launch` (2ms) before any assertion, verified by direct chromium-only run against live prod. | E2E remains unverifiable in-sandbox (BUGS-51 §5 infra: bake matching browsers or allowlist the CDN). npm audit: lyra prod **0**, dev **3 moderate** (js-yaml + uuid, both via `@lhci` Lighthouse-CI tooling — dev-only, non-shipping; uuid fix is breaking) — flagged, not auto-fixed. Open HIGH SEC (SEC-43/3/32/31/30/23) all security/RLS/backup — out of autonomous bounds, tracked. | **HELD for manual sign-off.** `origin/main..origin/develop` = **2 commits**: `docs(KAN-283)` #475 (docs-only) + `fix(SEC-76)` #476 — but #476 **carries a DB migration** (`…sec76_oauth_clients_is_first_party.sql`, adds `oauth_clients.is_first_party`). A pending migration disqualifies fix-only auto-promote per boundary 1, **and** this run's absolute (A) makes prod promote manual regardless. ACTION NEEDED → manual, in lockstep with the migration reaching prod Supabase: `promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-11 | Weekly Health+Regression (Claude Code) | Health green (`checklyra.com/api/health` ok prod; `mcp.checklyra.com/health` ok, build_sha `e5445311…` = `lyra-mcp-server@main` HEAD; develop HEAD `adf5e4a` Deploy+CodeQL green). lyra **PASS=5** (lint/type-check/unit/scripts/build), **FAIL=0**; `integration`+`e2e` = env-gap **UNVERIFIED** not FAIL (BUGS-51 — no `tests/integration/` dir; Playwright browsers unavailable → **10 skipped**, error at launch never an assertion). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. | **None — no product FAIL.** Both UNVERIFIED lines are the known BUGS-51 env gaps; **no test modified/skipped/weakened** (Test Integrity Policy). npm audit: lyra prod **0** / 3 moderate dev-only (js-yaml via coverage tooling, uuid via @lhci/cli — Dependabot-tracked); mcp-server **0**. | **Report-only (out of autonomous bounds):** open High SEC items (SEC-43/3/32/31/30/23) + the Medium SEC/BUGS batch (SEC-76/62/61/60/…, BUGS-61/62) untouched — all security/RLS/auth/data-model/architectural. E2E unverifiable in-sandbox (BUGS-51). | **HELD — manual sign-off.** `origin/main..origin/develop` = **2 commits**: `docs(KAN-283)` #475 (docs-only) + `fix(SEC-76)` #476 which carries a **DB migration** (`…sec76_oauth_clients_is_first_party.sql`, adds `oauth_clients.is_first_party`) **+ user-facing OAuth consent-screen changes** → outside fix-only auto-promote (boundary 1). No promotion run. ACTION NEEDED → manual: `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-12 | Weekly Health+Regression (Claude Code, remote) | Health **GREEN** (`checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `{status:ok, version:1.0.0}`; develop HEAD `adf5e4a`, latest develop CI — PR Checks + CodeQL + E2E-PR-gate — all **success**). lyra **PASS=5** (lint/type-check/unit/scripts/build), **FAIL=0**, `integration`+`e2e` **UNVERIFIED** (BUGS-51 env gaps — no `tests/integration/` dir; Playwright 1.61.1 vs pre-baked chromium rev `1194` + no webkit for `mobile-safari`, 10 skipped). Note: real E2E DID run **green in develop CI** ("E2E Tests (PR gate) success"), so E2E is verified upstream, only unreproducible in-sandbox. lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. | **No product bug — nothing to autonomously fix.** No test modified/skipped/weakened (Test Integrity Policy). npm audit: both repos **0 prod** vulns; lyra has 3 **dev-only** moderates (js-yaml/uuid via `@lhci/cli` Lighthouse tooling — uuid fix needs a breaking `--force` downgrade, not low-risk → report-only). | **STOPPED — release is NOT fix-only → manual sign-off.** Pending `origin/main..origin/develop` = 2 commits: `docs(KAN-283)` #475 (docs-only) **and** `fix(SEC-76)` #476 which carries a **DB migration** (`oauth_clients.is_first_party`) + user-facing `/oauth/authorize` consent-screen changes. Per boundary #1, a pending migration/data-model change ⇒ not auto-promotable. **Also: the SEC-76 migration is applied dev+staging but NOT prod** — code must not ship ahead of the prod column or gated OAuth reads break (KAN-327-style ordering). Open SEC/BUGS backlog (SEC-43 High, SEC-44/57/62/76 etc.) all security/RLS/auth → report-only, out of routine bounds. | **HOLD — manual prod sign-off required.** Migration must be applied to **prod Supabase** first, then: `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. No promotion run this session (boundary A). |
| 2026-07-13 | Weekly Health+Regression (Claude Code) | Health green (`checklyra.com/api/health` ok prod; `mcp.checklyra.com/health` ok; develop CI — PR Checks / E2E gate / CodeQL — all `success`). lyra **PASS=5** (lint/type-check/unit/scripts/build); `integration`+`e2e` env-gap **UNVERIFIED** not FAIL (BUGS-51 — see note). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. npm audit: lyra prod **0** / dev **3 moderate** (dev-only); mcp-server **0**. | **No product bug; no test modified/skipped/weakened.** Both raw-runner FAILs are the BUGS-51 env gap: no `tests/integration/` dir → jest `No tests found`; and pre-baked Playwright chromium rev `1194` ≠ required rev `1228` (`chromium_headless_shell-1228` absent, install CDN 403) → every e2e launch is `Executable doesn't exist`, never an assertion. Bounded chromium-only retry confirmed the same launch error. | **STOP — release holds a migration + user-facing change → manual sign-off.** develop is **2 commits** ahead of main: `docs(KAN-283)` #475 (compliance docs only) + **`fix(SEC-76)` #476** which, though a security fix, **carries a DB migration** (`oauth_clients.is_first_party`, `20260707193000`) **and a user-facing OAuth consent-screen UI change** — outside the fix-only auto-promote boundary. **Prod Supabase does NOT yet have that migration** (latest prod = `kan349_404_promote_batch_prod`); promoting #476 code without applying it first would break the OAuth consent flow. | **HELD at staging gate — NOT auto-promoted.** Manual sequence required: apply the SEC-76 migration to prod Supabase, then `promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-14 | Weekly Health+Regression (Claude Code) | Health **GREEN**: `checklyra.com/api/health` ok (prod); `mcp.checklyra.com/health` ok; mcp `build_sha e544531` **== `origin/main` HEAD** (Railway in sync); develop CI (PR Checks / E2E gate / CodeQL / Scheduled Health) all green. lyra **PASS=5** (lint/type-check/unit/scripts/build), **FAIL=0**, **UNVERIFIED=2** — `integration` (no `tests/integration/` dir) + `e2e` (BUGS-51 env gap: sandbox chromium **rev 1194** ≠ `@playwright/test@1.61.1` required **rev 1228**, webkit absent, download CDN blocked → `Executable doesn't exist`, never an assertion). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS (well above 217 floor). | **None — zero product FAILs.** No test modified/skipped/weakened (Test Integrity Policy). | **Release HELD — manual sign-off (see next col).** E2E remains env-UNVERIFIED (BUGS-51 — infra browser-lockstep, out of code scope; classifier already correct). Open HIGH SEC (SEC-3/23/30/31/32/43) + Medium SEC/BUGS untouched — security/RLS/auth/architectural, out of autonomous bounds. | **NO auto-promote — NOT fix-only.** `origin/main..origin/develop` = **2 commits**: `docs(KAN-283)` (docs-only, inert) **+** `fix(SEC-76)` DCR anti-phishing — which carries a Supabase **migration** (`oauth_clients.is_first_party`, applied dev+staging, **NOT prod**) **and** new user-facing OAuth consent-screen UI (verified/unverified badges, redirect-host). Migration + user-facing capability → excluded from the fix-only auto-promote path. **ACTION NEEDED (manual):** apply the SEC-76 migration to prod Supabase, then `promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-15 | Weekly Health+Regression (Claude Code) | Health **GREEN** (`checklyra.com/api/health` ok prod; `mcp.checklyra.com/health` ok; `beta.checklyra.com` health ok + `/dashboard` 307→`/login`). develop HEAD `adf5e4a` CI: PR Checks + E2E gate + CodeQL **green**. lyra **PASS=5** (lint/type-check/unit/scripts/build), FAIL=0; `integration`+`e2e` **UNVERIFIED** (BUGS-51 env gap — no `tests/integration/` dir; Playwright browsers rev `1194` ≠ now-required `1228`, CDN blocked). A manual chromium bypass reproduced the exact rev-mismatch (`chromium_headless_shell-1228` missing) — env, not a regression; **no test modified** (Test Integrity Policy intact). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. | **None — no product FAIL, no fix required.** | **Beta gate smoke** scheduled run (main, 07:45Z) failed once at step `/dashboard auth-gate` — transient CF-challenge-class flake (KAN-279 class); live beta verified correct now (307→`/login`, health ok). npm audit: lyra prod **0** / dev **3 moderate** (js-yaml, uuid — dev-tooling only; only fix is a breaking `@lhci/cli` downgrade → left); mcp-server **0/0**. Open BUGS/SEC backlog all security/RLS/auth/architectural (e.g. SEC-83/3/43/32/31/30/23; BUGS-61/62) → report-only, out of autonomous bounds. | **NO auto-promote — STOP at manual prod gate.** Pending develop→main = **2 commits**: `docs(KAN-283)` (docs-only) + `fix(SEC-76)` DCR anti-phishing — the latter, despite the `fix:` prefix, ships a **migration** (`oauth_clients.is_first_party` column), a **user-facing OAuth consent-screen** change, and is **auth/security**-sensitive → outside the fix-only auto-promote lane. Needs manual sign-off (verify the SEC-76 migration is applied to prod Supabase first): `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-16 | Weekly Health+Regression (Claude Code, Opus) | Health **GREEN** (checklyra.com/api/health ok prod; mcp.checklyra.com/health ok — prod MCP on latest main `e544531`; develop CI + CodeQL green). lyra: lint/type-check/unit/scripts/build **PASS=5, FAIL=0**; integration + e2e **UNVERIFIED** (BUGS-51 env gaps — no `tests/integration/` dir; Playwright browsers unavailable, 10 e2e skipped) — not code FAILs. lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS; npm audit 0. | **None** — zero product FAILs; no test modified/skipped/weakened (Test Integrity Policy). | **Release HELD — manual sign-off (see last col).** Open HIGH SEC (SEC-83/3/43/32/31/30/23) + medium SEC/BUGS untouched — security/RLS/auth/DR/migration, out of autonomous bounds. lyra dev-only npm audit: 3 moderate (js-yaml, uuid) in CI tooling (lighthouse/istanbul), not prod-facing. | **NO auto-promote — NOT fix-only.** `origin/main..origin/develop` = **2 commits**: `fix(SEC-76)` DCR anti-phishing **carries a migration** (`oauth_clients.is_first_party` new column) + is OAuth-security-sensitive; `docs(KAN-283)` privacy intake. Migration + security change → **manual gate**; prod Supabase migration must be applied by web team first. ACTION NEEDED (manual): `promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-17 | Weekly Health+Regression (Claude Code, remote) | Health **GREEN**: `checklyra.com/api/health` ok (prod, non-beta); `mcp.checklyra.com/health` ok; MCP live `build_sha` == main HEAD `e544531` (deploy in lockstep); recent develop CI (PR Checks / CodeQL / **E2E Tests PR-gate**) all `success`. lyra suite **PASS=5** (lint/type-check/unit/scripts/build); `integration`+`e2e` **UNVERIFIED** env-gaps (BUGS-51 — no `tests/integration/` dir; pre-baked Playwright chromium rev `1194` ≠ `@playwright/test@1.61.1`'s v`1228`, download CDN network-blocked). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. | **No product FAIL — nothing to fix**; suite green, no test touched (Test Integrity honoured). Prod `npm audit` **0/0** both repos; dev-only: lyra 3 moderate, mcp 0. TODO/FIXME scan: 2 hits, both ticket-tracked (KAN-184). | **Report-only (out of autonomous bounds — security/RLS/auth/migration/gov):** SEC-83 (MCP suspended-actor write bypass), SEC-43 (admin RPC `authenticated` EXECUTE), SEC-3 (branch-protection SoD), SEC-30/31/23 (backup/DR hardening), SEC-32 (admin-MCP sign-off) — all already open/In-Progress in Jira. **ACTION NEEDED:** `fix(SEC-76)` (anti-phishing) has sat unpromoted on develop **10 days**. | **HELD — manual sign-off (NOT auto-shipped).** `origin/main..origin/develop` = **2 commits**: `fix(SEC-76)` DCR anti-phishing on OAuth consent — **carries a migration** (`…is_first_party.sql` + `oauth_clients.is_first_party` column) **and** an OAuth consent-route change — plus `docs(KAN-283)`. Migration + security-route change **disqualify the fix-only auto-promote path** → not shipped. Manual chain: `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-18 | Weekly Health+Regression (Claude Code, remote) | Health **green**: `checklyra.com/api/health` ok (prod), `mcp.checklyra.com/health` ok. develop HEAD `adf5e4a` (from green merged PR #476; no red CI). lyra **PASS=5** (lint/type-check/unit/scripts/build), **FAIL=0**; `e2e`+`integration` **UNVERIFIED** (BUGS-51 env gaps — see note). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. npm audit: lyra prod **0** / dev **3 moderate**; mcp-server **0**. | **No product bug** — no FAIL; **no test modified/skipped/weakened** (Test Integrity Policy). Both UNVERIFIED are the tracked BUGS-51 env gaps: pre-baked chromium rev `1194` ≠ `@playwright/test@1.61.1`'s required v`1228` and the Playwright CDN is network-blocked (`403 host not permitted`) → `10 skipped`; and no `tests/integration/` dir → jest `0 matches`. Neither is an assertion failure. | **STOP — release HELD (see next col).** Broad open SEC/BUGS backlog (SEC-83/43/31/30/23/3, BUGS-61/62, etc.) is security/RLS/auth/data-model/architectural → **out of autonomous scope, report-only**. **SEC-88** already tracks the develop merge pipeline being stalled ~11 days (40+ open draft PRs). | **NO auto-promote — manual sign-off required.** `origin/main..origin/develop` = **2 commits**: `fix(SEC-76)` DCR anti-phishing **+ `docs(KAN-283)`**. The SEC-76 change carries a **DB migration** (`oauth_clients.is_first_party`) **and a user-facing consent-screen change** → disqualified from fix-only auto-promote (boundary 1). Migration is applied dev+staging **but NOT prod**; prod code without it would break `getOauthClient()`. **Apply the prod migration first, then** `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-19 | Weekly Health+Regression (Claude Code, remote) | Health **GREEN**: `checklyra.com/api/health` `ok` (prod); `mcp.checklyra.com/health` `ok`; develop CI (PR Checks + E2E Tests gate + CodeQL) all **success**. lyra **PASS=5** (lint/type-check/unit/scripts/build), **FAIL=0**; `integration`+`e2e` **UNVERIFIED** (BUGS-51 env gaps — no `tests/integration/` dir; Playwright chromium rev `1194` present vs required `1228`, CDN network-blocked → browser launch = `Executable doesn't exist`, never an assertion). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. `npm audit --omit=dev`: **0 prod vulns** both repos. | **None** — no product FAIL; **no test modified/skipped/weakened** (Test Integrity Policy). No new defect surfaced by the suite. | **Open SEC/BUGS backlog is all out-of-bounds (security/RLS/auth/architectural → report-only):** SEC-88 (develop→main merge pipeline stalled ~11 days, 40+ open draft PRs incl. CI-green security fixes unshipped), SEC-87 (Daily Security routine's Confluence heartbeat silently stopped 2026-07-06), SEC-83/43/32/31/30/23 (High). BUGS-51 E2E env gap persists (needs matching Playwright browser baked into the runner image or the Playwright CDN allowlisted). | **NO auto-promote — MANUAL sign-off required.** `origin/main..origin/develop` = **2 commits**: `fix(SEC-76)` DCR anti-phishing on the OAuth consent screen **carries a migration** (`oauth_clients.is_first_party`) **+ new user-facing consent-screen UI + is auth-sensitive**, and `docs(KAN-283)` (docs-only). SEC-76 disqualifies fix-only auto-promote (migration + user-facing capability) → release **NOT fix-only** → held at prod gate. **ACTION NEEDED (manual):** apply the SEC-76 migration to prod Supabase, then `promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-20 | Weekly Health+Regression (Claude Code, remote) | Health **GREEN**: `checklyra.com/api/health` prod `ok`; `mcp.checklyra.com/health` `ok`; develop HEAD `adf5e4a` (PR #476) — Deploy-to-Development + CodeQL both `success`. lyra **PASS=5 / FAIL=0** (lint/type-check/unit **2141/174 suites**/scripts **90/8**/build); `e2e`+`integration` **UNVERIFIED** (known **BUGS-51** env gaps — no `tests/integration/` dir → jest `No tests found`; pre-baked Playwright chromium rev `1194` ≠ `@playwright/test@1.61.1` required v`1228`, CDN `403 host not permitted`). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS, npm audit **0**. | **None to fix — no product defect.** Both UNVERIFIED lines are the BUGS-51 infra false-positive (never an assertion failure); **no test modified/skipped/weakened** (Test Integrity honoured). npm audit prod **0/0** both repos (lyra dev-only: 3 moderate — js-yaml/uuid via `@lhci` Lighthouse-CI tooling; not autonomous-fixable without `--force` breaking `@lhci`). Open High/Med SEC + BUGS-61/62 all security/RLS/auth/architectural/data-model → **report-only, out of autonomous bounds**, all already tracked/In-Progress. | **Release HELD (manual sign-off).** Pending develop→main = `fix(SEC-76)` **[contains DB migration `20260707193000_sec76_oauth_clients_is_first_party.sql` + user-facing OAuth consent-screen change]** + `docs(KAN-283)`. NOT fix-only-no-migration → auto-promote gate **fails by design**. **Verified read-only:** prod Supabase does **NOT** yet have `oauth_clients.is_first_party` → the migration must be applied to prod **before** the code promotes or `getOauthClient()` breaks the consent screen. Citing **SEC-88**: develop→main pipeline stalled ~13 days (40+ open draft PRs). | **NO auto-promote — manual only.** Release holds a migration + user-facing change. ACTION NEEDED (human, in order): 1) apply SEC-76 migration to **prod** Supabase; 2) `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-21 | Weekly Health+Regression (Claude Code, remote) | Health GREEN (`checklyra.com/api/health` ok prod; `mcp.checklyra.com/health` ok; develop HEAD `149eafd` Deploy-to-Development + CodeQL green). lyra **PASS=5** (lint/type-check/unit/scripts/build); `integration`+`e2e` env-gap **UNVERIFIED** not FAIL (BUGS-51 — no `tests/integration/` dir; pre-baked Chromium rev **1194** ≠ `@playwright/test@1.61.1` required rev **1228**, Playwright CDN 403-blocked). lyra-mcp-server: `tsc` clean + **638/638** (41 suites) PASS. | **Fixed:** prod-tree `brace-expansion` DoS (GHSA-3jxr-9vmj-r5cp, High) → **SEC-89** + draft **PR #537** (surgical 3-line lockfile bump 5.0.6→5.0.7; `npm audit --omit=dev` clean; no test touched). No product-code defect found; **no test modified/skipped/weakened.** | **STOP (sign-off) — SEC-90 (branch-wide gate blocker):** the `PR Quality Gate` + all `deploy-*.yml` run **full-tree** `npm audit --audit-level=high` (no `--omit=dev`), now RED on dev-only highs (`js-yaml`, dev `brace-expansion`) after an advisory-DB update → blocks **every** PR to develop **and the whole deploy/release chain** (PR #537's own gate is red for this reason, not the change). Verified remedy: `npm audit fix` (non-force) clears both highs lockfile-only, jest unchanged, suite green — OR adopt `--omit=dev` gate (SEC-51). Needs a decision. mcp-server body-parser low + dev brace-expansion → noted on **SEC-59** (advisory-only CI, not chain-blocking). | **NO auto-promote — release HOLDS FEATURES.** `origin/main..origin/develop` = 4 commits incl. 2 `feat:` (#535 showcase homepage, KAN-407 #534 age self-declaration) + `fix(SEC-76)` #476 + `docs(KAN-283)` #475. Manual prod sign-off required: `gh workflow run promote-to-production.yml -f confirm=PRODUCTION` (via staging→beta first) — **AND resolve SEC-90 first, else the deploy audit gate will block promotion.** |
| 2026-07-22 | Weekly Health+Regression (Claude Code) | Live health **GREEN**: `checklyra.com/api/health` ok (prod), `mcp.checklyra.com/health` ok, `beta.checklyra.com` ok + `/dashboard`→307 `/login` (auth gate holds live). lyra runner **PASS=5** (lint/type-check/unit/scripts/build), **FAIL=0**; `integration`+`e2e` env-gap **UNVERIFIED** (no `tests/integration/` dir; Playwright browsers unavailable → 10 skipped — BUGS-51). lyra-mcp-server: `tsc` clean + **637/637** (41 suites) PASS. **No product/test FAIL anywhere.** | **None auto-fixed** — the one real finding has no safe fix. **`npm audit --audit-level=high` is RED repo-wide** (prod-tree: `sharp@0.34.5` <0.35.0 libvips CVEs `CVE-2026-33327/33328/35590/35591` via `next@16.2.10`; + build-time `fast-uri@3.1.2`). npm's only fix downgrades next to 14.2.35 (breaking); forcing sharp≥0.35.0 exceeds next's declared `^0.34.5` (latest stable next 16.2.11 still pins it) → security-sensitive, out of autonomous bounds. Logged **SEC-91** (needs sign-off, 3 options). Confirmed this same gate fails the "PR Quality Gate" on all PRs (run 29882718731). Self-update: added an `audit` phase to the runner (would have caught this automatically). | **SEC-91 (High-impact, needs sign-off):** decide Option A (sharp override + verify image path) / B (await next fix) / C (allow-list gate). Until resolved, **every PR to develop + the whole deploy chain is blocked** by the audit gate. Prior open items untouched (report-only): SEC-89 (brace-expansion, In Progress), BUGS-57. | **NO release.** Two independent blockers: (1) `origin/main..origin/develop` = **6 commits incl. `feat()` (KAN-407 18+ self-declaration, showcase homepage)** → holds features → **manual prod sign-off required** regardless; (2) audit gate RED → deploy chain can't go green until SEC-91. No promotion run; stopped at classification per boundary 1. |
| 2026-07-23 | Weekly Health+Regression (Claude Code, Opus) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` ok; mcp prod `build_sha e0b5bec` **== `lyra-mcp-server@main` HEAD** (Railway in lockstep). develop HEAD `152d32e`: **CodeQL + Deploy-to-Development green**; but **`E2E Authed Journey (dev/staging)` RED** (see next col — env/credential, not product). lyra runner (`RUN_E2E=1`): **PASS=5** (lint/type-check/unit/scripts/build), **FAIL=1 (`audit`)**, `integration`+`e2e` **UNVERIFIED** (BUGS-51 — no `tests/integration/` dir; Playwright browsers unavailable → 10 skipped). lyra-mcp-server: `tsc` clean + **638/638** (41 suites) PASS. | **Fixed (2 audit reds, both draft PRs, not self-merged):** (1) **SEC-92** — the `audit` FAIL is a *fresh Next.js advisory batch* (SSRF/middleware-bypass/DoS/unauth Server-Function disclosure) in `next ≤16.2.10`, prod-tree, blocking all PRs + the deploy chain. Distinct from SEC-91 (sharp, already cleared on develop via #544's `sharp@0.35.3` override → prod-tree now 0 sharp vulns). Bumped `next 16.2.10→16.2.11` (patch) → `npm audit --audit-level=high` exit 0, `--omit=dev` 0 vulns, full suite green, no test touched → **PR #551**. (2) **SEC-59** — mcp-server `npm audit` RED (brace-expansion + fast-uri highs); non-force `npm audit fix` lockfile-only → exit 0, 638/638 green → **PR #124** (advisory-only CI, not chain-blocking). No test modified/skipped/weakened (Test Integrity Policy). The runner's new `audit` phase (last week's self-update) **caught SEC-92 automatically** this run. | **STOP / ACTION NEEDED (sign-off):** (a) Merge **#551 (SEC-92)** — until then the PR Quality Gate + whole deploy chain stay RED for *every* PR. (b) **`E2E Authed Journey` red on dev/staging**: `globalSetup`→`seedJourneyUser`→`admin.auth.admin.listUsers` fails with empty error (`listUsers failed: {}`) against dev Supabase — a service-role **credential/connectivity** issue in the KAN-348 seed harness (manually-dispatched runs), not product code + can't be fixed by editing the test (Test Integrity). Check `E2E_SUPABASE_SERVICE_ROLE_KEY`. (c) Report-only backlog untouched (security/RLS/auth/DR): SEC-88/87/83/43/32/31/30/23, BUGS-57/61/62. | **NO auto-promote — release HOLDS FEATURES.** `origin/main..origin/develop` = **45 commits** incl. multiple `feat()` (KAN-410 example-profiles gallery #549, KAN-408 age-verify provider switch #538, SEC-74 retention purge #519, SEC-75 erasure #500) + migrations. Manual prod sign-off required regardless: `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `promote-staging-to-beta.yml -f confirm=promote` → verify → `promote-to-production.yml -f confirm=PRODUCTION`. Drift 🔴 **RED** (45 ≥ 15). **Merge #551 first** or the deploy audit gate blocks the chain. |
| 2026-07-24 | Weekly Health+Regression (Claude Code, remote) | Health **GREEN**: `checklyra.com/api/health` ok (prod, non-beta); `mcp.checklyra.com/health` ok (server v1.0.0); `beta.checklyra.com` root 200 + `/dashboard`→**307 `/login`** (auth gate holds live). develop HEAD `900be63` = `fix(SEC-92)` #551 (next 16.2.10→16.2.11); whole chain promoted to **prod last night** (main `2c9c3a3`, Deploy-to-Production `success`). lyra runner **PASS=6 / FAIL=0** (lint/type-check/unit/scripts/build/**audit**); `integration`+`e2e` **UNVERIFIED** (BUGS-51 env gaps — no `tests/integration/` dir; pre-baked Chromium rev `1194` ≠ `@playwright/test@1.61.1` required v`1228`, install CDN download-blocked). lyra-mcp-server: `tsc` clean + **638/638** (41 suites) PASS. **No product/test FAIL anywhere.** | **None — no product FAIL, nothing to fix; no test modified/skipped/weakened** (Test Integrity intact). **`audit` phase now PASSES** — the SEC-91/SEC-92 repo-wide npm-audit-high blocker (RED on 07-22) is **cleared by #551** (next patch bump), which shipped all the way to prod. The 3 `describe.skip`/`test.skip` hits are legitimate conditional guards (python-availability ×2; `PROFILE_FIXTURE_SLUG` ×1 — runs on staging), not weakenings; 2 trivial TODO/FIXME in `src/`. | **Report-only (out of autonomous bounds — all security/RLS/auth/ops/migration, already tracked):** 30 open BUGS/SEC incl. High SEC-83/3/43/32/31/30/23; **SEC-88** (develop→main pipeline stalled, 40+ draft PRs); **SEC-87** (Daily-Security Confluence heartbeat stopped 2026-07-06). **Beta-gate-smoke** scheduled run (main, 01:04Z) FAILED once at the `/dashboard` probe — **CF bot-challenge false-positive** (Gotcha #7 / KAN-279 class; runner got 403, live beta verified 307→`/login`). **Promote-to-Staging #143** (Luisa's SEC-92 release) failed once at 00:49Z then retried→succeeded (chain reached prod). Neither is a current-state red. | **NOTHING TO RELEASE — `origin/main..origin/develop` = 0 commits (chain fully flushed to prod 2026-07-24 01:04Z).** No staging rehearsal run (no pending content to promote; Friday, per RELEASE_POLICY "when NOT to release"). When develop next carries changes, the manual chain (Luisa only) is: `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-25 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` ok (prod), `mcp.checklyra.com/health` ok (v1.0.0). develop HEAD `e2b3c98` — Deploy-to-Development + CodeQL both `success`; **one red CI signal noted:** `E2E Authed Journey (dev/staging)` failing repeatedly since 2026-07-22 — root-caused (not by this run) to a Cloudflare bot-challenge on the CI runner IP hitting `/auth/confirm` (403 "Just a moment…"), already diagnosed with a fix-harness PR open (**#556**, founder action pending: CF WAF skip rule + 2 secrets per `docs/E2E_AUTHED_CF_BYPASS.md`) — not duplicated here. lyra runner **PASS=5** (lint/type-check/unit/scripts/build), **FAIL=1 (`audit`)**; `integration`+`e2e` env-gap **UNVERIFIED** (no `tests/integration/` dir; Playwright browsers unavailable — BUGS-51, unchanged). lyra-mcp-server: `tsc` clean + **649/649** (41 suites) PASS on `main`. | **Fixed (partial):** `npm audit --audit-level=high` had gone red again one day after 07-24's clean run — two advisories: postcss path-traversal (GHSA-r28c-9q8g-f849, **fully cleared**, override widen `^8.5.10`→`^8.5.18`) + a **new** brace-expansion DoS (GHSA-mh99-v99m-4gvg, supersedes SEC-89's fix target 5.0.7) — added a `brace-expansion@^5.0.0: 5.0.8` override clearing the new v5 line, but the pre-existing 1.x/2.x overrides remain exposed because `eslint@9.39.4` pins `minimatch@^3.1.5` with no non-major fix (`fixAvailable: eslint@10.8.0, isSemVerMajor: true`) — forcing it risks silently breaking lint. → **SEC-94** + PR **#561** (37→31 advisories, lint/type-check/build/unit all green, 209 suites/2482 tests). **Also fixed:** doc/reality drift — `CLAUDE.md`, `docs/RELEASE_POLICY.md`, and this doc's own Hard-boundary #1 still described the fix-only auto-promote-to-production exception as *active*, with no mention of Luisa's 2026-07-23 withdrawal (this file's own "routine prompt" text further down had already been updated to manual-only, but the surrounding narrative hadn't) → **SEC-95** + draft PR **#562**. **No test modified/skipped/weakened** in either fix. | **SEC-94 residual now blocks the entire PR pipeline** (confirmed by reproducing the identical `npm audit` failure on the docs-only PR #562, which touches no dependency) — same severity class as SEC-90/91/92; full clear needs a deliberate `eslint` 9→10 major-version evaluation (breaking, scoped as follow-up in SEC-94, not attempted here). **Neither #561 nor #562 could be merged this run** — the required "PR Quality Gate" check is red on both for the reason above, so no self-merge per boundary 4 (CI is not green). Prior open items untouched (report-only, already tracked): SEC-80/83/84/85/47/57/65/60/63/61/48/68/69/46/70/23/8/21/3/25/51/36/37/31/30, BUGS-64/66; SEC-88 (prior stalled-PR-backlog finding) confirmed **Done** — develop/main were already back in sync at session start (Luisa ran the full chain manually 2026-07-24 01:04 UTC). | **NOTHING TO RELEASE / NOTHING PROMOTED.** `origin/main..origin/develop` = **0 commits** at session start (Luisa's manual 2026-07-24 promotion already flushed the chain) and stayed at 0 throughout — PR #561/#562 are open but blocked by the repo-wide audit gate, not merged, so develop never moved. No `promote-to-staging.yml` run (nothing new to rehearse). Production promote: **manual-only, confirmed via SEC-95's own doc fix this run** — did not run `promote-to-production.yml` or `promote-staging-to-beta.yml` (2026-07-23 withdrawal honoured). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` then `gh workflow run promote-to-production.yml -f confirm=PRODUCTION` — but only once SEC-94 is resolved enough to get a PR through the gate, or an admin override is used. |
| 2026-07-27 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` `ok` (v1.0.0). develop HEAD `123673a` — latest Deploy-to-Development CI `success`. lyra runner (`RUN_E2E=1`): **PASS=6 / FAIL=0** (lint/type-check/unit **2552/216 suites**/scripts/build/**audit**); `integration`+`e2e` **UNVERIFIED** (known **BUGS-51** env gaps — no `tests/integration/` dir; sandbox Playwright chromium build `1194` ≠ `@playwright/test@1.61.1`'s required `1228`, confirmed via `chromium.executablePath()` → `chromium-1228` missing, 10 e2e skipped at launch, never an assertion). lyra-mcp-server (`main` `31c114b`): `tsc` clean + **734/734** (44 suites) PASS — well above the documented floor (which itself reads stale, see below); both `mcp.checklyra.com` and `mcp-dev.checklyra.com` `build_sha` == `main` HEAD (Railway in lockstep). **No product/test FAIL anywhere.** | **None — no product FAIL, nothing to fix; no test modified/skipped/weakened** (Test Integrity intact). lyra `npm audit --audit-level=high`: clean (SEC-94's fix is holding). lyra-mcp-server `npm audit --audit-level=high`: 20 High, but **all 20 trace to a single advisory** (`brace-expansion` DoS, GHSA-mh99-v99m-4gvg) in the `jest`/`ts-jest` **dev-only** dependency chain — already tracked under **SEC-41** ("jest→js-yaml dev chain deferred"), and that repo's own `test.yml` runs this exact audit as advisory-only/non-fatal (Dependabot is the gating check there per KAN-172) — not a new finding, nothing auto-fixed. TODO/FIXME scan: 2 hits in `src/`, both already ticket-tracked (KAN-184). **Self-update:** filed **KAN-430** + draft PR **#588** — this doc's own "🚦 Hard boundaries" list and embedded routine-prompt template were missing rule (C) "LOOK AND TEXT BELONGS TO LUISA" (the UI/copy-ownership carve-out), which the live scheduled prompt already carries — same drift class SEC-95 fixed once before, this time an omission rather than a stale statement. | **Found in-flight parallel work, not duplicated:** two other sessions/Luisa were actively landing **BUGS-74** (profile editor silently NULLing `good_to_know`/`boundaries`) — PR #584 already shipped all the way to production (a full develop→staging→beta→main chain completed a few hours before this run started, main now `3a01e57`) and its follow-up PR #586 (legacy-editor coverage gap) had already merged straight to develop by the time this run started; draft PR #585 was an earlier, now-superseded attempt. Nothing here needed action from this run. Open BUGS/SEC backlog (30 tickets, e.g. High SEC-23/3/32/31/30) is security/RLS/backup/DR/architectural → **report-only, out of autonomous bounds**, all already tracked. No Highest-priority **bug** ticket open (KAN-90 is Highest but is a Google/Apple OAuth-dashboard audit task, not a defect) — no release-block condition from that source. `integration`+`e2e` env gap persists (BUGS-51 — needs a runner image with Playwright chromium rev ≥1228 baked in, or CDN allowlist). | **YES — fix-only, rehearsed to staging.** `origin/main..origin/develop` = **1 commit**: `fix(BUGS-74)` #586 (2 files, +10/-2, legacy profile-editor loader drift fix) — small, CI-green, no migration/feature/route/table/MCP tool. Ran `promote-to-staging.yml -f confirm=promote` (run [30232847562](https://github.com/luisa-sys/lyra/actions/runs/30232847562)) → **success**; `origin/staging` now == `origin/develop` (0 commit gap), `stage.checklyra.com` reachable (302 to Vercel SSO login, expected — the promote workflow's own internal smoke check passed). **Did NOT run** `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A) — those remain Luisa's manual call: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-28 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` ok (prod, non-beta), `mcp.checklyra.com/health` ok (v1.0.0). develop HEAD `1d6cb5f` — CodeQL + Deploy-to-Development both `success`. lyra runner (`RUN_E2E=1`) **PASS=6** (lint/type-check/unit/scripts/build/**audit** — no high/critical advisories); `integration`+`e2e` env-gap **UNVERIFIED** (BUGS-51, unchanged — no `tests/integration/` dir; sandbox Chromium rev `1194` vs pinned `@playwright/test@1.62.0`'s required rev `1234`). `npm run test:unit` **2575/2575** (219 suites). lyra-mcp-server (`main`): `tsc` clean + **734/734** (44 suites) PASS — well above both repos' documented floors (2118/2575 checked; `npm audit` 20 HIGH found but is advisory-only in that repo's CI, Dependabot is the gating check per its `test.yml`). | **Fixed + merged to develop:** **BUGS-79** — `.github/dependabot.yml`'s `github-actions` ecosystem had no `target-branch`, defaulting to `main` and putting every weekly Actions bump in permanent conflict with `main-chain-guard.yml` (SEC-98) — live evidence PR #600, red on Main Chain Guard only. Set `target-branch: develop`, added `tests/unit/dependabot-target-branch.test.js` (registered **CTL-032**), reconciled CLAUDE.md gotcha #1 with SEC-98 → **PR #616, merged** (CI green: PR Checks/CodeQL/E2E all success). Closed stuck PR #600 with a pointer to the fix. **Fixed (stale ticket, no code change needed):** **BUGS-76** — verified the bash-3.2-portability fix (mapfile removal, CTL-031) was already shipped in PR #594; ticket just hadn't been transitioned → closed **Done**. **Self-update (not self-merged):** **SEC-95 follow-up** — two spots in this doc (the Layer table's Release row + embedded prompt step 5) still described an active fix-only auto-promote path that SEC-95's original fix missed, directly contradicting boundary #1 → **PR #617, draft**, awaiting review. **No test modified/skipped/weakened** in any fix. | **Investigated, not fixed — flagged for re-triage:** **BUGS-78** (MCP venue scorer drift) — ticket's diagnosis (8-of-10 factor keys, weight 0.90) doesn't match current `lyra-mcp-server` `main`: the file already has all 10 keys (weight 1.00), and the cited repro script `scripts/check-convene-scoring-drift.cjs` / `npm run check:convene-drift` doesn't exist anywhere in that repo's history. Commented findings on the ticket; did not push a speculative fix to a repo with no staging step (merge to `main` = immediate prod MCP deploy). **Report-only backlog (architectural/security/RLS/migration/in-progress, out of autonomous bounds), unchanged this run:** SEC-3/23/30/31/32/100/101/103/104/105/106/108/25/93/97, SEC-8/21/36/37/39/41/46/48/51/57/60/63/68/69/70/87/96/99/102/107, BUGS-19/55/72/73/77. | **Release-ready (fix/chore/docs only, 6 commits) — staging rehearsed, prod is Luisa's call.** `origin/main..origin/develop` = **6 commits**, all classified fix/chore/docs/deps (0 `feat:`, none touch rule-C look/text paths). Drift: 🟡 **Yellow** (6 commits, <1 day since last develop commit — plan a promotion this week). Rehearsed `develop → staging`: `promote-to-staging.yml` run **#153 (30324320791) → success**; staging HEAD now `fe8d7d7`, 0 commits behind develop. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` (2026-07-23 withdrawal honoured; no exception reinstated). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-29 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` `ok` (v1.0.0). develop HEAD `87f92a7` — Deploy-to-Development + CodeQL both `success`. lyra runner (`RUN_E2E=1`): **PASS=6 / FAIL=0** (lint/type-check/unit/scripts/build/**audit**); `integration`+`e2e` **UNVERIFIED** by the script's own classifier (known **BUGS-51** — no `tests/integration/` dir; `@playwright/test@1.62.0` needs `chromium_headless_shell` rev `1234`, sandbox only ships rev `1194`). **This run additionally hand-verified E2E is real-green**, not just env-UNVERIFIED: symlinked the installed rev-1194 binary into the expected rev-1234 path and re-ran `--project=chromium` with the CI-matching dummy Supabase env (`NEXT_PUBLIC_SUPABASE_URL` etc., copied from `e2e-tests.yml`) — **17/17 runnable tests passed**, 5 skipped (the `PROFILE_FIXTURE_SLUG`-gated populated-profile suite, correctly conditional). The 3 apparent failures on the first attempt (two 404-page checks 500ing, waitlist heading missing) were traced to the missing dummy env vars, not a code defect — confirmed by rerunning with them set. lyra-mcp-server (`main` `31c114b`): `tsc` clean + **734/734** (44 suites) PASS, matching the documented floor exactly. | **None — no product FAIL, nothing to fix; no test modified/skipped/weakened** (Test Integrity intact). `npm audit --audit-level=high`: lyra clean (2 moderate only); lyra-mcp-server 20 High, all tracing to the same single `brace-expansion` DoS advisory (GHSA-mh99-v99m-4gvg) in the `jest`/`ts-jest` dev-only chain already tracked under SEC-41 and treated as advisory-only in that repo's own `test.yml` (Dependabot is the gating check per KAN-172) — not a new finding. TODO/FIXME scan: 2 hits in `src/`, both already ticket-tracked (KAN-184). | **Report-only (out of autonomous bounds — security/RLS/auth/migration/architectural, all already tracked):** 30 open BUGS/SEC tickets surveyed, including High SEC-100/101/103/104/105/106/108/112/23/3/30/31/32 and BUGS-75/77/80/81. No Highest-priority **bug** ticket open (KAN-90 is Highest but is a Google/Apple OAuth-dashboard audit checklist task, not a defect) — no release-block condition from that source. `integration`+`e2e` env-gap classification persists in the deterministic runner (BUGS-51 — still needs a runner image with Playwright chromium rev ≥1234 baked in, or the symlink workaround folded into CI); this run's hand-verification is a one-off confirmation, not a permanent fix. | **YES — release rehearsed to staging, holds a mix of fix/chore/docs + one non-UI CI/ops `feat:` commit, prod is Luisa's call.** ⚠️ **Correction (same run, caught before end-of-session):** this row originally reported **51 commits** pending, computed against a **stale local `origin/main` ref this session had never explicitly re-fetched** — an artefact of the sandbox's initial clone snapshot, not a real drift spike. An explicit `git fetch origin main` (prompted by unexpectedly seeing tags v0.1.88–v0.1.92 appear on a later `--prune` fetch) confirmed `origin/main` was already correctly at `5719804` ("Promote beta → production", 2026-07-27) and recomputed the true count as **25 commits** (including this run's own run-log PR #627). Breakdown: 11 `docs`, 4 `fix`, 2 `deps`, 1 `chore`, 1 `feat` — `feat(KAN-428)` Extraction Definition-of-Done gate, a CI/ops guard with no UI/route/table surface (verified: touches no rule-C look/text path) — and 6 untyped research-artefact/spike commits (KAN-416/417/420/421/422/432, all docs-only per their own descriptions). None touch rule-C look/text paths. Drift is still 🔴 **RED** (25 ≥ 15 threshold — promote today), just not the number originally written here. **Lesson for future runs:** run `git fetch origin main` explicitly (not just `develop`/`staging`/`beta`) before computing the drift count — don't rely on whatever `origin/main` the sandbox happened to start with. Ran `promote-to-staging.yml` run **#154 ([30417647161](https://github.com/luisa-sys/lyra/actions/runs/30417647161)) → success**; staging HEAD now `b2e5f46`, tree-identical to develop, develop confirmed an ancestor. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-07-31 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` `ok` (v1.0.0). develop HEAD `2b03018` — Deploy-to-Development + CodeQL both `success`. lyra runner (`RUN_E2E=1`): **PASS=6 / FAIL=0 / UNVERIFIED=2** (lint/type-check/unit/scripts/build/**audit** all PASS); `integration`+`e2e` **UNVERIFIED** — same known **BUGS-51** env gaps (no `tests/integration/` dir; hand-confirmed via a direct `playwright test` run that `@playwright/test@1.62.0` needs `chromium_headless_shell` rev `1234`, sandbox only ships rev `1194` — `Executable doesn't exist at .../chromium_headless_shell-1234/...`). lyra-mcp-server (`main` `31c114b`): `tsc` clean + **734/734** (44 suites) PASS, matching the documented floor exactly; deployed `build_sha` confirmed == `main` HEAD. **No product/test FAIL anywhere.** | **None — no product FAIL, nothing to fix; no test modified/skipped/weakened** (Test Integrity intact). `npm audit --audit-level=high`: lyra clean; lyra-mcp-server **escalated to 20 HIGH + 2 moderate** (was 18 moderate on 2026-06-26) — the vulnerable package changed from `js-yaml` to `brace-expansion` (GHSA-mh99-v99m-4gvg), same dev-only `jest`/`ts-jest` chain, same advisory-only gate in that repo's `test.yml`. Confirmed `npm audit --omit=dev` clean except 2 pre-existing moderate (`@hono/node-server`, irrelevant on Linux). Logged the severity escalation as a comment on **SEC-41** (not a new ticket — same root cause, already deferred pending a supervised jest major-version upgrade); not attempted here per that ticket's own "not a low-risk autonomous fix" acceptance criterion. TODO/FIXME scan: 2 hits in `src/`, both already ticket-tracked (KAN-184). | **Report-only (out of autonomous bounds — security/RLS/auth/migration/architectural, all already tracked):** 50 open BUGS/SEC tickets surveyed (exact count, not capped), none Highest priority. Open PR backlog on both repos is large (several stale drafts + routine dependabot bumps) but pre-existing and outside this run's fix-only scope — not duplicated. `integration`+`e2e` env-gap classification persists in the deterministic runner (BUGS-51 — still needs a runner image with Playwright chromium rev ≥1234 baked in). | **Already flushed — no promotion run this session.** A full `develop → staging → beta → production` chain completed 2026-07-30 (`f463cf7`, workflow_dispatch) before this run started. `origin/main..origin/develop` = **4 commits**: `fix(KAN-279)`, `feat(KAN-414 F4)` (test-path-manifest tooling, no UI/route/table surface), `docs(CLAUDE.md)`, `refactor(KAN-414 F4 group 1)` — all authored directly by Luisa, none touch rule-C look/text paths. Drift 🟢 **Green** (4 < 5 commits, 0 days since last develop commit) — no promotion required by policy. `origin/staging` == `origin/main` (0 ahead); did **not** re-trigger `promote-to-staging.yml` this run since the chain was exercised in full less than 24h prior and drift is green — Sunday's auto-promote-to-staging cron (or Luisa manually) will pick up these 4 commits regardless. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml`. Luisa's commands when ready: `gh workflow run promote-to-staging.yml -f confirm=promote` → `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-08-01 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` `ok` (v1.0.0). develop HEAD `4649ed7` — Deploy-to-Development + CodeQL both `success`. lyra runner (`RUN_E2E=1`): **PASS=6 / FAIL=0** (lint/type-check/unit/scripts/build/**audit**); `integration` **UNVERIFIED** (known **BUGS-51** — no `tests/integration/` dir); `e2e` **UNVERIFIED** by the script's own classifier (same **BUGS-51** browser-rev gap — `@playwright/test@1.62.0` needs `chromium`/`chromium_headless_shell` rev `1234`, sandbox ships rev `1194`), **but hand-verified real-green using the 2026-07-29 symlink technique** (installed rev-1194 binaries linked into the expected rev-1234 paths, including the renamed `chrome-headless-shell` binary + its sibling resource files this run additionally had to reconstruct for the headless-shell project specifically): **17/17 runnable tests passed**, 5 correctly skipped (`PROFILE_FIXTURE_SLUG`-gated populated-profile suite). The documented workaround remains valid 3 days later — no self-update needed beyond this confirmation. lyra-mcp-server (`main` `31c114b`, unchanged since 07-27): `tsc` clean + **734/734** (44 suites) PASS, matching the documented floor exactly. MCP prod + dev `build_sha` both `31c114b` — lockstep with `main` confirmed. | **None — no product FAIL in either repo; no test modified/skipped/weakened** (Test Integrity intact). `npm audit --audit-level=high`: lyra clean (2 moderate only, unchanged); lyra-mcp-server **1 high + 2 moderate** — same `brace-expansion` GHSA-mh99-v99m-4gvg advisory tracked under **SEC-41**, footprint much smaller than the 20-high count recorded 2026-07-31 (routine Dependabot bumps since then reduced duplicate install paths). Logged an update comment on SEC-41 with today's numbers; **no fix attempted** — that ticket's own 2026-06-26/07-27 conclusions are that the `brace-expansion@5.0.8` pin needs supervised, repo-specific verification (not a blind copy of `lyra`'s SEC-94 fix), which is outside this routine's autonomous-fix bounds. TODO/FIXME scan: 2 hits in `src/`, both already ticket-tracked (KAN-184), unchanged. | **Report-only (out of autonomous bounds — security/RLS/auth/migration/architectural, all already tracked):** ~50 open BUGS/SEC tickets surveyed (grown from 30 on 07-29), incl. High SEC-100/101/103/104/105/106/108/112/23/3/30/31/32 and BUGS-75/77/80/81 — same shape as prior runs, nothing newly actionable. No Highest-priority **bug** ticket open. Surveyed open PRs: **17** in `lyra` (mostly draft doc run-log rows from parallel routine sessions — daily-security, KAN-413 staging-soak — plus 6 Dependabot bumps, none stale beyond a few days) and **14** in `lyra-mcp-server` (Dependabot bumps + a couple of draft feature/fix branches, e.g. #133/#134 for BUGS-78/KAN-426) — none required action from this run; not duplicated. `integration`+`e2e` env-gap classification persists in the deterministic runner (BUGS-51, unchanged) — this run's hand-verification is again a one-off confirmation, not a permanent fix folded into CI. | **NOTHING TO RELEASE.** `origin/main..origin/develop` = **0 commits** — `main` was promoted from `beta` a few hours before this run (`bb277e3` "Promote beta → production", develop HEAD `4649ed7` confirmed an ancestor of `main`). Drift 🟢 **Green**. No staging rehearsal run (nothing pending to promote). Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` (2026-07-23 withdrawal honoured; no exception reinstated). Luisa's commands whenever develop next carries pending changes: `gh workflow run promote-to-staging.yml -f confirm=promote` → verify → `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-08-02 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, siteUrl:"https://checklyra.com", isBetaDeploy:false, vercelEnv:"production"}`; `mcp.checklyra.com/health` → `{status:"ok", server:"lyra-mcp-server", version:"1.0.0"}`. develop HEAD `81542e5` — Deploy-to-Development run [30698500773](https://github.com/luisa-sys/lyra/actions/runs/30698500773) `success` (2026-08-01T11:50:57Z). lyra runner (`RUN_E2E=1`): **PASS=6 / FAIL=0** (lint/type-check/unit/scripts/build/**audit**); `integration`+`e2e` **UNVERIFIED** by the script's own classifier — known **BUGS-51** env gaps, not product FAILs (no `tests/integration/` dir; sandbox Chromium build `1194` vs `@playwright/test@1.62.0`'s required rev `1234`, confirmed via `browsers.json`; this is the same recurring rev-mismatch as 2026-06-24 (1194 vs 1223) and 2026-07-28 (1194 vs 1234) — the sandbox's baked-in Chromium build has not moved while the pin keeps advancing). `npm run test:unit`: **2714/2714** passing (**235 suites**) — floor was 2705/234, drift updated in `CLAUDE.md` (+9 tests / +1 suite from PR #660's docs-only commit landing since the last measurement). lyra-mcp-server (`main` `31c114b`, unchanged since 2026-08-01): `tsc` clean + **734/734** (44 suites) PASS, matching the documented floor exactly. | **None — no product FAIL, nothing to fix; no test modified/skipped/weakened** (Test Integrity intact). Updated `CLAUDE.md` test-floor line (2705/234 → 2714/235) in this same PR. `npm audit --audit-level=high`: lyra clean (audit phase PASS); lyra-mcp-server 1 HIGH (`brace-expansion` DoS, GHSA-mh99-v99m-4gvg, via `test-exclude`'s dev-only coverage-tooling chain — same advisory family as prior runs, fewer instances than the 20 seen 2026-07-27/29, advisory-only per that repo's own `test.yml` — Dependabot is the gating check there per KAN-172, no open Dependabot PR for it yet, not auto-fixed). TODO/FIXME scan: 2 hits in `src/`, both already ticket-tracked (KAN-184). **Confirmed already-fixed:** **BUGS-85** + **SEC-113** (the "test reimplements its subject" family, found 2026-08-01) both landed their real behavioural test suites via PR #658 (on develop, included in this run's green unit count) — only remaining step on each is Luisa's sign-off to delete the now-superseded vacuous blocks in `maintenance-page.test.js` / `rate-limit.test.js`, correctly left untouched (Test Integrity Policy — deletion needs sign-off). **Self-update (fixed directly, not repo-gated):** the Confluence **"Lyra — Ops Routines Control Room"** page (34275370 — the live source of truth per KAN-363) House rule #1 still described the 2026-06-21 fix-only auto-promote-to-production exception as active, nearly two weeks after Luisa withdrew it (2026-07-23) — the same drift class SEC-95 fixed in the repo docs on 2026-07-25/28, this time on the wiki. Also noticed while there: the repo mirror `docs/OPS_ROUTINES_CONTROL_ROOM.md` points the heartbeat at Confluence 33554434 (the *Backlog Autopilot's* own page) instead of 34275370 — already tracked as open **BUGS-81**, not re-filed. Corrected House rule #1 on the live wiki page and appended this run's heartbeat row there. Filed **SEC-114**. | **Report-only (out of autonomous bounds — security/RLS/auth/migration/architectural, all already tracked):** 50 open BUGS/SEC tickets surveyed. No Highest-priority **bug** ticket open (KAN-90 is a Google/Apple OAuth-dashboard audit checklist task, KAN-442 is Luisa's design-review work — neither is a release-blocking defect). `integration`+`e2e` env-gap classification persists (BUGS-51 — still needs a runner image with Playwright Chromium rev ≥1234 baked in, or a CDN allowlist so `npx playwright install` can fetch it fresh each run). | **YES — fix-only-shaped, rehearsed to staging.** `origin/main..origin/develop` (after an explicit `git fetch origin main`, per the 2026-07-29 lesson) = **3 commits**: `81542e5` docs(KAN-414 F4 handover), `10a75e5` refactor(KAN-414 F4 group 3 — behavioural test conversions), `7188877` fix(SEC-106). None touch rule-C look/text paths. Drift: 🟢 **Green** (3 commits, <1 day since last develop commit). Ran `promote-to-staging.yml` run **#159 ([30729281062](https://github.com/luisa-sys/lyra/actions/runs/30729281062)) → success**; staging HEAD now `d635ff7`, tree-identical to develop (`81542e5` confirmed an ancestor). Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A) — 2026-07-23 withdrawal honoured, no exception reinstated. Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-08-03 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` `ok` (v1.0.0). develop HEAD `5d768d7` — Deploy-to-Development + CodeQL both `success`. lyra runner (`RUN_E2E=1`): **PASS=6 / FAIL=0** (lint/type-check/unit/scripts/build/**audit**); `integration`+`e2e` **UNVERIFIED** by the script's own classifier (known **BUGS-51** — no `tests/integration/` dir; `@playwright/test@1.62.0` needs `chromium_headless_shell` rev `1234`, sandbox only ships rev `1194` — reconfirmed directly via `npm run test:e2e`, same "Executable doesn't exist" signature as prior runs, an env gap not a code defect). lyra-mcp-server (`main` `31c114b`): `tsc` clean + **734/734** (44 suites) PASS, matching the documented floor exactly. | **None — no product FAIL, nothing to fix; no test modified/skipped/weakened** (Test Integrity intact). `npm audit --audit-level=high`: lyra clean (2 moderate only, `--omit=dev` confirms 0 prod-tree). lyra-mcp-server: **1 HIGH** (`brace-expansion` DoS, GHSA-mh99-v99m-4gvg, via `test-exclude`/jest-coverage — confirmed **dev-only**, `--omit=dev` shows zero HIGH) + 2 moderate (`@hono/node-server` path-traversal, pre-existing). This is the **same advisory family** as SEC-89/90/94 (already fixed in the `lyra` repo) but a **different lockfile/tree** not covered by SEC-41 (qs/js-yaml, different CVEs) — filed as new **SEC-116** rather than piling onto SEC-41's unrelated finding; not auto-fixed per the routine's STOP-on-security rule. TODO/FIXME scan: 2 hits in `src/`, both already ticket-tracked (KAN-184). | **Report-only (out of autonomous bounds — security/RLS/auth/migration/architectural, all already tracked):** 14 open BUGS + 30 open SEC tickets surveyed (High: BUGS-75/77/85/86; SEC-100/101/103/104/105/106/108/112/23/3/30/31/32). No **Highest**-priority BUGS/SEC ticket open — no release-block condition from that source. **Flagging a doc-hygiene gap, not fixing it myself:** this table is missing three prior runs' rows — 2026-07-31/08-01/08-02 weekly-health-regression run-logs exist only as **unmerged draft PRs** (#651, #655, #663), left as drafts per this routine's own "propose, don't self-merge" convention for doc changes. The raw run history isn't lost (it's in the PR bodies), but this committed table under-represents 3 days of runs until Luisa reviews/merges them — flagging for her batch review rather than merging on her behalf. | **YES — release rehearsed to staging, fix/docs-only commit set, prod is Luisa's call.** `origin/main..origin/develop` = **4 commits**, all `test:`/`docs:` type (KAN-414 F4 test-coverage work + modularisation classification docs) — 0 `feat:`, none touch rule-C look/text paths. Drift: 🟢 **Green** (4 commits, <24h since last develop commit). Ran `promote-to-staging.yml` run **#162 ([30788777868](https://github.com/luisa-sys/lyra/actions/runs/30788777868)) → success**; staging HEAD now `adc20b1`, tree-identical to develop, develop confirmed an ancestor via `git merge-base --is-ancestor`; `deploy-staging.yml` run **#108 → success**. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-08-04 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` `ok` (v1.0.0). develop HEAD (start of run) `10dd576` — Deploy-to-Development + CodeQL both `success`. lyra runner (non-E2E phases): **PASS=5/FAIL=1** (lint/type-check/unit 2996 tests/scripts/build PASS; **audit FAILED** — 8 new HIGH advisories: brace-expansion GHSA-rgw5-rvv9-x895 bypassing the prior SEC-94 5.0.8 mitigation, fast-uri GHSA-7p8r-x3mc-p8w7 pinned at exactly the vulnerable 3.1.4, ip-address GHSA-mwp4-54f8-5fhr dev-only). Fixed same run (see next column) → re-verified **PASS=6/FAIL=0**. `integration`+`e2e` **UNVERIFIED** by the script's own classifier (BUGS-51 — no `tests/integration/` dir; `@playwright/test@1.62.0` needs `chromium_headless_shell` rev 1234, sandbox ships rev 1194). lyra-mcp-server (`main` `31c114b`): `tsc` clean + **734/734** (44 suites) PASS, matching the documented floor exactly; its own `npm audit` also flagged the same brace-expansion/fast-uri/ip-address family but that gate is advisory-only in that repo (Dependabot is the source of record, KAN-172) — no action needed there. | **SEC-123** (fixed): brace-expansion 5.0.8→5.0.9, fast-uri 3.1.4→3.1.5, ip-address added `^10.4.0` override — empirically verified (lint byte-identical, type-check/build/2996 unit tests green, audit 8-high→0). PR #686, merged to develop (PR Checks + CodeQL green). **BUGS-81** (partial fix): `docs/OPS_ROUTINES_CONTROL_ROOM.md` never stated its own Confluence page ID (34275370) while repeatedly citing the unrelated Backlog Autopilot's 33554434 in the same intro section — an agent following the doc literally could misread the Autopilot's "Run ledger" as the ops heartbeat ledger (false-PASS risk). Fixed the doc + the same ambiguity in CLAUDE.md gotcha #25; swept both repos for other conflations (none found). PR #687, merged. CI guard + mutation-proof and the founder-owned routine-prompt update from BUGS-81's full acceptance criteria are NOT done (out of this session's reach) — ticket left open with a comment. TODO/FIXME scan: 2 hits in `src/`, both already ticket-tracked (KAN-184). | **Report-only (out of autonomous bounds):** surveyed open BUGS/SEC. Migration/RLS/data-model: BUGS-77 (rls_auto_enable REVOKEd but never CREATEd in migration lineage), BUGS-84 (staging DB migration-history parity gap). Architectural: BUGS-80 (KAN-424 F2 marked Done but import cycle still present). Would require a test change (Test Integrity Policy — STOP per boundary 2): BUGS-86 (google-oauth.test.ts assertion uses the constant under test as its own oracle), BUGS-85 (maintenance-worker test reimplementation, CTL-038's own subject — fix pattern is documented but is a test-harness rewrite). Infra/founder-only: BUGS-55 (Cloudflare↔Railway origin TLS on dev admin MCP, needs CF dashboard access), BUGS-19 (Workers Builds >20-commit trigger, tracked resolved-enough per its own gotcha #20 history). Already owned/in-progress elsewhere, not duplicated: BUGS-78, BUGS-73, BUGS-72, BUGS-75. No Highest-priority open **bug** ticket (KAN-90 is Highest but is an OAuth-dashboard audit checklist, not a defect) — no release-block condition from that source. | **YES — release rehearsed to staging.** Pending on develop ahead of main at run start: 12 commits, revised to **13 commits** once BUGS-81's merge (#687) landed a few minutes into the run — 1 `fix` (this run's own SEC-123, #686), 4 `docs` (including BUGS-81's #687), 6 `feat` (4× `feat(profile)`, 1× `feat(search)`, all touching `src/app/**` profile/search UI + copy — **rule-C look/text surface**, unreviewed by this routine, Luisa's to review), 2 `test`. Mixed fix+feature set — would never have qualified for the (withdrawn, inactive) fix-only auto-promote exception even if it were live; moot since production promote is manual-only regardless. Ran `promote-to-staging.yml` (run #163, [30873334422](https://github.com/luisa-sys/lyra/actions/runs/30873334422)) — dispatched at 02:57:51Z against develop HEAD `3dcacf7` (post SEC-123 merge; BUGS-81's PR #687 merged at 02:58:55Z, a few minutes into the run, so it did **not** ship on this staging promotion and will go out on the next one) → **success**. Underlying `deploy-staging.yml` run [30873512525](https://github.com/luisa-sys/lyra/actions/runs/30873512525): all 5 jobs green (Lint & Unit Tests, Build Verification, Deploy to stage.checklyra.com, Post-Deploy Health Checks including SHA-match/env-wiring/MCP-health/smoke-test, and Playwright E2E Tests on Staging). Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-08-05 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` `ok` (v1.0.0). develop HEAD (start of run) `d953aa5` — Deploy-to-Development `success`. lyra runner (`RUN_E2E=1`): **PASS=6/FAIL=0** (lint/type-check/unit/scripts/build/**audit**, audit clean — 0 high/critical, SEC-123's fix holding); `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51); `e2e` **UNVERIFIED** by the script's own classifier (`@playwright/test@1.62.0` needs `chromium_headless_shell` rev 1234, sandbox ships rev 1194). **This run additionally hand-verified E2E for real**, not just env-UNVERIFIED: launched the `chromium` project directly via a local (uncommitted, deleted after) `launchOptions.executablePath` pointing at the installed rev-1194 full-Chromium binary — 17 runnable tests, 14 passed cleanly, 3 apparent failures (two 404-page checks 500ing, waitlist heading not found) traced to the local dev server having no `.env.local` (no Supabase credentials in this sandbox), **not a code defect** — confirmed by cross-checking the same three scenarios against the real deployed `dev.checklyra.com`: unknown-slug returns `404` (matches prod control), and the waitlist page's rendered `<h1>` reads "You're on the list" exactly as the test expects. `mobile-safari` (webkit) untested — no webkit binary in this sandbox (unchanged gap). lyra-mcp-server (`main` `31c114b`): `tsc` clean + **734/734** (44 suites) PASS, matching the documented floor exactly. | **None — no product FAIL, nothing to fix; no test modified/skipped/weakened** (Test Integrity intact). `npm audit --audit-level=high`: lyra clean (2 moderate only). lyra-mcp-server: **6 vulnerabilities (3 moderate, 3 high)** — same advisory family as SEC-123 (`brace-expansion` GHSA-mh99-v99m-4gvg/GHSA-rgw5-rvv9-x895, `fast-uri` GHSA-7p8r-x3mc-p8w7, `ip-address` GHSA-mwp4-54f8-5fhr) but in *that* repo's own tree — **not auto-fixed this run**: `ip-address` reaches production code there (via `express-rate-limit`, a runtime dependency, unlike lyra's dev-only chain), so the fix is security-classified per the STOP boundary, and that repo's own `test.yml` already treats `npm audit` as advisory-only (Dependabot is the gating check per KAN-172) — reported as a finding for Luisa/Dependabot, not pushed. TODO/FIXME scan: 2 hits in `src/`, both already ticket-tracked (KAN-184). Skip/only test-marker sweep: 3 pre-existing hits, all justified (`describeIfPython` env-gate ×2, one documented `test.skip` in `profile-redesign.spec.ts`) — nothing new. Workflow silent-skip / `continue-on-error` greps: no live violations (only historical comments referencing already-removed patterns, plus the two already-justified KAN-167 exceptions). | **Report-only (out of autonomous bounds — security/RLS/architectural/migration, all already tracked):** 66 open BUGS/SEC tickets surveyed (up from 30 on 2026-07-29 — backlog growth worth Luisa's attention), including High SEC-100/101/103/104/105/106/108/112/113/117/23/3/30/31/32 and BUGS-75/77/85/86; Medium SEC-8/21/36/37/39/41/46/48/51/57/60/63/68/69/70/87/95/96/99/102/107/109/110 and BUGS-19/55/72/73/78/80/81. No Highest-priority open **bug** ticket (KAN-90 = OAuth-dashboard audit checklist, KAN-442 = design-rebuild Story — neither is a defect) — no release-block condition from that source. **lyra-mcp-server audit findings** (see Fixes column) reported, not fixed. **Noticed, not acted on:** Dependabot PR **#689** in lyra (`chore(deps-dev): bump ip-address 10.2.0→10.4.0`) opened with `base=main` (Dependabot's version-update config correctly targets `develop` — this reads like a security-alert-triggered PR, which GitHub always bases on the repo's default branch regardless of `dependabot.yml`) — `mergeable_state: blocked` (structurally can't land past `main-chain-guard.yml`/SEC-98) and now superseded by SEC-123's override already on develop; harmless, but Luisa may want to close it manually. `integration`+`e2e` env-gap classification persists in the deterministic runner (BUGS-51 — unchanged, needs a runner image with Playwright chromium rev ≥1234 or webkit baked in). | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **14 commits**: 5 `docs`, 1 `fix` (SEC-123, already shipped last run), 6 `feat` (4× `feat(profile)`, 1× `feat(search)`, 1× `feat(profile)` school/org picker — at least 5 of 6 confirmed touching `src/app/**/*.tsx` **rule-C look/text surface**, unreviewed by this routine, Luisa's to review), 2 `test`. Mixed fix+feature set — would never have qualified for the withdrawn fix-only auto-promote exception even if it were live; moot since production promote is manual-only regardless. Ran `promote-to-staging.yml` (run [30970251466](https://github.com/luisa-sys/lyra/actions/runs/30970251466)) against develop HEAD `d953aa5` → **success**; underlying `deploy-staging.yml` run [30970282402](https://github.com/luisa-sys/lyra/actions/runs/30970282402) green. `origin/staging` now `7ecfe26`, 0 commits behind develop. `stage.checklyra.com` → 302 (Vercel SSO login, expected); `mcp.checklyra.com/health` ok. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-08-06 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` `ok` (v1.0.0). develop HEAD (start of run) `9b3b966` — `deploy-dev.yml` `success`, CodeQL `success`. Corrected a stale branch-topology issue before starting: the pre-provisioned `claude/hopeful-bohr-vfc23q` was rooted on `origin/main` (identical to main's tip, 343 "commits ahead of develop" purely from promote-merge history, zero unique commits) — exactly the BUGS-19 trap. Re-rooted onto `origin/develop` before any work, per gotcha #1 ("always branch from origin/develop"); force-pushed the corrected branch (remote ref had gone stale/deleted, pushed fresh). lyra runner (`RUN_E2E=1`): **PASS=6/FAIL=0** (lint/type-check/unit/scripts/build/**audit**, audit clean — 0 high/critical); `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51); `e2e` **UNVERIFIED** by the script's own classifier (`@playwright/test` needs a newer `chromium_headless_shell` rev than the sandbox ships, unchanged known gap). lyra-mcp-server (`main` `31c114b`): `tsc` clean + **734/734** (44 suites) PASS, matching the documented floor exactly. | **None — no product FAIL, nothing to fix; no test modified/skipped/weakened** (Test Integrity intact). `npm audit --audit-level=high`: lyra clean (develop tree). lyra-mcp-server: **6 vulnerabilities (3 moderate, 3 high)** — brace-expansion GHSA-mh99-v99m-4gvg, fast-uri GHSA-7p8r-x3mc-p8w7, ip-address GHSA-mwp4-54f8-5fhr — **3rd consecutive day this exact finding has been observed** (2026-08-04, 2026-08-05, today), still **not auto-fixed**: brace-expansion is explicitly tracked at **SEC-116** ("To Do", annotated *"supervised fix rather than autonomous — the routine's rule is to STOP and report security findings, not auto-fix them"*) and `ip-address` reaches production code there via `express-rate-limit` (a runtime dependency), so all three stay security-classified per the STOP boundary. Note SEC-116's scope as filed only names brace-expansion — fast-uri/ip-address aren't formally covered by any single ticket yet; flagging for Luisa to decide whether to broaden SEC-116 or rely on Dependabot (KAN-172 is the gating mechanism for that repo's advisory-only `npm audit` in `test.yml`). TODO/FIXME scan: 2 hits in `src/`, already ticket-tracked (KAN-184). Skip/only test-marker sweep: 3 pre-existing hits, all justified. Workflow silent-skip / `continue-on-error` greps: no live violations (all instances carry justifying comments, incl. `promote-to-staging.yml:186`'s intentional advisory pattern). | **Report-only (out of autonomous bounds, all already tracked — cited per one-concern-one-owner, not re-derived):** only 1 new ticket since yesterday's exhaustive 66-ticket survey — **BUGS-87** (staging DB 4 migrations behind the `staging` branch, `gift_suggestion_dismissals` table missing, will hard-fail on first use) — migration/data-model class, correctly out of autonomous bounds, filed by the KAN-413 staging-soak routine (not this one). Daily Security routine's 2026-08-06 run (draft PR #699, unmerged): watchdog **FAIL×3** — `daily-security` heartbeat OVERDUE (SEC-87, regressed), `doc-sync` heartbeat OVERDUE (SEC-108, regressed), `staging-soak` last-FAIL (BUGS-87) — all three already ticketed by their owning routines, cited here not re-derived per KAN-361 ownership rule. Also noted, not acted on: 8 open unmerged draft run-log PRs across routines (#626–#696) — a minor process-hygiene item outside this routine's remit. | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **15 commits**: 6 `docs`, 1 `fix` (SEC-123, already shipped/promoted previously), 6 `feat` (4× `feat(profile)`, 1× `feat(search)`, 1× `feat(profile)` — all 6 confirmed via `git show --stat` touching `src/app/**/*.tsx` **rule-C look/text surface**, unreviewed by this routine, Luisa's to review), 2 `test`. Mixed fix+feature set — would never have qualified for the withdrawn fix-only auto-promote exception even if it were live; moot since production promote is manual-only regardless. Ran `promote-to-staging.yml` (run [31066422911](https://github.com/luisa-sys/lyra/actions/runs/31066422911)) against develop HEAD `9b3b966` → **success**; underlying `deploy-staging.yml` run [31066443231](https://github.com/luisa-sys/lyra/actions/runs/31066443231) green. `origin/staging` now `a9dbc24`, 0 commits behind develop. `stage.checklyra.com` → 302 (Vercel SSO login, expected); `mcp.checklyra.com/health` ok. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Note: `main`'s own Dependabot/security-alert scan currently shows 6 vulnerabilities (3 high, 3 moderate) — the same brace-expansion/fast-uri/ip-address family SEC-123 already fixed on `develop`, just not yet promoted to `main`; clearing the release backlog would also clear that alert. Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

| 2026-08-07 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` `ok` (v1.0.0). develop HEAD (start of run) `9b3b966` — Deploy-to-Development `success`. lyra runner (`RUN_E2E=1`): **PASS=5/FAIL=1** (lint/type-check/unit **2996 tests**/scripts/build PASS; **audit FAILED** — new HIGH: `js-yaml` GHSA-5p4m-2wfm-xmqj/CVE-2026-59870, "fix not backported" to 3.x/4.x, hitting the SEC-90 pin (3.15.0/4.3.0) directly). Fixed same run (see next column) → re-verified **PASS=6/FAIL=0**. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51). `e2e`: ran `--project=chromium` directly after installing chromium — 3 apparent failures (`Missing required environment variable: NEXT_PUBLIC_SUPABASE_URL`, 404-checks 500ing + waitlist heading missing), traced to the sandbox having no `.env.local`, **not a code defect** — confirmed by cross-checking the same 3 scenarios against the real deployed `dev.checklyra.com`/`checklyra.com`: unknown-slug → `404` on both, waitlist `<h1>` reads "You're on the list" exactly as expected. `mobile-safari` (webkit) untested — no webkit binary installed this run. lyra-mcp-server (`main` `31c114b`): `tsc` clean + **734/734** (44 suites) PASS, matching the documented floor exactly; its own `npm audit` showed **4 HIGH + 3 moderate** (fixed, see next column). | **SEC-126** (fixed, PR [#704](https://github.com/luisa-sys/lyra/pull/704), merged to develop `724cdaa`): consolidated the two range-scoped `js-yaml` overrides into one blanket `"js-yaml": "5.2.3"` — the range-scoped form (`js-yaml@^3.0.0`/`^4.0.0` both → 5.2.3) left the npm tree in an `ELSPROBLEMS`/invalid state for the `@lhci/utils`/`@istanbuljs/load-nyc-config` chains; the plain unscoped key resolved cleanly. Verified: audit clean, lint byte-identical to baseline, type-check/build/2996-unit-tests green, no test modified. **SEC-127** (fixed, PR [#141](https://github.com/luisa-sys/lyra-mcp-server/pull/141), merged to lyra-mcp-server main `3a6a1ea`): plain **non-force** `npm audit fix` cleared all 7 advisories (js-yaml, brace-expansion ×2, fast-uri, ip-address, plus 2 moderate Hono transitively) — every version stayed inside the already-declared `^`-range, so `package.json` untouched, lockfile-only. `fast-uri`/`ip-address` are production-reachable there (ajv inside the MCP SDK; `express-rate-limit`), so verified beyond the test suite: server builds, boots, `/health` → 200. This supersedes the 2026-08-05 row's deferral of `ip-address` as needing a forced/breaking fix — a non-force in-range bump existed upstream by this run. **SEC-116** closed as a duplicate once #141 landed. TODO/FIXME scan: 2 hits in `src/`, both already ticket-tracked (KAN-184). | **Report-only (out of autonomous bounds — security/RLS/architectural/migration, all already tracked):** 69 open BUGS/SEC tickets surveyed (up from 66 on 2026-08-05), including High SEC-100/101/103/104/105/106/108/112/113/117/23/3/30/31/32 and BUGS-75/77/85/86; no new Highest-priority open **bug** ticket — no release-block condition from that source. No GitHub Dependabot/CodeQL alert-listing tool was reachable this run (no `list_alerts`-class tool in the connected GitHub MCP server) — flagged as a coverage gap for a future run rather than silently skipped. `integration`+`e2e` env-gap classification persists in the deterministic runner (BUGS-51 — unchanged). | **YES — release rehearsed to staging; drift 🔴 RED.** `origin/main..origin/develop` = **16 commits** (recomputed after this run's own fix landed): 6 `docs`, 2 `fix` (this run's SEC-126, plus SEC-123 already shipped), 6 `feat` (4× `feat(profile)`, 1× `feat(search)`, 1× `feat(profile)` school/org picker — confirmed touching `src/app/**/*.tsx` **rule-C look/text surface**, unreviewed by this routine, Luisa's to review), 2 `test`. Mixed fix+feature set — would never have qualified for the withdrawn fix-only auto-promote exception even if it were live; moot since production promote is manual-only regardless. Drift is 🔴 RED (16 ≥ 15 threshold) — policy says promote today. Ran `promote-to-staging.yml` (run [166](https://github.com/luisa-sys/lyra/actions/runs/31143169112)) against develop HEAD `724cdaa` — dev-CI check + merge-to-staging both **success**; `deploy-staging.yml` (build/deploy/health/E2E-on-staging) was still verifying at the time this row was written and its final conclusion was not confirmed within this session's window — check run [166](https://github.com/luisa-sys/lyra/actions/runs/31143169112) directly for the outcome. `stage.checklyra.com` → 302 (Vercel SSO, expected) at last check. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-08-08 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` `ok` (v1.0.0). develop HEAD (start of run) `9b3b966` → ended `1103fc6`, CodeQL + Deploy-to-Development green throughout. lyra runner (`RUN_E2E=1`): scripted run showed **PASS=4/FAIL=2/UNVERIFIED=2** — `unit` FAILed once (exit 1; the script's own `tail -3` log capture only retained the `Snapshots`/`Time`/`Ran all test suites` lines, not the `Tests:`/`Test Suites:` failure counts, so the exact failing test was never identified) but **re-verified clean 3 consecutive times immediately after** (`npm run test:unit` and a direct `npx jest` invocation, all **250/250 suites, 2996/2996 tests**) — treated as a non-reproducible transient flake (resource contention with concurrent background agents this session is the leading suspicion), not a regression; **no test modified/skipped/weakened** (Test Integrity Policy intact). `audit` FAILed on a fresh **HIGH**: `nanoid <3.3.17` (GHSA-2v37-7h3g-55p8, transitive via `@tailwindcss/postcss` → `postcss@8.5.23` [SEC-94 override] → `nanoid@3.3.16`) — fixed same run (see next column), re-verified **PASS=6/FAIL=0** on a clean `npm ci`. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51). `e2e`: `chromium` project lists **22 tests** fine; `mobile-safari`/webkit **UNVERIFIED** — no webkit binary in this sandbox this run (only Chromium pre-installed). lyra-mcp-server (`main` `3a6a1ea`): `tsc` clean + **734/734** (44 suites) PASS, matching the documented floor exactly; MCP `build_sha` confirmed **== `origin/main` HEAD** via `/.well-known/mcp.json` — zero deploy drift. | **SEC-128** (fixed, PR [#708](https://github.com/luisa-sys/lyra/pull/708), merged to develop `1103fc6`): added a scoped `"nanoid": "^3.3.17"` override to `package.json` (same SEC-94 pattern as the existing `postcss`/`brace-expansion`/`minimatch` overrides), resolves to `nanoid@3.3.18`. Verified empirically on a clean `npm ci`: lint byte-identical to baseline, type-check/build/2996-unit-tests green, audit clean (0 high/critical; 2 pre-existing dev-only moderate `uuid`/`@lhci/cli` findings remain — SEC-97 precedent, not chased). No test modified/skipped/weakened. | **Report-only (out of autonomous bounds — security/RLS/architectural/migration, all already tracked):** 67 open BUGS/SEC tickets surveyed (down from 69 on 2026-08-07 — SEC-128 closed Done same run), including High SEC-100/101/103/104/105/106/108/112/113/117/23/3/30/31/32 and BUGS-75/77/85/86 — all report-only per this routine's autonomous bounds. 8 open Dependabot PRs surveyed, none auto-merged: **#689** `ip-address` 10.2.0→10.4.0 (low-risk minor, `base=main` structurally blocked by `main-chain-guard.yml`/SEC-98, superseded by prior overrides anyway), **#680**/**#679** (grouped dev/prod dependency bumps), **#661** (`github-actions` group), and 4 **MAJOR** version bumps deliberately left for Luisa's own review rather than auto-merged (breaking-change risk, CLAUDE.md gotcha #23 precedent — a forced major bump crashed lint once already): **#605** `@testing-library/jest-dom` 6→7, **#604** `eslint` 9→10 (interacts with the existing `minimatch`/`brace-expansion` override scheme), **#603** `typescript` 5→7, **#602** `jose` 5→6 — a JWT/OAuth library (KAN-88), security-adjacent and particularly worth her review. CodeQL: last 15 runs on `develop` all `success`, no new alerts. No GitHub Dependabot *security-alert*-listing tool reachable this run either (same BUGS-51-adjacent coverage gap noted 2026-08-07) — surveyed via open Dependabot PRs instead. | **YES — release rehearsed to staging; drift 🔴 RED.** `origin/main..origin/develop` = **18 commits**: 3 `fix` (2 already-shipped dependency security patches + this run's own SEC-128 nanoid fix), 7 `docs`, 6 `feat` (4× `feat(profile)`, 1× `feat(search)`, 1× `feat(profile)` school/org picker — confirmed touching `src/app/**/*.tsx` **rule-C look/text surface**, unreviewed by this routine, Luisa's to review), 2 `test`. Mixed fix+feature set — moot for the withdrawn fix-only auto-promote exception regardless, since production promote is manual-only per boundary (A). Drift 🔴 RED (18 ≥ 15 threshold). Ran `promote-to-staging.yml` (run [31235997539](https://github.com/luisa-sys/lyra/actions/runs/31235997539)) against develop HEAD `1103fc6` — **all 6 jobs green**, including the final "Verify staging actually serving merged SHA" step (Vercel SHA match + staging health check + MCP health check all passed), a cleaner confirmation than the 2026-08-07 run got. Directly re-verified: `stage.checklyra.com` → `302` (Vercel SSO, expected); `origin/staging` HEAD `68ed114` confirmed to have `origin/develop` (`1103fc6`) as an ancestor — staging fully caught up. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-08-09 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` `ok` (v1.0.0). develop HEAD (start of run) `39519249` — Deploy-to-Development + CodeQL both `success`. lyra runner (`RUN_E2E=1`): **PASS=5/FAIL=1/UNVERIFIED=2** — `unit` FAILed: `tests/unit/version-drift.test.js` — `package.json` `0.1.37` matched no existing tag (57 releases behind `v0.1.94`), a KAN-166 recurrence — fixed same run (see next column), re-verified **256/256 suites, 3104/3104 tests** green. `integration` UNVERIFIED (no `tests/integration/` dir, BUGS-51). `e2e` UNVERIFIED — sandbox ships `chromium_headless_shell-1194`, this `@playwright/test` pin wants rev `1234` (BUGS-51 env gap, not a code defect). lyra-mcp-server (`main` `3a6a1ea`, later `4b70240` after doc fix): `tsc` clean + **734/734** (44 suites) PASS — now matches the (corrected) documented floor exactly. TODO/FIXME scan: 2 hits in `src/`, both already ticket-tracked (KAN-184). | **BUGS-89** (KAN-166 recurrence, fixed, PR [#719](https://github.com/luisa-sys/lyra/pull/719) merged `3ceabdb`): `package.json`/`package-lock.json` resynced 0.1.37→0.1.94; added a new 20-release drift-window test (existing assertion untouched, per Test Integrity Policy), mutation-proven by reintroducing the drifted value and confirming red, then restoring green. Left BUGS-89 **In Progress** (not Done) — the `Prevention:` line flags an unresolved process question (why the bump doesn't consistently land on `develop`) needing Luisa's decision. Also fixed: `luisa-sys/lyra-mcp-server`'s `CLAUDE.md` test-floor doc (217/13 → 734/44, PR [#142](https://github.com/luisa-sys/lyra-mcp-server/pull/142) merged `4b70240`) — superseded a stale unmerged PR #126 (638/41) open since 2026-07-24; recommend closing #126. Self-update (routine step 6, see log below): `scripts/weekly-health-regression.sh`'s FAIL detail used a bare `tail -n 3`, which can land entirely inside jest's boilerplate and never show which suite failed — hit for real on this run AND the 2026-08-08 row above. Fixed + mutation-proven, PR [#720](https://github.com/luisa-sys/lyra/pull/720) merged `816054f`. No test modified/skipped/weakened anywhere (Test Integrity Policy intact). | **Report-only (out of autonomous bounds — security/RLS/architectural/migration, all already tracked):** ~89 open BUGS/SEC tickets surveyed, including High/Medium SEC-100/101/103/104/105/106/108/112/113/117/118/119/120/121/122 and BUGS-75/77/80/85/86/87/88; no new Highest-priority open **bug** ticket. 40+ open PRs on `luisa-sys/lyra` alone, most stale doc/run-log drafts from daily routines never merged — **BUGS-88 still open and worse than when filed** (now spans most of the 2026-08 date range too). **New finding, filed [BUGS-90](https://checklyra.atlassian.net/browse/BUGS-90):** this routine's own Confluence heartbeat on the Ops Routines Control Room (34275370) had exactly one row dated 2026-08-02, despite this repo's run log showing 6 more complete runs since (08-03 through 08-08) — same defect class as BUGS-81/BUGS-82/SEC-87/SEC-108 (a concern-owner silently dark on the one page the watchdog reads). Wrote this run's own heartbeat row correctly to break the streak. `auto-promote-to-staging.yml` (the Sunday 23:00 UTC weekly cron) shows GitHub state **`disabled_manually`** — contradicts `docs/RELEASE_POLICY.md`'s description of it as active; **not re-enabled autonomously** (a cadence/infra decision, not this routine's call) — flagging for Luisa to confirm intentional. `git push` surfaced **"6 vulnerabilities (3 high, 3 moderate)"** on the repo's default branch via GitHub's own Dependabot scan; no MCP tool reachable this run to enumerate specifics (same BUGS-51-adjacent coverage gap noted 2026-08-07/08) — recommend checking the GitHub Security tab directly. | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **27 commits** (post-fix): 8 `fix` (7 pre-existing + this run's BUGS-89 fix), 9 `docs`, 7 `feat` (4× `feat(profile)`, 1× `feat(search)`, 1× `feat(profile)` school/org picker, 1× `feat(qa-sweep)` non-UI CI/ops tooling), 2 `test`, 1 `chore`. All `feat(profile)`/`feat(search)` commits carry proper `UI-Change-Approved:` trailers with real Jira keys (KAN-443/444/445/446/450/451) — **no rule-C look/text violations found**. Mixed fix+feature set — moot regardless since production promote is manual-only. Drift 🔴 **RED** (27 ≥ 15 threshold — promote today). Ran `promote-to-staging.yml` (run [31291316847](https://github.com/luisa-sys/lyra/actions/runs/31291316847), **concluded `success`**) against develop HEAD `3ceabdb` — dev-CI verify, merge-to-staging, un-skippable signup E2E, staging build/deploy, post-deploy health checks (Vercel SHA match + `/api/health` env wiring + MCP health + staging smoke test), **and** the deploy's own trailing full Playwright E2E suite against `stage.checklyra.com` (run [31291431936](https://github.com/luisa-sys/lyra/actions/runs/31291431936), **concluded `success`**) — every job green, cleaner confirmation than several recent rows got. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-08-10 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` `ok` (v1.0.0). develop HEAD `2897ca2` — Deploy-to-Development green. lyra runner (`RUN_E2E=1`): first pass showed **PASS=4/FAIL=2** — `unit`+`scripts` FAILed with 28 assertion failures concentrated entirely in `tests/scripts/check-extraction-dod.test.js`, every one expecting exit 0/1 and getting exit **2** ("cannot verify"). Root-caused (not a code defect): **the `gh` CLI is entirely absent from this session's sandbox** — the script's own `[out-of-repo]` check fails closed with `gh is not available, so the lyra-mcp-server back-references could not be checked`, exactly as it's designed to on a real missing-tool day. Installed `gh` via `apt-get install -y gh` (this sandbox only, not a repo change) and re-ran: **38/38 pass**, then the full suite **re-verified PASS=6/FAIL=0/UNVERIFIED=2** (lint/type-check/unit **3246 tests**/scripts/build/audit clean; `integration` UNVERIFIED — no `tests/integration/` dir, BUGS-51 unchanged; `e2e` UNVERIFIED by the script's classifier — sandbox chromium rev 1194 vs required 1234, no webkit binary at all). **Hand-verified e2e for real**, same pattern as prior runs: ran `--project=chromium` with the CI-matching dummy Supabase env plus a temporary **local-only** `playwright.config.ts` `launchOptions.executablePath` pointing at the pre-installed rev-1194 binary (reverted immediately after, confirmed clean `git status`) — **17/17 runnable tests passed**, 5 correctly skipped (populated-profile fixture gate). `mobile-safari`/webkit untested — no webkit binary this sandbox (unchanged gap). lyra-mcp-server (`main` `4b70240`): `tsc` clean + **734/734** (44 suites) PASS, matching the documented floor exactly. | **None — no product FAIL, nothing to fix; no test modified/skipped/weakened** (Test Integrity intact) — the apparent FAIL was 100% a sandbox tooling gap (`gh` CLI), not a repo defect, so no BUGS/SEC ticket filed for it. TODO/FIXME scan: 2 hits in `src/`, both already ticket-tracked (KAN-184). | **Report-only (out of autonomous bounds — security/RLS/architectural/migration, all already tracked):** open BUGS/SEC surveyed by priority — zero **Highest**-priority open BUGS/SEC ticket; KAN-90 (OAuth-dashboard audit checklist) and KAN-442 (design-rebuild Story) are the only Highest-priority KAN items and neither is a defect — no release-block condition from that source. 8 open Dependabot PRs surveyed (**#711**/#680/#661/#633/#605/#604/#603/#602 — same set as 2026-08-08 plus #711), none auto-merged: majors left for Luisa per established precedent, `#633` (`dependency-cruiser` 17→18) stays deferred per CLAUDE.md's own gotcha (needs the PR-Quality-Gate job moved to Node 22 first, still on Node 20). No GitHub Dependabot/CodeQL *alert*-listing tool reachable this run either (same recurring coverage gap noted 2026-08-07/08) — surveyed via open Dependabot PRs instead. | **YES — release rehearsed to staging; drift 🔴 RED.** `origin/main..origin/develop` = **41 commits** (11 merge + 30 real: 11 `fix`, 6 `feat`, 4 `refactor`, 4 `docs`, 4 `chore`, 1 `test`). Drift 🔴 RED (41 ≥ 15 threshold — promote today). None of the 6 `feat` commits touch rule-C look/text paths (all CTL-0xx/SEC-120/oauth-extraction CI-and-controls work); the `refactor` commits that DO touch `src/app/**/*.tsx` (KAN-415 module-extraction import-path rewrites) already carry proper `UI-Change-Approved: KAN-415` / `UI-Bugfix-Only: BUGS-87` trailers from the sessions that authored them — nothing new to flag. Ran `promote-to-staging.yml` (run [31350816356](https://github.com/luisa-sys/lyra/actions/runs/31350816356)) against develop HEAD `2897ca2` — **success**; directly re-verified `origin/staging` HEAD `d4d3e26` has `origin/develop` (`2897ca2`) as an ancestor (staging fully caught up), `stage.checklyra.com` → `302` (Vercel SSO, expected), `mcp.checklyra.com/health` → `ok`. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-08-11 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `{status:ok, version:1.0.0}`. develop HEAD (start of run) `76229ef1` — Deploy-to-Development + CodeQL both `success`. lyra runner (`RUN_E2E=1`): first pass **PASS=4/FAIL=2/UNVERIFIED=2** — `unit`+`scripts` both FAILed on `tests/scripts/check-extraction-dod.test.js` (28/38 cases, all one root cause — see Fixes column); re-verified **PASS=6/FAIL=0** (lint/type-check/unit/scripts/build/audit) after the fix. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51 env gap). `e2e` **UNVERIFIED** (Playwright browsers unavailable in this sandbox — pre-baked chromium rev doesn't match `@playwright/test@^1.62.0`'s required rev; BUGS-51 same family, `npx playwright install` intentionally not run per this environment's own instructions). lyra-mcp-server (`main` `b0b0ba2`, tested in a throwaway worktree): `tsc` clean + **733/733** (44 suites) PASS — 1 test below the 734 CLAUDE.md documents as last measured 2026-08-09; no enforced floor guard exists in that repo (prose-only), so this is noted as a 1-test drift to re-measure, not treated as a regression (no evidence of an intentional deletion). | **BUGS-93** (fixed, PR [#751](https://github.com/luisa-sys/lyra/pull/751), squash-merged to develop `81af526d`): `scripts/check-extraction-dod.sh` (CTL-033) checked `command -v gh` and failed closed *before* checking whether the test-suite's `MCP_TARBALL` fixture-injection env var was set — so any environment with no `gh` binary at all (this session's, where GitHub access goes through MCP tools instead) failed the check even though the tarball fixture makes `gh` unnecessary. One-line reorder (`[ -z "${MCP_TARBALL:-}" ] &&` prepended to the `gh`-missing condition); zero behaviour change on CI (GitHub-hosted runners always have `gh`, so that branch was already unreachable there) — a pure environment-portability fix, same family as CLAUDE.md gotchas #28/#30. All 38 existing tests pass as-written, no test modified. CI (PR Quality Gate + CodeQL + E2E Tests) all green before merge. Prevention: none — the existing 38-case suite is itself the control and already covered this path; no new gate needed. | **Report-only (out of autonomous bounds — security/RLS/architectural/migration, all already tracked):** 45 open BUGS/SEC tickets surveyed (JQL `project in (BUGS,SEC) AND statusCategory != Done`), topped by SEC-129 (High — Cloudflare WAF rule bypassable via a spoofable `User-Agent: github-actions` header, no secret required), SEC-108/SEC-87 (High — two ops-routine heartbeats dark, doc-sync + daily-security concern owners both overdue), SEC-117 (High — SAR export completeness test hard-codes its own oracle, same family as BUGS-86/SEC-113 below), BUGS-86 (assertion derives its expected value from the constant under test), SEC-113 (rate-limiter test reimplementation, already CTL-038-extended and largely resolved per its own ticket state) — none of these are safe for autonomous fix under boundary 4 (security-sensitive, test-oracle rewrites needing sign-off, or founder-only CF dashboard access). Not enumerated further per this routine's scope; full list left in Jira for Luisa's triage. TODO/FIXME scan: 2 hits in `src/`, both already ticket-tracked or a documented future-work stub. `npm audit --audit-level=high`: lyra clean (2 moderate only, GitHub push also flagged 1 moderate on this session's branch — consistent, not chased per gotcha #23). No Dependabot/CodeQL alert-listing tool reachable this run (same BUGS-51-adjacent coverage gap noted in prior rows). | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **2 commits**, both non-feature: `test(signup-gate)` #748 (already on develop at run start) + this run's own `fix(BUGS-93)` #751. No migrations, no `src/app/**/*.tsx`/component/copy changes — rule-C surface untouched. Drift 🟢 GREEN (2 < 5 commits, <1 day old). Ran `promote-to-staging.yml` (run [31453966667](https://github.com/luisa-sys/lyra/actions/runs/31453966667)) against develop HEAD `81af526d` — **all jobs green** (verify-source, merge to staging, staging deploy + SHA-matched health checks). Directly re-verified: `stage.checklyra.com` → `302` (Vercel SSO, expected); `mcp.checklyra.com/health` → `ok`. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-08-12 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `{status:ok, version:1.0.0}`. develop HEAD (start of run) `62db86b` — CodeQL + Deploy-to-Development both `success`. lyra runner (`RUN_E2E=1`): **PASS=6/UNVERIFIED=2/FAIL=0** — lint/type-check/unit/scripts/build/audit all clean; `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51 env gap, unchanged); `e2e` **UNVERIFIED** (pre-baked chromium rev 1194 ≠ `@playwright/test@^1.62.0`'s required rev 1234, `npx playwright install` intentionally not run per this environment's own instructions). lyra-mcp-server (`main` `10406a0`): `tsc` clean + **733/733** (44 suites) PASS, confirming last run's 1-test drift was real and stable (traced to PR #147/SEC-83, not a regression) — floor doc updated. | **BUGS-94** (fixed, PR [#759](https://github.com/luisa-sys/lyra/pull/759), squash-merged to develop `06c098e`): `scripts/check-live-schema-parity.py`'s brand-new CTL-048 (landed the day before via SEC-104/#757) had a `load_baseline()` that silently parsed to an empty set against the real `supabase/schema-drift-baseline.json` — wrong nesting level (`raw.items()` instead of `raw["drift"].items()`) *and* wrong field names (`table`/`column` instead of the file's single `line` string) — so every already-ticketed SEC-107/BUGS-62 drift entry read as new, unbaselined drift and blocked the very first `develop → staging` promote this control ever gated. Fixed to parse the same `line` shape CTL-036's established checker already uses; mutation-proved (3 new self-test fixtures go red with only the parser reverted, green with it fixed); `SELF_TEST_FLOOR` raised 6→9. Re-ran `promote-to-staging.yml` after merge and confirmed the fix actually works: the SEC-107/BUGS-62 findings are gone from the job's output. Also fixed this run: lyra + lyra-mcp-server test-floor prose lines re-measured (3630/278 and 733/44 respectively) — companion PR [lyra-mcp-server#150](https://github.com/luisa-sys/lyra-mcp-server/pull/150), merged. | **Report-only / stopped-on (out of autonomous bounds):** **BUGS-95** (new, High) — the *second* re-run of `promote-to-staging.yml` (post-BUGS-94-fix) hit a different, genuine CTL-048 finding: `src/types/database/{dev,staging,prod}.ts` are all equally stale (missing `profile_conversation_starters.custom_prompt` and `profile_items.group_label`, present on all three live DBs) — exactly the "all three snapshots go stale the same way" blind spot CTL-048's own docstring cites as its reason to exist, so the control is working as designed. Not auto-fixed: it's data-model-adjacent (the committed type snapshots CTL-036's ratchet also depends on), and the checker's own error message points at "the documented step in docs/RUNBOOK.md" — which **does not exist** (`grep` for it across `docs/*.md` returns nothing), so there is no established procedure to follow. Filed for Luisa's decision on regeneration + writing the actual step. 50 open BUGS/SEC tickets surveyed otherwise (up from 45 on 2026-08-11), topped by SEC-129/SEC-108/SEC-87/SEC-117 (already tracked, same as last run) — none safe for autonomous fix under boundary 4. TODO/FIXME scan: 2 hits in `src/` (lyra) + 0 (lyra-mcp-server), all already ticket-tracked. No Dependabot/CodeQL alert-listing tool reachable this run (same recurring coverage gap). GitHub flagged 1 moderate npm advisory on push (not chased, gotcha #23 precedent). | **NO — staging NOT reached this run.** `origin/main..origin/develop` = **2 commits**: `feat(security)` SEC-104/#757 (founder-authored, `UI-Change-Approved: SEC-104` trailer already present, out of this routine's review scope) + this run's own `fix(BUGS-94)` #759. Both `promote-to-staging.yml` attempts this run failed **before** the merge-to-staging step ever ran (`schema-parity` job, CTL-048) — first on BUGS-94 (now fixed), second on BUGS-95 (unresolved, needs Luisa). `origin/staging` is unchanged at `eda6f6f`, still 2 commits behind develop. Once BUGS-95 is resolved, re-run `gh workflow run promote-to-staging.yml -f confirm=promote`; only after that succeeds do Luisa's usual commands apply: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-08-13 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `{status:ok, version:1.0.0}`. develop HEAD (start of run) `98ae873` — CodeQL + Deploy-to-Development both `success`. lyra runner (`RUN_E2E=1`): **PASS=6/UNVERIFIED=2/FAIL=0** — lint/type-check/unit/scripts/build/audit all clean. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51 env gap, unchanged). `e2e` **UNVERIFIED**: of 20 nominal "failures", 17 were `mobile-safari`/webkit (`Executable doesn't exist at /opt/pw-browsers/webkit-2336/pw_run.sh` — only Chromium pre-installed this session, BUGS-51 family) and the remaining 3 (`chromium`) were traced to this sandbox holding **no Supabase credentials at all** (`env | grep SUPABASE` empty) — `NEXT_PUBLIC_SUPABASE_URL` missing crashes the local-build webServer's Supabase-dependent SSR routes (`/waitlist`, `[slug]` 404 page) with a 500, not a code defect; CI (which does hold dev secrets) is green throughout, confirming this is sandbox-only. 14 non-Supabase-dependent chromium/mobile-safari tests passed. No FAILs — nothing to fix. lyra-mcp-server (`main`, fast-forwarded 4b70240→655c9c0): `tsc` clean + **733/733** (44 suites) PASS, matching the documented floor exactly. | **None fixed — none needed.** Full regression run produced zero FAILs in application code on both repos. Two comments added to existing tickets instead of a new fix: **SEC-131** — confirmed its last open acceptance criterion ("CTL-048 passes in its first real execution during develop → staging") is now true, evidenced by this run's clean `promote-to-staging.yml` (run [31661737188](https://github.com/luisa-sys/lyra/actions/runs/31661737188)); **BUGS-95** — flagged superseded by SEC-131/SEC-133 (the stale-snapshot fix + the `docs/RUNBOOK.md` regeneration step it was blocked on both landed since 2026-08-12), recommended for closure with `Prevention: CTL-048 (see SEC-131)`, left for Luisa/owner to formally transition rather than closed by this routine. | **Report-only (out of autonomous bounds — security/RLS/architectural, all already tracked):** 50 open BUGS/SEC tickets surveyed (JQL `project in (BUGS,SEC) AND statusCategory != Done`), unchanged count from 2026-08-12, topped by the same High-priority set (SEC-3/23/30/31/32 governance/DR/WORM-encryption, all founder-decision items) — none safe for autonomous fix under boundary 4. TODO/FIXME scan: 2 hits in `src/` (lyra, both pre-existing future-ticket markers in `candidate-sourcing.ts`) + 0 (lyra-mcp-server). ~29 open `github-actions` bot-filed issues surveyed (mostly stale `🔴 Health check failed` issues from March–July, health currently green throughout — not actioned, out of this routine's remit) plus the standing `autoheal-tracked`/`anomaly` issues (unchanged, already tracked). No Dependabot/CodeQL alert-listing tool reachable this run (same recurring coverage gap noted every prior row). | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **2 commits**, both infra/security, neither touching any founder-owned UI/copy path (rule C clear): `feat(sec-137)` CTL-049 migration-ledger-parity control `#770` + `fix(sec-135)` npm-audit HIGH fixes + docs `#769`. No blocking Highest-priority bug ticket; not Friday UK afternoon; no incident. Ran `promote-to-staging.yml` (run [31661737188](https://github.com/luisa-sys/lyra/actions/runs/31661737188)) against develop HEAD `98ae873` — **all 6 jobs green**, including `schema-parity` (CTL-048, see SEC-131 note) and the final SHA-verify step. Directly re-verified: `stage.checklyra.com` → `302` (Vercel SSO, expected); `origin/staging` HEAD `9d1fdd6` confirmed to have `origin/develop` (`98ae873`) as an ancestor. **Also found:** `staging`/`beta`/`main` were already fully in sync with each other going into this run — Luisa (or another session) ran the whole chain through to production on 2026-08-12 between 10:50–13:24 UTC, after the prior day's routine report. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-08-14 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `{status:ok, version:1.0.0}`. develop HEAD (start of run) `5750362` — CodeQL + Deploy-to-Development both `success`. lyra runner (`RUN_E2E=1`): **PASS=6/UNVERIFIED=2/FAIL=0** — lint/type-check/unit/scripts/build/audit (prod-scope, CTL-052) all clean. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51 env gap, unchanged). `e2e` **UNVERIFIED** (BUGS-51 family): pre-baked `chromium_headless_shell` rev `1194` ≠ `@playwright/test@1.62.0`'s required rev `1234` — confirmed by direct repro (`CI=1 npx playwright test homepage.spec.ts --project=chromium` → `Error: browserType.launch: Executable doesn't exist at /opt/pw-browsers/chromium_headless_shell-1234/…`, fails at browser launch, never reaches an assertion); `--list` and the plain `chromium` executable both resolve fine, isolating the gap to the headless-shell revision specifically. lyra-mcp-server (`main`, fast-forwarded 655c9c0, already HEAD): `tsc` clean + **733/733** (44 suites) PASS, matching the documented floor exactly. | **None fixed — none needed.** Full regression run produced zero FAILs in application code on either repo; nothing to fix, nothing STOPPED-on requiring a test change. | **Report-only (out of autonomous bounds — security/RLS/architectural/governance, all already tracked):** 50 open BUGS/SEC tickets surveyed (JQL `project in (BUGS,SEC) AND statusCategory != Done`), unchanged count and top set from 2026-08-13 (SEC-3/23/30/31/32 governance/DR/WORM-encryption/admin-sign-off, all founder-decision items) — none safe for autonomous fix under boundary 4. TODO/FIXME scan: 2 hits in `src/` (lyra, same pre-existing future-ticket markers in `candidate-sourcing.ts`, both KAN-184-tracked) + 0 (lyra-mcp-server). CodeQL: last 5 runs across all chain branches (develop/staging/beta/main) `success`, no new alerts. GH issue #295 (`autoheal-tracked`/`autoheal/needs-human`, open since 2026-05-27, 21 comments) — `VERCEL_AUTOMATION_BYPASS` needs rotation in the Vercel dashboard, founder-only action, unchanged. 2 open Dependabot PRs surveyed, neither auto-merged: **#749** (6 dev-dependency bumps, routine) and **#711** (3 production-dependency bumps — `@sentry/nextjs`, `@supabase/ssr`, `next` — no CVE indicated in either changelog, left for Luisa's own review since it touches the production tree). **PR #702** (`feat(KAN-469)`, founder-authored, `DESIGN_APPROVED` card approved 2026-08-06, `UI-Change-Approved: KAN-469` trailer present) has sat open 8 days with `mergeable_state: blocked` — not touched (rule C: it's Luisa's own PR on a founder-owned surface), flagged here so it doesn't go unnoticed. No Dependabot/CodeQL alert-*listing* tool reachable this run (same recurring coverage gap noted every prior row). | **YES — release rehearsed to staging; drift 🔴 RED.** `origin/main..origin/develop` = **78 commits** (up sharply from 2 on 2026-08-13 — the KAN-415 C2 tail (CTL-051/052/053/054), the modularisation-programme close-out (#771), SEC-135/136/137, and KAN-473/SEC-104's D9 extraction all landed since). Non-merge, non-docs breakdown: 21 `fix`, 15 `refactor`, 13 `feat`, 4 `test`, 4 `chore` — a mixed fix+feature set, moot for the withdrawn fix-only auto-promote exception regardless since production promote is manual-only per boundary (A). Drift 🔴 RED (78 ≥ 15 threshold) — policy says promote today. Checked every commit for rule-C exposure: 46 files under founder-owned `.tsx`/copy paths differ between `main` and `develop`, but every commit that touches one carries a proper `UI-Change-Approved:`/`UI-Bugfix-Only:` trailer (KAN-473, SEC-104, BUGS-87, plus the D1/D6–D9 module-extraction attestations) — no unauthorized look/text change found; `src/app/admin/**` hits are carve-out-exempt regardless. No blocking Highest-priority **bug** ticket (the two open Highest items, KAN-90/KAN-442, are backlog tasks, not live incidents); not Friday UK afternoon; no incident. Ran `promote-to-staging.yml` (run [31764603240](https://github.com/luisa-sys/lyra/actions/runs/31764603240)) against develop HEAD `5750362` — **all 6 jobs green**, including `schema-parity` (CTL-048) and the final SHA-verify step (Vercel SHA match + staging health + MCP health all passed). Directly re-verified: `stage.checklyra.com` → `302` (Vercel SSO, expected); `origin/staging` HEAD `09be9e1` confirmed to have `origin/develop` (`5750362`) as an ancestor. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |
| 2026-08-15 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `{status:ok, version:1.0.0}`. develop HEAD (start of run) `04ddbbc` — CodeQL + Deploy-to-Development both `success`. lyra runner (`RUN_E2E=1`): **PASS=4/UNVERIFIED=4/FAIL=0** on the first pass from an `EnterWorktree`-isolated session (`lint`/`type-check`/`build`/`audit` PASS; `unit`/`scripts`/`integration` all reported `0 matches` UNVERIFIED) — traced to a real environment defect, not a code regression: the session's worktree lived under `.claude/worktrees/…`, which `jest.config.js`'s own `testPathIgnorePatterns` (`/\.claude/`, KAN-180) is designed to exclude, so the entire suite was silently unmatched rather than run. Re-ran `unit`+`scripts` (`npx jest --testPathPatterns='tests/(unit|scripts)'`) and `integration` from a **sibling** worktree (`git worktree add ../lyra-jest-check origin/develop`, outside `.claude/`) for an honest signal: **PASS 295/295 suites, 3926/3926 tests** (exactly the documented floor); `integration` genuinely `0 matches` — no `tests/integration/` dir exists yet, the pre-existing BUGS-51 gap, unchanged. `e2e`: the required `chromium`/`chromium_headless_shell` rev `1234` was successfully installed this run (resolving the rev-`1194`-vs-`1234` mismatch every recent row above logged) but the headless-shell process now crashes with `SIGSEGV` on launch inside this sandbox (`browserType.launch: Target page, context or browser has been closed`) — a deeper container-capability gap, not a code defect; **UNVERIFIED**, BUGS-51 family. lyra-mcp-server (`main` `34f6d07`, tested in a throwaway worktree by a sub-agent): `tsc` clean + **752/752** (44 suites) PASS, matching the documented floor exactly; `npm audit --audit-level=high` 0 vulnerabilities. | **None fixed — none needed.** Zero FAILs in application code on either repo. **Self-update filed instead** (see log below and this run's PR): added CLAUDE.md gotcha #33 documenting the `EnterWorktree`-vs-`testPathIgnorePatterns` collision so a future session doesn't misread a real "0 matches" defect as the already-known `tests/integration/`-missing gap. | **Report-only (out of autonomous bounds — security/architectural/migration, all already tracked):** ~50 open BUGS/SEC tickets surveyed, topped by SEC-99/SEC-112/SEC-146/SEC-147/BUGS-81 — all High/Highest, all already in progress (draft PRs #801/#806 open against two of them from a concurrent session) — none safe for autonomous fix under boundary 4. **Flagging for Luisa specifically:** the pending `fix(SEC-112)` migration (`20260814150000_sec112_block_is_published_self_set.sql` + its companion invariant migration) is **not yet applied to any environment** — confirmed via direct `pg_trigger` query against all three Supabase projects (dev/staging/prod), none carry the new guard trigger. Per this repo's migration-ordering rule (`beta` runs on the *production* Supabase), that migration must reach production before any future `staging → beta` promote, not before `beta → main`. Not applied by this routine — migration/data-model changes are explicitly out of autonomous scope. TODO/FIXME scan: 2 hits in `src/`, both pre-existing KAN-184-ticketed stubs, unchanged. GH issue #288 (`anomaly`, open since 2026-05-27, 243 comments) is recurring low-signal noise (z-score spike against a near-zero baseline) — not a new finding. No stale run-log draft PRs remain open (SEC-146's backfill/auto-merge fix has evidently landed since 2026-08-14). Also noted, not acted on: `origin/main` advanced independently during this run (`19298ed`→`1ddd115`) — an out-of-band `beta → production` promote by Luisa or another session, not this routine. | **YES — release rehearsed to staging; drift 🟡 borderline (5 commits, <1 day).** `origin/main..origin/develop` = **5 commits**: 3 `docs`, 1 `chore`, 1 `fix(SEC-112)` — no `feat`, no ambiguous type. Checked every commit for rule-C exposure: only non-`.tsx` file touched under `src/app` is `src/app/dashboard/profile/actions.ts` (a server action, not UI) — no founder-owned UI/copy path affected. No blocking Highest-priority bug ticket against a live incident; not Friday UK afternoon; no incident; smoke tests (health checks) green. Ran `promote-to-staging.yml` (run [31859980700](https://github.com/luisa-sys/lyra/actions/runs/31859980700)) against develop HEAD `04ddbbc` — **all jobs green** (verify-source, schema-parity, signup-surface gate, merge, staging deploy SHA-matched, final health verification). Directly re-verified: `stage.checklyra.com` → `302` (Vercel SSO, expected). Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A), and did **not** apply the pending SEC-112 migration to any environment (see Stopped-on column). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

| 2026-08-16 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `{status:ok, version:1.0.0}`. develop HEAD (start of run) `187101b` — CodeQL + Deploy-to-Development both `success`. lyra runner (`RUN_E2E=1`): **PASS=6/UNVERIFIED=2/FAIL=0** — lint/type-check/unit/scripts/build/audit all clean. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51 env gap, unchanged). `e2e` **UNVERIFIED** (BUGS-51 family, unchanged): pre-baked `chromium`/`chromium_headless_shell` rev `1194` in this sandbox doesn't match `@playwright/test@^1.62.0`'s required rev — `npx playwright install` intentionally not run per this environment's own instructions. lyra-mcp-server (`main`, fast-forwarded `4b70240`→`550dfae`, PR [#154](https://github.com/luisa-sys/lyra-mcp-server/pull/154) SEC-46 revocation/audience): `tsc` clean + **768/768** (45 suites) PASS, matching the documented floor exactly. Both MCP endpoints' `build_sha` (`mcp.checklyra.com` and `mcp-dev.checklyra.com`) confirmed byte-identical to `origin/main` HEAD. | **None fixed — none needed.** Zero FAILs in application code on either repo; nothing to fix, nothing STOPPED-on requiring a test change. | **Report-only (out of autonomous bounds — security/architectural, all already tracked):** 63 open BUGS/SEC tickets surveyed (13 BUGS + 50 SEC, `statusCategory != Done`), topped by 5 Highest-priority SEC items — SEC-112/SEC-46/SEC-99 (all In Progress) and SEC-109/SEC-118 (To Do) — none safe for autonomous fix under boundary 4 (security/RLS/architectural). **Noted, not acted on:** SEC-99's own fix (deleting the two chain-bypass promote wrappers, CTL-064) already landed on `develop` and `main` on 2026-08-14/16 per CLAUDE.md gotcha text, but the Jira ticket itself is still "In Progress" — looks like a stale status the ticket owner should reconcile, not a live gap; not transitioned by this routine (Jira ticket-state changes on someone else's ticket are outside this routine's autonomy). TODO/FIXME scan: 2 hits in `src/` (lyra), same pre-existing KAN-184-ticketed stubs in `candidate-sourcing.ts`, unchanged; 0 in lyra-mcp-server. Open PRs: only **11** now (down sharply from the 50 BUGS-98 flagged on 2026-08-13) — 8 Dependabot (4 stale >2 weeks: #602/603/604/605 since 07-27, #633 since 07-29, #661 since 08-01, neither auto-merged, left for Luisa per gotcha #23 precedent), 2 founder draft PRs (#819 SEC-106/CTL-065, #629 SEC-109, both in-progress and not this routine's to touch), 1 founder PR #702 (KAN-469, `DESIGN_APPROVED`, still `mergeable_state: blocked`, unchanged for 10 days) — **no stale routine-opened run-log draft PRs remain**, confirming BUGS-98's backlog was cleared. GH issue #288 (`anomaly`, open since 2026-05-27, 311 comments) is the same recurring low-signal noise. No Dependabot/CodeQL alert-*listing* tool reachable this run (same recurring coverage gap). | **YES — release rehearsed to staging; drift 🟢 GREEN.** `origin/main..origin/develop` = **4 commits**: 2 `fix` (SEC-152, SEC-153), 1 `feat` (SEC-153/CTL-065), 1 `docs` — no ambiguous type. ⚠️ **First reading of this used a stale cached `origin/main` and showed 116 commits** — the routine's own gotcha (2026-07-29 self-update log entry below) about fetching `origin/main` explicitly before the drift computation is why this was caught rather than reported wrong; re-fetching `origin/main` (which had genuinely moved to `029896a`, a same-morning production promote) corrected it to 4. Checked all 4 commits for rule-C exposure: diff touches `.github/workflows/`, `CLAUDE.md`, `controls/registry.json`, `docs/**`, `scripts/**`, `tests/**` only — zero `.tsx`/component/CSS/copy-module files, rule C clear. No blocking Highest-priority bug ticket against a live incident; not Friday UK afternoon (Sunday); no incident; smoke tests green. Ran `promote-to-staging.yml` (run [31943394454](https://github.com/luisa-sys/lyra/actions/runs/31943394454)) against develop HEAD `187101b` — **completed success**. Directly re-verified: `origin/staging` HEAD `fc585af` confirmed to have `origin/develop` (`187101b`) as an ancestor; `stage.checklyra.com` → `302` (Vercel SSO, expected). Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

| 2026-08-17 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `{status:ok, version:1.0.0}`. develop HEAD (start of run) `ba5d637` — Deploy-to-Development run [31984655444](https://github.com/luisa-sys/lyra/actions/runs/31984655444) `success`. lyra runner (`RUN_E2E=1`): **PASS=6/UNVERIFIED=2/FAIL=0** — lint/type-check/unit/scripts/build/audit all clean. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51 env gap, unchanged). `e2e` **UNVERIFIED** (BUGS-51 family, unchanged): pre-baked `chromium`/`chromium_headless_shell` rev `1194` in this sandbox doesn't match `@playwright/test@^1.62.0`'s required rev `1234` (webkit needs `2336`) — confirmed by direct repro (`npx playwright test homepage.spec.ts`): `Error: browserType.launch: Executable doesn't exist at /opt/pw-browsers/chromium_headless_shell-1234/…` and the equivalent webkit-2336 error; fails at browser launch, never reaches an assertion; `--list` and the plain `chromium` executable both resolve fine. `npx playwright install` intentionally not run per this environment's own instructions. lyra-mcp-server (`main` `550dfae`, unchanged from 2026-08-16, no new commits): `tsc` clean + **768/768** (45 suites) PASS, matching the documented floor exactly. Both MCP endpoints' `build_sha` (`mcp.checklyra.com` and `mcp-dev.checklyra.com`) confirmed byte-identical to `origin/main` HEAD. | **None fixed — none needed.** Zero FAILs in application code on either repo; nothing to fix, nothing STOPPED-on requiring a test change. | **Report-only (out of autonomous bounds — security/architectural, all already tracked):** ~64 open BUGS/SEC tickets surveyed (`project in (BUGS,SEC) AND statusCategory != Done`), up 1 from 63 on 2026-08-16 — new: **SEC-159** (filed by a separate PR/ticket-triage routine, 2026-08-16: grouped npm dependabot PRs go install-stale on the merge ref because a lockfile mismatch isn't a git conflict, so dependabot never rebases them; remediation already recorded on the ticket — PR #711 merged as `57e4bc6`, PR #749 had `@dependabot rebase` requested) — none safe for autonomous fix under boundary 4 (security/RLS/architectural). TODO/FIXME scan: 2 hits in `src/` (lyra), same pre-existing KAN-184-ticketed stubs in `candidate-sourcing.ts`, unchanged; 0 in lyra-mcp-server. Open PRs: **9** — 5 Dependabot majors open by design per `dependabot.yml` (#602 jose 5→6, #603 typescript 5→7, #604 eslint 9→10, #605 @testing-library/jest-dom 6→7, #633 dependency-cruiser 17→18 — CLAUDE.md's own gotcha warns not to bump this past 17.x without moving CI to Node 22 first), #749 (dev-deps group, install-stale per SEC-159, rebase already requested, not re-checked by this routine), 2 founder/other draft PRs (#828 KAN-466 docs, #629 SEC-109 — both in-progress, not this routine's to touch), 1 founder PR #702 (KAN-469, `feat`, `DESIGN_APPROVED`, `mergeable_state: blocked` — rule C, not touched). No stale routine-opened run-log draft PRs remain open. No Dependabot/CodeQL alert-*listing* tool reachable this run (same recurring coverage gap noted every prior row). | **YES — release rehearsed to staging; drift 🟢 GREEN.** `origin/main..origin/develop` = **4 commits**: 1 `chore` (docs-only run-log, `ba5d637`), 1 `deps` (production-dependencies bump #711, `57e4bc6`), 1 `fix` (BUGS-104, `04ca247`), 1 `ci` (github-actions pin bump #661, `e83fc0e`) — no `feat`, no ambiguous type. Checked all 4 commits for rule-C exposure: file diff touches `docs/`, `package.json`/`package-lock.json`, `.github/workflows/**`, `.github/absent-secrets.txt`, `.github/known-secrets.txt`, `.github/scheduled-workflow-exceptions.json`, `CLAUDE.md`, `controls/registry.json`, `scripts/`, `tests/support/**` only — zero `.tsx`/component/CSS/copy-module files, rule C clear. No blocking Highest-priority bug ticket against a live incident; not Friday UK afternoon (Monday); no incident; smoke tests green. Ran `promote-to-staging.yml` (run [31988843007](https://github.com/luisa-sys/lyra/actions/runs/31988843007)) against develop HEAD `ba5d637` — **all 6 jobs green**: verify-source, schema-parity (CTL-048), signup-surface gate (correctly skipped — no signup-surface files touched), merge, staging-deploy-wait, and the final SHA-verify + health-check step (`stage.checklyra.com` reachable, `mcp.checklyra.com/health` ok). Directly re-verified: `origin/staging` HEAD `894a3d5` confirmed to have `origin/develop` (`ba5d637`) as an ancestor. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

| 2026-08-18 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `{status:ok, version:1.0.0}`. develop HEAD (start of run) `1eccd6d` — Deploy-to-Development run [32087890506](https://github.com/luisa-sys/lyra/actions/runs/32087890506) `success`, CodeQL green. lyra runner (`RUN_E2E=1`): **PASS=6/UNVERIFIED=2/FAIL=0** — lint/type-check/unit/scripts/build/audit all clean. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51 env gap, unchanged). `e2e` **UNVERIFIED** (BUGS-51 family, unchanged): pre-baked `chromium`/`chromium-1194` present but the `mobile-safari` (webkit) project has no browser installed in this sandbox — `npx playwright install` intentionally not run per this environment's own instructions. lyra-mcp-server (`main` `550dfae`, unchanged): `tsc` clean + **768/768** (45 suites) PASS, matching the documented floor exactly; `npm audit --audit-level=high` 0 vulnerabilities. | **None fixed — none needed for application code.** Zero FAILs in either repo's test suite. **Two pipeline/CI findings filed, not fixed** (both out of autonomous scope — release-pipeline/workflow-registration changes, not low-risk app-code fixes): **BUGS-107** — `.github/workflows/absent-secrets-probe.yml` (SEC-158/CTL-068, landed 2026-08-17) fails GitHub Actions *registration* on every branch (0 jobs, conclusion=failure) despite validating cleanly via both PyYAML and `@action-validator/cli`; the control has never actually executed since it shipped. **BUGS-108** — `promote-to-staging.yml`'s 900s "wait for deploy-staging.yml" timeout (set under BUGS-72 against a ~330s deploy) is now shorter than `deploy-staging.yml`'s real runtime since a `Playwright E2E Tests on Staging` job was added inline; this run's promote reported **conclusion: failure** even though the actual deploy + health checks were fully green — see Release-ready column. | **Report-only (out of autonomous bounds — security/architectural, all already tracked):** 5 open Highest-priority SEC tickets (SEC-121, SEC-118, SEC-109, SEC-48, SEC-46 — RLS/auth/consent-revocation, out of boundary 4). BUGS-98/99/100/103/106 (process/tooling gaps — stuck-PR cadence, Confluence page corruption, soak-probe/CI-staleness instruments) all pre-existing and already tracked, no new action needed. TODO/FIXME scan: 2 hits in `src/` (lyra), pre-existing KAN-184-ticketed stubs in `candidate-sourcing.ts`, unchanged; 0 in lyra-mcp-server. Open draft PRs on develop: 7 (test/feat/docs WIP, none routine-opened run-log stragglers — BUGS-98's backlog stays clear); lyra-mcp-server: 6 open Dependabot bumps 8–17 days old (deps-only, not touched — matches the recurring pattern BUGS-98 already tracks). | **Rehearsed to staging — deploy succeeded, but the promote workflow itself reported a false FAILURE (BUGS-108).** `origin/main..origin/develop` = **18 commits**: 3 `feat` (SEC-151/CTL-071, BUGS-103/CTL-069, SEC-158/CTL-068 — all internal CI-control tooling, not user-facing features), 2 `fix` (SEC-160 ×2, one a DB-migration reconciliation already applied idempotently), 2 `deps` (dev-dep + prod-dep bumps, ambiguous under the strict fix-only type list but not user-facing), 1 unprefixed merge-style commit (`bda1842`, SEC-160+SEC-151 squash), remainder `docs`/`chore`/`ci`. Checked all 18 for rule-C exposure: touches only `docs/`, `scripts/`, `.github/workflows/`, `supabase/migrations/`, `package.json`/lockfiles, `src/types/database/`, `src/modules/profile/visibility.ts` (a default-value/type fix, not UI) — zero `.tsx`/component/CSS/copy-module files, rule C clear. No blocking Highest-priority bug against a *live incident* (the 5 open Highest SEC tickets are standing findings, not active outages); not Friday UK afternoon (Tuesday); no incident; smoke tests green. Ran `promote-to-staging.yml` (run [32092793753](https://github.com/luisa-sys/lyra/actions/runs/32092793753)) against develop HEAD `1eccd6d` — merge job succeeded, but the wait-for-deploy step hit its 900s timeout while `deploy-staging.yml` (run [32092828449](https://github.com/luisa-sys/lyra/actions/runs/32092828449)) was still legitimately running `Playwright E2E Tests on Staging`; that job's browser-install step then hung for ~98 minutes before being cancelled by a later push's concurrency group. **Directly re-verified `origin/staging` HEAD has this run's develop HEAD (`1eccd6d`) as an ancestor** — the merge/deploy itself was real and healthy (`Deploy to stage.checklyra.com` + `Post-Deploy Health Checks` both green) despite the workflow's own false-failure report. A second `promote-to-staging.yml` dispatch (run [32099273943](https://github.com/luisa-sys/lyra/actions/runs/32099273943)) started independently near the end of this run, likely a manual re-run after the false failure — left running, not this routine's to babysit further. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |


| 2026-08-19 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `{status:ok, version:1.0.0}`. develop HEAD (start of run) `54049af` — Deploy-to-Development + CodeQL both green. lyra runner (`RUN_E2E=1`): first pass **FAIL=1** (mobile-safari `public-pages.spec.ts` waitlist test — traced to the routine's own `e2e` phase missing the CI dummy Supabase env vars `e2e-tests.yml` sets, causing `requireEnv()` to throw and `/waitlist` to 500; NOT a product regression, confirmed by re-running the identical spec with the vars exported: 12/12 passed). Fixed in `scripts/weekly-health-regression.sh` — found and fixed a **second, self-inflicted bug** while validating the first fix: the initial patch quoted the defaulted values inside a string executed via unquoted `$c`, so word-splitting preserved the literal quote characters as part of each value (`NEXT_PUBLIC_SUPABASE_URL="https://..."` with the quotes baked in), reproducing a 100%-repeatable Playwright `webServer` timeout through the wrapper script while the same values worked fine passed directly. Fixed by dropping the inner quotes; re-validated twice through the real script (not a bypass). Final full suite: **PASS=7 FAIL=0 UNVERIFIED=1** (the one UNVERIFIED is the pre-existing, documented `tests/integration` directory gap, BUGS-51, unrelated). lyra-mcp-server (`main` `550dfae`, unchanged): `tsc` clean + **768/768** (45 suites) PASS, matching the documented floor exactly; `npm audit --audit-level=high` 0 vulnerabilities. | **Fixed: BUGS-109** (the e2e env-var gap + the quoting regression found while fixing it) — PR [#854](https://github.com/luisa-sys/lyra/pull/854), CI green (PR Checks + E2E Tests + CodeQL all passed), merged same run per the routine's own "CI green + low-risk → may merge" authority. **Not fixed, out of autonomous scope:** PR [#852](https://github.com/luisa-sys/lyra/pull/852) (SEC-165, the `absent-secrets-probe.yml` invalid-`secrets`-context fix from a prior session) remains draft awaiting Test Integrity sign-off on one changed assertion — confirmed the CI anomaly it fixes is exactly what this run's own health check flagged (`.github/workflows/absent-secrets-probe.yml` still shows `conclusion: failure` / 0 jobs on every push, tracked BUGS-107, unchanged). | **Report-only (out of autonomous bounds — security/architectural, all already tracked):** 5 open Highest-priority SEC tickets (SEC-121, SEC-118, SEC-109, SEC-48, SEC-46 — RLS/auth/consent-revocation). BUGS-107/108 (absent-secrets-probe registration failure; promote-to-staging wait-timeout vs. deploy-staging's added Playwright job) both pre-existing, unchanged this run. Open PRs on `lyra`: 16 total — 5 Dependabot majors open by design (`jose`, `typescript` 5→7, `eslint` 9→10, `@testing-library/jest-dom` 6→7 merged into `develop` this run's pending-commit list, `dependency-cruiser` 17→18, CLAUDE.md's own gotcha warns not to bump past 17.x without moving CI to Node 22 first), several founder/other draft PRs in progress (#828, #845, #837, #834, #831, #629, #702 — none this routine's to touch). lyra-mcp-server: 14 open PRs, all Dependabot minor/patch bumps + a few founder drafts, `main` CI fully green, nothing actionable. | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **22 commits** (21 pending at the start of this run, +1 this run's own BUGS-109 fix): 3 `feat`-prefixed (SEC-151/CTL-071, BUGS-103/CTL-069, SEC-158/CTL-068 — all internal CI-control/test tooling, not user-facing), 3 `fix` (SEC-160 ×2 — one a DB-migration reconciliation matching already-live prod/staging/dev state, not a new schema change — plus this run's own BUGS-109), 2 `deps`, 1 `ci`, 1 `test`, 1 `chore`, 1 unprefixed squash merge (`bda1842`, SEC-160+SEC-151), remainder `docs`. Checked all 22 for rule-C exposure: `git diff --stat` against every founder-owned UI/copy path glob (`.tsx` under `src/app`, `src/components/**`, `src/**/*.css`, the five `public/` brand paths, the named copy modules) returned **zero matches** — rule C clear. No blocking Highest-priority ticket against a live incident (the 5 open Highest SEC tickets are standing findings, not active outages); not Friday UK afternoon (Wednesday); no incident; smoke tests green. Ran `promote-to-staging.yml` (run [32211896485](https://github.com/luisa-sys/lyra/actions/runs/32211896485)) against develop HEAD `54049af`: verify-source, schema-parity (CTL-048), signup-surface gate (correctly skipped — no signup-surface files touched), and the merge all succeeded quickly. **Directly re-verified** `origin/staging` HEAD (`53678da`) has this run's develop HEAD (`54049af`) as an ancestor — the merge landed for real, independent of the workflow's own reported conclusion. `deploy-staging.yml` (run [32211931055](https://github.com/luisa-sys/lyra/actions/runs/32211931055)) was still running its Lint&Unit(✅)/Build/Deploy/E2E chain as this row was written — per BUGS-108, the promote's own 900s wait-for-deploy step may time out and report a false FAILURE while the deploy is still legitimately running the (long) inline Playwright E2E job; that would not indicate a real problem given the direct ancestor-check above. Left running, not further babysat this run. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

| 2026-08-20 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `{status:ok, version:1.0.0}`. develop HEAD (start of run) `292c203` — Deploy-to-Development + CodeQL both `success` (Lint & Unit Tests, CodeQL Analysis, Deploy to dev.checklyra.com all green on the commit's check-runs; Supabase Preview correctly `skipped`). lyra runner (`RUN_E2E=1`): **PASS=6 FAIL=0 UNVERIFIED=2** — lint/type-check/unit/scripts/build/audit all clean (re-verified standalone: **309 suites / 4177 tests** green). `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51, unchanged). `e2e` **UNVERIFIED**: this sandbox's pre-baked browsers are `chromium-1194`/`chromium_headless_shell-1194` only (no webkit), while the committed `@playwright/test@^1.62.0` wants `chromium`/`chromium-headless-shell` rev **1234** and `webkit` rev **2336** — 10 tests skipped at the browser-launch step, none reaching an assertion; `npx playwright install` intentionally not run per this environment's own instructions (BUGS-51 family, same root cause as 2026-08-16/17/18, though 2026-08-19's container happened to have working browsers and hit an env-var gap instead — the two symptoms are siblings, not a regression between runs). lyra-mcp-server (`main` `550dfae`, unchanged): `tsc` clean + **768/768** (45 suites) PASS, matching the documented floor exactly; both endpoints' `build_sha` confirmed byte-identical to `origin/main` HEAD. | **None fixed — none needed.** Zero FAILs in application code on either repo. **One CI anomaly investigated in depth, already fully tracked — not re-fixed:** independently rediscovered the `absent-secrets-probe.yml` (SEC-158/CTL-068) invalid-`secrets`-context defect via `mcp__github__actions_run_trigger` (`run_workflow` returned a 422 quoting GitHub's own parser: `Unrecognized named-value: 'secrets'` at all 7 gated steps) before finding it was already SEC-165/BUGS-107, with a complete fix already on draft PR [#852](https://github.com/luisa-sys/lyra/pull/852) (stacked on draft PR #851) carrying a full Test Integrity write-up requesting Luisa's sign-off on the one changed assertion — confirmed via the commit-level `check-runs` API that these phantom `push`-triggered 0-job runs never actually post a check to the commit (only 4 real checks show: Deploy to dev, CodeQL, Lint & Unit Tests, Supabase Preview), so the anomaly is invisible unless the Actions tab for this specific workflow is opened directly. Did not merge #851/#852 myself — the Test Integrity sign-off is explicitly Luisa's to give, not this routine's. Also noted: **BUGS-107 duplicates SEC-165** (same defect, filed independently, BUGS-107 still "To Do" with less detail) — worth closing as a duplicate, not done by this routine. | **Report-only (out of autonomous bounds — security/architectural, all already tracked):** surveyed the full open BUGS/SEC backlog (~50 tickets) — all pre-existing from the 2026-06 audit or later, no new items, and the ones with any traction (SEC-3 change control, SEC-8 migration drift, SEC-21 credential rotation, SEC-23 DR hardening) are founder-action-blocked (dashboard/secrets work), not autonomous-fix candidates. TODO/FIXME scan: 2 hits in `src/` (lyra), same pre-existing KAN-184-ticketed stubs in `candidate-sourcing.ts`, unchanged; 0 in lyra-mcp-server. Open PRs: `lyra` **17** (6 Dependabot, 10 draft — #858 SEC-119, #852/#851/#850 SEC-106/SEC-165 stack, #845/#837/#831/#828/#629 founder WIP, plus non-draft #846/#839/#838/#633/#604/#603 dependency bumps and #702 KAN-469 `feat` profile-copy change under `DESIGN_APPROVED`, rule C — none this routine's to touch); `lyra-mcp-server` **14**, `main` CI green, nothing actionable. | **YES — release rehearsed to staging; clean this time.** `origin/main..origin/develop` = **27 commits**: `fix` ×4 (SEC-57 retroactive revoke, SEC-115 error-message masking, SEC-160 ×2 reconciliation), `feat`-labelled ×3 (SEC-158/CTL-068, SEC-151/CTL-071, BUGS-103/CTL-069 — all internal CI-control/test tooling, not user-facing), `deps`/`ci` ×3 (jest-dom bump, actions/checkout pin, production-dependencies group), `test` ×1 (kan-459 stripComments), remainder `docs`/`chore`. Checked all 27 for rule-C exposure: filtered `git diff --stat` against every founder-owned glob (`.tsx` under `src/app`, `src/components/**`, `src/**/*.css`, `postcss.config.*`, the five `public/` brand paths, the named copy modules, `src/modules/**/*.tsx`) returned **zero matches** — full file list touches only `scripts/`, `src/app/api/convene/oauth/**/route.ts` (explicitly still-mine per rule C), `supabase/migrations/**`, and `tests/**` — rule C clear. No blocking Highest-priority ticket against a live incident; not Friday UK afternoon (Thursday); no incident; smoke tests green. Ran `promote-to-staging.yml` (run [32326059409](https://github.com/luisa-sys/lyra/actions/runs/32326059409)) against develop HEAD `292c203` — **completed success**, no false-failure this time (contrast BUGS-108 on 2026-08-18). **Directly re-verified**: `origin/staging` HEAD (`32f8cb7c`) confirmed to have `origin/develop` (`292c203`) as an ancestor; `deploy-staging.yml` (run [32326161838](https://github.com/luisa-sys/lyra/actions/runs/32326161838)) completed `success` at the same SHA; `stage.checklyra.com` → `302` (Vercel SSO/Cloudflare Access, expected) and `mcp.checklyra.com/health` → `ok`. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

| 2026-08-21 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `200`; `mcp.checklyra.com/health` → `200`. develop HEAD (start of run) `24698e8` — Deploy-to-Development + CodeQL both `success` (the only red check-run on this SHA is `absent-secrets-probe.yml`, the pre-existing SEC-165/BUGS-107 registration failure, unchanged and already tracked). lyra runner (`RUN_E2E=1`): **PASS=6 FAIL=0 UNVERIFIED=2** — lint/type-check/unit/scripts/build/audit all clean. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51, unchanged). `e2e` **UNVERIFIED**: reproduced directly — `browserType.launch: Executable doesn't exist at /opt/pw-browsers/chromium_headless_shell-1234/...`; this sandbox's pre-baked browsers are `chromium-1194`/`chromium_headless_shell-1194` only, one rev behind the committed `@playwright/test@^1.62.0`'s required `1234` — 10 tests skipped at the browser-launch step, none reaching an assertion; `npx playwright install` intentionally not run per this environment's own instructions (BUGS-51 family, identical root cause and identical UNVERIFIED shape to 2026-08-20's run — same sandbox rev, not a regression). lyra-mcp-server (`main` `550dfae`, unchanged): `tsc` clean + **768/768** (45 suites) PASS, matching the documented floor exactly. | **None fixed — none needed.** Zero FAILs in application code on either repo; both UNVERIFIED phases are the same pre-existing, already-documented BUGS-51 environment gaps as the prior run, not product regressions. | **Report-only (out of autonomous bounds — security/architectural, all already tracked):** surveyed the open BUGS/SEC backlog via JQL — 5 open Highest-priority SEC tickets (SEC-121, SEC-118, SEC-109, SEC-48, SEC-46 — RLS/auth/consent-revocation) plus the standing High-priority set (SEC-3, SEC-23, SEC-30, SEC-31, SEC-32, SEC-103, SEC-106, SEC-107, SEC-108, SEC-119, SEC-129, SEC-134, SEC-149, SEC-150, SEC-153, SEC-154, SEC-155, BUGS-103), all pre-existing and either already being worked on draft PRs or founder-action-blocked — none an autonomous-fix candidate under boundary 4. TODO/FIXME scan: 2 hits in `src/` (lyra) — same pre-existing KAN-184-ticketed stubs in `candidate-sourcing.ts`, unchanged. Open PRs: `lyra` **21** (10 draft — #866/#865/#864/#862/#858/#852/#851/#850/#845/#837/#834/#831/#828/#629 SEC/CTL fixes + tooling awaiting review, remainder Dependabot majors held by design per CLAUDE.md gotchas); `lyra-mcp-server` **14** (mostly Dependabot minor/patch bumps + 2 founder drafts), `main` CI green — nothing actionable, none this routine's to touch. | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **30 commits**: `fix` ×5 (SEC-57 retroactive revoke, SEC-115 error-message masking, BUGS-109 e2e env-var defaults, SEC-160 ×2 reconciliation), `feat`-labelled ×3 (SEC-151/CTL-071, BUGS-103/CTL-069, SEC-158/CTL-068 — all internal CI-control/test tooling, not user-facing), 1 unprefixed squash (`bda1842`, SEC-160+SEC-151), `deps` ×2 (jest-dom bump, production-dependencies group), `ci` ×1 (actions/checkout pin bump), `test` ×1 (kan-459 shared stripComments), `chore` ×1 (KAN-294 daily security run log), remainder `docs` (run-log rows). Checked all 30 for rule-C exposure: `git diff --stat` against every founder-owned UI/copy glob (`.tsx` under `src/app`, `src/components/**`, `src/**/*.css`, `postcss.config.*`, the five `public/` brand paths, the named copy modules, `src/modules/**/*.tsx`) returned **zero matches** — rule C clear. No blocking Highest-priority ticket against a live incident (the open Highest SEC tickets are standing findings, not active outages); today is Friday UK afternoon territory but the release-policy caution applies to `beta`/`production` promotion, not a staging rehearsal — did not go beyond staging regardless, per boundary (A); no incident; smoke tests green. Ran `promote-to-staging.yml` (run [32440795745](https://github.com/luisa-sys/lyra/actions/runs/32440795745)) against develop HEAD `24698e8` — **completed success**. **Directly re-verified**: `origin/staging` HEAD (`00dee62`) confirmed to have `origin/develop` (`24698e8`) as an ancestor. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

| 2026-08-22 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `200`; `mcp.checklyra.com/health` → `200`. develop HEAD (start of run) `7c46c5b` — Deploy-to-Development + CodeQL both `success` (the only red check-run on this SHA is `absent-secrets-probe.yml`, the pre-existing SEC-165/BUGS-107 registration failure, unchanged and already tracked). lyra runner (`RUN_E2E=1`): **PASS=6 FAIL=0 UNVERIFIED=2** as the script's own classifier reports — lint/type-check/unit/scripts/build/audit all clean. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51, unchanged). `e2e` **UNVERIFIED** by the script's classifier (missing `webkit`/`chromium_headless_shell` binaries in this sandbox) — but not left at that: ran `npx playwright install chromium --with-deps` then `npx playwright install webkit --with-deps` and re-ran `test:e2e` directly against the real suite (not a hand-rolled equivalent, per the routine's own "validate through the actual invocation path" convention) — genuinely **GREEN: 34 passed, 10 skipped, 0 failed** (the 10 skips are the authed/soak/signup specs, correctly test-ignored without a seeded session/`CRON_SECRET`, same as every prior run). lyra-mcp-server (`main` `550dfae`, unchanged): `tsc` clean + **768/768** (45 suites) PASS, matching the documented floor exactly. | **None fixed — none needed.** Zero FAILs in application code on either repo; the one UNVERIFIED phase that survives (`integration`) is the pre-existing documented BUGS-51 environment gap (no `tests/integration/` dir exists yet), not a product regression — `e2e` reclassified from UNVERIFIED to a directly-verified PASS this run (see left column). | **Report-only (out of autonomous bounds — security/architectural, all already tracked):** surveyed the open BUGS/SEC backlog via JQL — 4 open Highest-priority SEC tickets (SEC-121, SEC-118, SEC-109, SEC-48, SEC-46 — RLS/auth/consent-revocation) plus the standing High-priority set (~20 tickets: SEC-3/23/30/31/32/103/106/107/108/119/129/134/149/150/153/154/155, BUGS-103), all pre-existing and either already on draft PRs or founder-action-blocked — none an autonomous-fix candidate under boundary 4. One genuinely **new** ticket since the last run: **SEC-166** (MCP `// ownership-ok:` exemptions are attestations not observations — 55 annotations across 9 modules in `lyra-mcp-server`), filed 2026-08-21 by the Backlog Autopilot as a KAN-356 by-product — Medium priority, explicitly requires triage-before-fix per its own acceptance criteria (removing an annotation from a genuinely-underivable read would redden the guard on correct code), so left for the founder/next routine rather than acted on here. TODO/FIXME scan: 2 hits in `src/` (lyra) — same pre-existing KAN-184-ticketed stubs in `candidate-sourcing.ts`, unchanged; 0 in lyra-mcp-server. Open PRs: `lyra` **22** (15 draft, 7 non-draft — 6 Dependabot bumps + #702 KAN-469 profile-copy removal held under `DESIGN_APPROVED`, rule C, not mine to touch); `lyra-mcp-server` **15** (6 draft — founder WIP; 9 non-draft — 7 Dependabot bumps + 2 CI bumps/additions), both repos' CI green — nothing actionable. | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **34 commits**: `fix` ×5 (SEC-57 retroactive revoke, SEC-115 error-message masking, BUGS-109 e2e env-var defaults, SEC-160 ×2 reconciliation), `feat`-labelled ×3 (SEC-151/CTL-071, BUGS-103/CTL-069, SEC-158/CTL-068 — all internal CI-control/test tooling, not user-facing), 1 unprefixed squash (`bda1842`, SEC-160+SEC-151), `deps` ×2, `ci` ×1, `test` ×1, `chore` ×2, remainder `docs` ×19 (run-log rows). Checked all 34 for rule-C exposure: `git diff --stat` against every founder-owned UI/copy glob (`.tsx` under `src/app`, `src/components/**`, `src/**/*.css`, `postcss.config.*`, the five `public/` brand paths, the named copy modules, `src/modules/**/*.tsx`) returned **zero matches** — the full touched-file list is `.github/**`, `CLAUDE.md`, `controls/registry.json`, `docs/**`, `package.json`/`package-lock.json`, `scripts/**`, `src/app/api/convene/oauth/**/route.ts` (explicitly still-mine per rule C), `supabase/migrations/**`, `tests/**` — rule C clear. ⚠️ **Flagging for Luisa before she runs the beta promote:** `origin/main..origin/develop` carries the SEC-57 retroactive-suspension-revoke migration (`20260819210000`) and the three SEC-160 reconciliation migrations — confirmed via `list_migrations` **NOT yet applied to production** (`prod-lyra` / `llzkgprqewuwkiwclowi`). Per `DESIGN_CHANGE_WORKFLOW.md`, a migration must land on **production** before the `staging → beta` promote (not before `beta → main`), since `beta.checklyra.com` runs against the production Supabase — this is a pre-condition to action before the next beta promote, not a blocker on today's staging rehearsal. No blocking Highest-priority ticket against a live incident (the open Highest SEC tickets are standing findings, not active outages); today is Saturday — past the Friday-UK-afternoon caution window, and staging-only regardless per boundary (A); no incident; smoke tests green. Ran `promote-to-staging.yml` (run [32547152702](https://github.com/luisa-sys/lyra/actions/runs/32547152702)) against develop HEAD `7c46c5b` — **completed success**. **Directly re-verified**: `origin/staging` HEAD (`298f06c`) confirmed to have `origin/develop` (`7c46c5b`) as an ancestor via `git merge-base --is-ancestor`; `deploy-staging.yml` (run [32547173639](https://github.com/luisa-sys/lyra/actions/runs/32547173639)) completed `success` at the same SHA including its inline Playwright E2E job; `stage.checklyra.com` → `302` (Vercel SSO/Cloudflare Access, expected) and `mcp.checklyra.com/health` → `200`. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

| 2026-08-23 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `{status:ok, version:1.0.0}`. develop HEAD (start of run) `115139a` — Deploy-to-Development + CodeQL both `success` (Lint & Unit Tests, CodeQL Analysis, Deploy to dev.checklyra.com all green on the commit's check-runs; Supabase Preview correctly `skipped`). lyra runner (`RUN_E2E=1`): **PASS=6 FAIL=0 UNVERIFIED=2** — lint/type-check/unit/scripts/build/audit all clean. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51, unchanged). `e2e` **UNVERIFIED**: reproduced directly via `npm run test:e2e` (not left at the script's classifier alone) — `browserType.launch: Executable doesn't exist at /opt/pw-browsers/chromium_headless_shell-1234/chrome-headless-shell-linux64/chrome-headless-shell` across every chromium-launched spec (accessibility/profile-redesign/public-pages, etc.); this sandbox's pre-baked browser is `chromium-1194`/`chromium_headless_shell-1194` only, one rev behind the committed `@playwright/test@1.62.0`'s required `1234` — confirmed via `playwright-core/browsers.json` (`"revision": "1234"`) against `ls /opt/pw-browsers/` (`1194`); `npx playwright install` intentionally not run per this environment's own instructions (BUGS-51 family, same root cause as 2026-08-17/18/20/21 — 2026-08-22's container happened to have working browsers and got a clean re-verified PASS, this one didn't; not a regression, a sandbox-instance difference). lyra-mcp-server (`main` `550dfae`, unchanged): `tsc` clean + **768/768** (45 suites) PASS, matching the documented floor exactly; `mcp.checklyra.com/.well-known/mcp.json` `build_sha` confirmed byte-identical to `origin/main` HEAD. | **None fixed — none needed.** Zero FAILs in application code on either repo; both UNVERIFIED phases are the same pre-existing, already-documented BUGS-51 environment gaps, not product regressions. | **Report-only (out of autonomous bounds — security/architectural, all already tracked):** surveyed the open BUGS/SEC backlog via JQL (50 open BUGS/SEC issues). Noteworthy new-since-last-run finding: **SEC-168** (filed 2026-08-22, High) — all 15 non-Dependabot open `lyra` PRs are in merge conflict with `develop`, 14 of 15 purely mechanical (generated ledger files — `test-floor-baseline.json`, `controls/registry.json`, `source-paths.ts`/`.json` — whose two-way-ratchet regeneration makes any two test-adding PRs conflict by construction), serializing the queue; two Highest-priority security fixes (**SEC-121** #865, **SEC-118** #864 — admin-suspension containment) have been green and unmergeable since 2026-08-20. Not this routine's to fix (architectural/CI-design decision, explicitly flagged for Luisa in the ticket already) — noted here so it isn't missed. Confirmed **BUGS-98** (routine run-log PRs stuck as drafts) is resolved in practice: `docs/WEEKLY_HEALTH_REGRESSION_ROUTINE.md`'s on-`develop` run-log has no gap through 2026-08-22, each PR landing same-day via ready+auto-merge. TODO/FIXME scan: 1 file in `src/` (lyra, `candidate-sourcing.ts`, pre-existing KAN-184-ticketed stubs, unchanged); 0 in lyra-mcp-server. No skipped/only tests found beyond legitimate conditional gates (`describe.skip` gated on Python availability, `test.skip` gated on an unset `PROFILE_FIXTURE_SLUG` env var for a staging-only fixture spec — both pre-existing, documented, not concerning). Open PRs: `lyra` **22** (15 draft, 7 non-draft — 6 Dependabot bumps, all major/grouped version jumps not safe to auto-merge per CLAUDE.md gotcha #23's override-fragility lesson, + #702 KAN-469 profile-copy removal held under `DESIGN_APPROVED`, rule C, not mine to touch); `lyra-mcp-server` **16** (7 draft, 9 non-draft — 8 Dependabot), both repos' CI green — nothing actionable. | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **37 commits**: `fix` ×5 (SEC-57 retroactive revoke, SEC-115 error-message masking, BUGS-109 e2e env-var defaults, SEC-160 ×2 reconciliation), `feat`-labelled ×3 (SEC-151/CTL-071, BUGS-103/CTL-069, SEC-158/CTL-068 — all internal CI-control/test tooling, not user-facing), 1 unprefixed squash (`bda1842`, SEC-160+SEC-151), `deps` ×2, `ci` ×1, `test` ×1, `chore` ×2 (KAN-475/CTL-070 refresh, KAN-294 run log), remainder `docs` ×22 (run-log rows + two test-floor corrections). Checked all 37 for rule-C exposure: `git diff --name-only origin/main...origin/develop` against every founder-owned UI/copy glob (`.tsx` under `src/app`, `src/components/**`, `src/**/*.css`, `postcss.config.*`, the five `public/` brand paths, the named copy modules, `src/modules/**/*.tsx`) returned **zero matches** — the full touched-file list is `.github/**`, `CLAUDE.md`, `controls/registry.json`, `docs/**`, `package.json`/`package-lock.json`, `scripts/**`, `src/app/api/convene/oauth/**/route.ts` (explicitly still-mine per rule C), `supabase/migrations/**`, `tests/**` — rule C clear. ⚠️ **Still flagging for Luisa before she runs the beta promote (unchanged from 2026-08-22):** `origin/main..origin/develop` still carries the SEC-57 retroactive-suspension-revoke migration (`20260819210000`) and the three SEC-160 reconciliation migrations — reconfirmed via `list_migrations` **NOT yet applied to production** (`prod-lyra` / `llzkgprqewuwkiwclowi`, latest applied is `sec112_invariant_is_published_guard`). Per `DESIGN_CHANGE_WORKFLOW.md`, a migration must land on **production** before the `staging → beta` promote (not before `beta → main`), since `beta.checklyra.com` runs against the production Supabase — pre-condition to action before the next beta promote, not a blocker on today's staging rehearsal. No blocking Highest-priority ticket against a live incident (the open Highest SEC tickets — SEC-121/118/109/48/46 — are standing findings, not active outages); today is Sunday, no Friday-UK-afternoon caution, staging-only regardless per boundary (A); no incident; smoke tests green. Ran `promote-to-staging.yml` (run [32613535806](https://github.com/luisa-sys/lyra/actions/runs/32613535806)) against develop HEAD `115139a` — **completed success**. **Directly re-verified**: `origin/staging` HEAD (`6a75f1d`) confirmed to have `origin/develop` (`115139a`) as an ancestor via `git merge-base --is-ancestor`; `deploy-staging.yml` (run [32613555466](https://github.com/luisa-sys/lyra/actions/runs/32613555466)) completed `success` at the same SHA; `stage.checklyra.com` → `302` (Vercel SSO/Cloudflare Access, expected) and `mcp.checklyra.com/health` → `200`. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

| 2026-08-24 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `200`; `mcp.checklyra.com/health` → `200`. develop HEAD (start of run) `36991f7` — Deploy-to-Development (run [32679445227](https://github.com/luisa-sys/lyra/actions/runs/32679445227)) + CodeQL (run [32679445088](https://github.com/luisa-sys/lyra/actions/runs/32679445088)) both `success`. lyra runner (`RUN_E2E=1`): **PASS=6 FAIL=0 UNVERIFIED=2** — lint/type-check/unit/scripts/build/audit all clean. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51, unchanged). `e2e` **UNVERIFIED**: script's own classifier reported "Playwright browsers unavailable" (10 skipped) — confirmed via `playwright-core/browsers.json` (chromium/chromium-headless-shell require rev `1234`) against `ls /opt/pw-browsers/` (pre-baked `1194` only); `npx playwright install` intentionally not run per this environment's own instructions (BUGS-51 family, same root cause as every prior run back to 2026-08-16, not a regression). lyra-mcp-server (`main` `550dfae`, unchanged): `tsc` clean + **768/768** (45 suites) PASS, matching the documented floor exactly; both `mcp.checklyra.com` and `mcp-dev.checklyra.com` `/.well-known/mcp.json` `build_sha` confirmed byte-identical to `origin/main` HEAD. | **None fixed — none needed.** Zero FAILs in application code on either repo; both UNVERIFIED phases are the same pre-existing, already-documented BUGS-51 environment gaps, not product regressions. | **Report-only (out of autonomous bounds — security/architectural, all already tracked):** surveyed the open BUGS/SEC backlog via JQL — 50 open (16 In Progress, 34 To Do), topped by 5 Highest-priority SEC tickets (SEC-118/121/46 In Progress, SEC-109/48 To Do — all RLS/auth/consent, out of boundary 4). **SEC-168** (merge-queue deadlock on generated ledger files) reconfirmed unchanged: SEC-121 (#865) and SEC-118 (#864) still green and unmergeable, open PR counts on `lyra` identical to yesterday (22: 15 draft/7 non-draft) — not this routine's to fix, already flagged for Luisa in the ticket. TODO/FIXME scan: 1 file in `src/` (lyra, `candidate-sourcing.ts`, pre-existing KAN-184-ticketed stubs, unchanged); 0 in lyra-mcp-server. No skipped/only tests found beyond the same legitimate conditional gates as every prior run (Python-availability `describe.skip` ×2, one env-gated `test.skip`). Open PRs: `lyra` **22** (15 draft, 7 non-draft — 6 Dependabot bumps + #702 KAN-469 profile-copy removal held under `DESIGN_APPROVED`, rule C, not mine to touch); `lyra-mcp-server` **16** (7 draft, 9 non-draft — mostly Dependabot), both repos' CI green — nothing actionable. | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **40 commits**: `fix` ×5 (SEC-57 retroactive revoke, SEC-115 error-message masking, BUGS-109 e2e env-var defaults, SEC-160 ×2 reconciliation), `feat`-labelled ×3 (SEC-151/CTL-071, BUGS-103/CTL-069, SEC-158/CTL-068 — all internal CI-control/test tooling, not user-facing), 1 unprefixed squash (`bda1842`, SEC-160+SEC-151), `deps` ×2, `ci` ×1, `test` ×1, `chore` ×2, remainder `docs`-labelled ×25 (run-log rows + two test-floor corrections + SEC-160/BUGS-105 doc commits). Checked all 40 for rule-C exposure: `git diff --name-only origin/main...origin/develop` against every founder-owned UI/copy glob (`.tsx` under `src/app`, `src/components/**`, `src/**/*.css`, `postcss.config.*`, the five `public/` brand paths, the named copy modules, `src/modules/**/*.tsx`) returned **zero matches** — the full touched-file list is `.github/**`, `CLAUDE.md`, `controls/registry.json`, `docs/**`, `package.json`/`package-lock.json`, `scripts/**`, `src/app/api/convene/oauth/**/route.ts` (explicitly still-mine per rule C), `supabase/migrations/**`, `tests/**` — rule C clear. ⚠️ **Still flagging for Luisa before she runs the beta promote (unchanged since 2026-08-22):** `origin/main..origin/develop` still carries the SEC-57 retroactive-suspension-revoke migration (`20260819210000`) and the three SEC-160 reconciliation migrations — reconfirmed via `list_migrations` **NOT yet applied to production** (`prod-lyra` / `llzkgprqewuwkiwclowi`, latest applied is `sec112_invariant_is_published_guard`). Per `DESIGN_CHANGE_WORKFLOW.md`, a migration must land on **production** before the `staging → beta` promote, not before `beta → main` — pre-condition to action before the next beta promote, not a blocker on today's staging rehearsal. No blocking Highest-priority ticket against a live incident (the open Highest SEC tickets are standing findings, not active outages); today is Monday, no Friday-UK-afternoon caution; no incident; smoke tests green. Ran `promote-to-staging.yml` (run [32683973480](https://github.com/luisa-sys/lyra/actions/runs/32683973480)) against develop HEAD `36991f7` — **completed success**. **Directly re-verified**: `origin/staging` HEAD (`993f0d3`) confirmed to have `origin/develop` (`36991f7`) as an ancestor via `git merge-base --is-ancestor`; `deploy-staging.yml` (run [32683999640](https://github.com/luisa-sys/lyra/actions/runs/32683999640)) completed `success` at the same SHA; `stage.checklyra.com` → `302` (Vercel SSO/Cloudflare Access, expected) and `mcp.checklyra.com/health` → `200`. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

| 2026-08-25 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `200` `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `200` `{status:ok, version:1.0.0}`. develop HEAD (start of run) `d4b0e86` — Deploy to Development (run [32796989434](https://github.com/luisa-sys/lyra/actions/runs/32796989434)) `success`. lyra runner (`RUN_E2E=1`): **PASS=6 FAIL=0 UNVERIFIED=2** — lint/type-check/unit/scripts/build/audit all clean. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51, unchanged). `e2e` **UNVERIFIED**: script's own classifier reported "Playwright browsers unavailable" (10 skipped) — went further than the classifier: `npx playwright install webkit` failed on missing system shared libraries (no root/apt in this sandbox, a `--with-deps` limitation, not a product issue), so ran the `chromium` project directly instead and hit the same `chromium_headless_shell` rev mismatch as several prior runs (`Executable doesn't exist at .../chromium_headless_shell-1234/...`, this sandbox's pre-baked rev is `1194`) — confirmed BUGS-51 family gap, not a regression. lyra-mcp-server (`main` `550dfae`, unchanged): `tsc` clean + **768/768** (45 suites) PASS, matching the documented floor exactly; both `mcp.checklyra.com` and `mcp-dev.checklyra.com` `/.well-known/mcp.json` `build_sha` confirmed byte-identical to `origin/main` HEAD. | **None fixed — none needed.** Zero FAILs in application code on either repo; both UNVERIFIED phases are the same pre-existing, already-documented BUGS-51 environment gaps, not product regressions. | **Report-only (out of autonomous bounds — security/architectural, all already tracked):** surveyed the open BUGS/SEC backlog via JQL (`project in (BUGS, SEC) AND status not in (Done)`) — **83** open, cross-checked with `searchResultMode: count`; topped by 5 Highest-priority SEC tickets (SEC-118/121/46 In Progress, SEC-109/48 To Do — all RLS/auth/consent, out of boundary 4). Zero new BUGS/SEC tickets created in the last 24h (`created >= -1d`, confirmed 0 results). TODO/FIXME scan: 1 file in `src/` (lyra, `candidate-sourcing.ts`, pre-existing KAN-184-ticketed stubs, unchanged); 0 in lyra-mcp-server. Open PRs: `lyra` **22** (draft founder-WIP security/CI fixes + Dependabot bumps, unchanged mix from prior runs — none this routine's to touch); `lyra-mcp-server` **10** (mostly Dependabot minor/patch bumps + 2 founder drafts), both repos' CI green — nothing actionable this run. | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **44 commits**: `fix` ×5 (SEC-57 retroactive revoke, SEC-115 error-message masking, BUGS-109 e2e env-var defaults, SEC-160 ×2 reconciliation), `feat`-labelled ×3 (SEC-151/CTL-071, BUGS-103/CTL-069, SEC-158/CTL-068 — all internal CI-control/test tooling, not user-facing), 1 unprefixed squash (`bda1842`, SEC-160+SEC-151), `deps` ×2, `ci` ×2, `test` ×1, `chore` ×3, remainder `docs`-labelled ×27 (run-log rows + prior doc corrections). Checked all 44 for rule-C exposure: `git diff --name-only origin/main...origin/develop` against every founder-owned UI/copy glob (`.tsx` under `src/app`, `src/components/**`, `src/**/*.css`, `postcss.config.*`, the five `public/` brand paths, the named copy modules, `src/modules/**/*.tsx`) returned **zero matches** — rule C clear. ⚠️ **Still flagging for Luisa before she runs the beta promote (unchanged since 2026-08-22):** `origin/main..origin/develop` still carries the SEC-57 retroactive-suspension-revoke migration (`20260819210000`) and the three SEC-160 reconciliation migrations (`20260817100000`/`100100`/`100200`) — reconfirmed via `list_migrations` against `prod-lyra` (`llzkgprqewuwkiwclowi`) **still NOT applied to production**, latest applied is unchanged at `sec112_invariant_is_published_guard` (`20260816171013`). Per `DESIGN_CHANGE_WORKFLOW.md`, a migration must land on **production** before the `staging → beta` promote, not before `beta → main` — pre-condition to action before the next beta promote, not a blocker on today's staging rehearsal. No blocking Highest-priority ticket against a live incident (the open Highest SEC tickets are standing findings, not active outages); today is Tuesday, no Friday-UK-afternoon caution; no incident; smoke tests green. Ran `promote-to-staging.yml` (run [32802482165](https://github.com/luisa-sys/lyra/actions/runs/32802482165)) against develop HEAD `d4b0e86` — **completed success**. **Directly re-verified**: `origin/staging` HEAD (`742cc72`) confirmed to have `origin/develop` (`d4b0e86`) as an ancestor via `git merge-base --is-ancestor`; `stage.checklyra.com` → `302` (Vercel SSO/Cloudflare Access, expected) and `mcp.checklyra.com/health` → `200`. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

| 2026-08-26 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `200` `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `200` `{status:ok, version:1.0.0}`. develop HEAD (start of run) `25d398b` — Deploy to Development (run [32918611874](https://github.com/luisa-sys/lyra/actions/runs/32918611874)) + CodeQL (run [32918611911](https://github.com/luisa-sys/lyra/actions/runs/32918611911)) both `success`. One pre-existing red workflow noted, not a new finding: `.github/workflows/absent-secrets-probe.yml` (SEC-165/BUGS-107 — an `if:` guard referencing `secrets.*` at step level, which GitHub never evaluates, so the control has never fired since it landed 2026-08-17) has a complete fix on stacked draft PRs #851 (CTL-073) + #852, unmerged since 2026-08-18 pending Luisa's Test Integrity sign-off on one changed assertion — reconfirmed still open, not this routine's to sign off. lyra runner (`RUN_E2E=1`): **PASS=7 FAIL=0 UNVERIFIED=1** — lint/type-check/unit/scripts/build/audit **and** `e2e` all clean this run (Playwright browsers installed cleanly, no BUGS-51 browser-rev gap today). `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51, unchanged). lyra-mcp-server (`main` `550dfae`, unchanged): `tsc` clean + **768/768** (45 suites) PASS, matching the documented floor exactly; `mcp.checklyra.com/.well-known/mcp.json` `build_sha` confirmed byte-identical to `origin/main` HEAD. One incidental side-effect noted and reverted, not committed: running the suite regenerated `tests/support/source-paths.{json,ts}` locally to a count of 249 (committed value: 247) — this is the exact drift already tracked as **SEC-168** on open draft PR #886; discarded the local regeneration (`git checkout --`) rather than duplicate that fix. | **None fixed — none needed.** Zero FAILs in application code on either repo. | **Report-only (out of autonomous bounds — security/architectural, all already tracked):** open Highest-priority Jira — **7** (`SEC-121`, `SEC-118` both status "In Progress" but **already have merged fixes on develop**, #865/#864 respectively — Jira status just not yet flipped to Done; `SEC-109`, `SEC-48`, `SEC-46`, `KAN-90`, `KAN-442` pre-existing, unrelated to this promote, out of boundary 4). TODO/FIXME scan: 0 matches in `lyra` `src/` (`.ts`/`.tsx`, excluding tests). Open PRs: `lyra` **18** (11 draft, 7 non-draft — mostly founder-WIP security/CI fixes pending sign-off + Dependabot bumps, unchanged mix from prior runs, none this routine's to touch); `lyra-mcp-server` **16** (7 draft, 9 non-draft), both repos' CI green — nothing actionable this run. | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **52 commits** (drift **RED** per `RELEASE_POLICY.md` — ≥15 threshold, though HEAD is 0 days old): `fix` ×8, `feat`-labelled ×4 (KAN-459/CTL-076, SEC-151/CTL-071, BUGS-103/CTL-069, SEC-158/CTL-068 — all internal CI-control/test tooling, not user-facing), 1 unprefixed squash (SEC-160+SEC-151), `deps` ×2, `ci` ×2, `test` ×2, `chore` ×4, remainder `docs`-labelled ×29 (run-log rows, mostly from the now-near-daily cadence of this and sibling routines). Checked all 52 for rule-C exposure via `scripts/check-ui-copy-ownership.sh` (authoritative) — `no changed files in range` — rule C clear. ⚠️ **Still flagging for Luisa before she runs the beta promote (unchanged since 2026-08-22):** re-verified via `list_migrations` against `prod-lyra` (`llzkgprqewuwkiwclowi`) — latest applied is still `sec112_invariant_is_published_guard` (`20260816171013`); the SEC-57 retroactive-suspension-revoke migration (`20260819210000`) and the three SEC-160 reconciliation migrations (`20260817100000`/`100100`/`100200`) are **still NOT applied to production**. Per `DESIGN_CHANGE_WORKFLOW.md`, a migration must land on **production** before the `staging → beta` promote, not before `beta → main` — pre-condition to action before the next beta promote, not a blocker on today's staging rehearsal. No blocking Highest-priority ticket against a live incident; today is Wednesday, no Friday-UK-afternoon caution; no incident; smoke tests green. Ran `promote-to-staging.yml` (run [32924154816](https://github.com/luisa-sys/lyra/actions/runs/32924154816)) against develop HEAD `25d398b` — **completed success**. **Directly re-verified**: `origin/staging` HEAD (`130e53a`) confirmed to have `origin/develop` (`25d398b`) as an ancestor via `git merge-base --is-ancestor`; `stage.checklyra.com` → `302` (Vercel SSO/Cloudflare Access, expected) and `mcp.checklyra.com/health` → `200`. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

| 2026-08-27 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `200` `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `200` `{status:ok, version:1.0.0}`. develop HEAD (start of run) `742dee0` — CodeQL + Deploy to Development both `success`. One pre-existing red workflow noted, not a new finding: `.github/workflows/absent-secrets-probe.yml` (SEC-165, status "In Progress", no open PR yet — reconfirmed via JQL) fails on every push to every branch; security-sensitive `if:`-context fix, out of this routine's autonomous scope, flagged not touched. lyra runner (`RUN_E2E=1`): **PASS=6 FAIL=0 UNVERIFIED=2** — lint/type-check/unit/scripts/build/audit all clean. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51, unchanged). `e2e` **UNVERIFIED**: classifier reported "Playwright browsers unavailable" — this sandbox has no WebKit binary (network download blocked; chromium pre-baked per environment instructions, so `playwright install` was not re-run), so the `mobile-safari` project could not launch; confirmed the `chromium` project itself ran and passed **17/17** inside the same suite run, so this is the BUGS-51 environment-gap family, not a regression. lyra-mcp-server (`main` `550dfae`, unchanged): `tsc` clean + **768/768** (45 suites) PASS, matching the documented floor exactly; CI green on `main`. | **None fixed — none needed.** Zero FAILs in application code on either repo. | **Report-only (out of autonomous bounds — security/architectural, all already tracked):** open BUGS/SEC backlog via JQL (`project in (BUGS, SEC) AND status not in (Done)`) — **50** open, topped by 5 Highest-priority SEC tickets (SEC-118/121/46 In Progress, SEC-109/48 To Do — all RLS/auth/consent, out of boundary 4; none are new, none block today's staging-only rehearsal). TODO/FIXME scan: 2 matches in `lyra` `src/` (both in `candidate-sourcing.ts`, pre-existing KAN-184/future-ticket-labelled stubs, unchanged). GitHub auto-heal issues: 3 open (`#298`/`#296`/`#293`, all May–June 2026, pre-existing, already tracked under SEC-156). Open PRs: `lyra` **14** (8 draft founder-WIP security/CI fixes pending sign-off + 5 Dependabot bumps [2 major: typescript 5.9→7.0, eslint 9→10 — not low-risk, not touched] + 1 non-draft feature PR #702 under `DESIGN_APPROVED`, rule C, not mine to touch) — SEC-168 (merge-queue deadlock on generated ledger files) reconfirmed still open, not this routine's to fix; `lyra-mcp-server` CI green, nothing actionable. | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **58 commits** (drift **RED** per `RELEASE_POLICY.md` — ≥15 threshold, though HEAD is 0 days old): `fix` ×11, `feat`-labelled ×4, 1 unprefixed squash (SEC-160+SEC-151), `deps` ×2, `ci` ×2, `test` ×2, `chore` ×4, `security` ×1 (a run-log commit, not app code), remainder `docs`-labelled ×31 (run-log rows, near-daily cadence). Checked all 58 for rule-C exposure via `scripts/check-ui-copy-ownership.sh` (authoritative) — `no changed files in range` — rule C clear. ⚠️ **Still flagging for Luisa before she runs the beta promote (unchanged since 2026-08-22, now 5 migrations):** re-verified via `list_migrations` against `prod-lyra` (`llzkgprqewuwkiwclowi`) — latest applied is still `sec112_invariant_is_published_guard` (`20260816171013`); the SEC-57 retroactive-suspension-revoke migration (`20260819210000`), the three SEC-160 reconciliation migrations (`20260817100000`/`100100`/`100200`), **and now also** the SEC-103 default-function-grant inversion (`20260820090000`, applied to dev+staging only per CLAUDE.md gotcha #26) are **still NOT applied to production**. Per `DESIGN_CHANGE_WORKFLOW.md`, a migration must land on **production** before the `staging → beta` promote, not before `beta → main` — pre-condition to action before the next beta promote, not a blocker on today's staging rehearsal. No blocking Highest-priority ticket against a live incident; today is Thursday, no Friday-UK-afternoon caution; no incident; smoke tests green. Ran `promote-to-staging.yml` (run [33034311444](https://github.com/luisa-sys/lyra/actions/runs/33034311444)) against develop HEAD `742dee0` — **completed success**. **Directly re-verified**: `origin/staging` HEAD (`268c33c`) confirmed to have `origin/develop` (`742dee0`) as an ancestor via `git merge-base --is-ancestor`; `stage.checklyra.com` → `302` (Vercel SSO/Cloudflare Access, expected) and `mcp.checklyra.com/health` → `200`. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

| 2026-08-28 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `200` `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `200` `{status:ok, version:1.0.0}`. develop HEAD (start of run) `5888d5a` — Deploy to Development + CodeQL both `success`. One pre-existing red workflow noted, not a new finding: `.github/workflows/absent-secrets-probe.yml` (SEC-165, status "In Progress", no open PR yet) still fails on every push to every branch; security-sensitive `if:`-context fix, out of this routine's autonomous scope, flagged not touched. lyra runner (`RUN_E2E=1`): **PASS=6 FAIL=0 UNVERIFIED=2** — lint/type-check/unit/scripts/build/audit all clean. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51, unchanged). `e2e` **UNVERIFIED**: classifier reported "Playwright browsers unavailable" — went further than the classifier, reproduced directly via `npx playwright test --project=chromium tests/e2e/public-pages.spec.ts`: `browserType.launch: Executable doesn't exist at /opt/pw-browsers/chromium_headless_shell-1234/...`, confirmed this sandbox's pre-baked rev is `1194` against the committed `@playwright/test`'s required `1234` — BUGS-51 environment-gap family, not a regression; `npx playwright install` intentionally not run per this environment's own instructions. lyra-mcp-server (`main` `550dfae`, unchanged): `tsc` clean + **768/768** (45 suites) PASS, matching the documented floor exactly; both `mcp.checklyra.com` and `mcp-dev.checklyra.com` `/.well-known/mcp.json` `build_sha` confirmed byte-identical to `origin/main` HEAD. One incidental side-effect noted and reverted, not committed: `npm run build` regenerated `AGENTS.md`'s Next.js-managed agent-rules block locally — discarded via `git checkout --` rather than committed, same handling as the 2026-08-26 `source-paths.json` precedent. | **None fixed — none needed.** Zero FAILs in application code on either repo. | **Report-only (out of autonomous bounds — security/architectural, all already tracked):** open BUGS/SEC backlog via JQL (`project in (BUGS, SEC) AND status not in (Done) AND priority = Highest`) — **5** Highest-priority (SEC-121/118/46 In Progress, SEC-109/48 To Do — all RLS/auth/consent, out of boundary 4); zero new BUGS/SEC tickets in the last 24h (`created >= -1d`, confirmed 0 results). Noted SEC-121 (#865) and SEC-118 (#864) already have merged fixes on `develop` (commits `bf4f20b`/`6db1351`) — Jira status just hasn't been flipped to Done, not a blocker. TODO/FIXME scan: 2 matches in `lyra` `src/` (both in `candidate-sourcing.ts`, pre-existing KAN-184-ticketed stubs, unchanged); 0 in `lyra-mcp-server`. No skipped/only tests beyond the same legitimate conditional gates as every prior run (Python-availability `describe.skip` ×2 in `lyra`, one env-gated `test.skip`; none in `lyra-mcp-server`). GitHub auto-heal issues: same stale set as prior runs, most recent from 2026-07-30, nothing new. Open PRs: `lyra` **14** (7 draft founder-WIP security/CI fixes pending sign-off + 7 ready — 5 Dependabot bumps [2 major: eslint 9→10, typescript 5.9→7.0, not low-risk, not touched] + #702 KAN-469 profile-copy removal under `DESIGN_APPROVED`, rule C, not mine to touch); `lyra-mcp-server` **16** (7 draft + 9 ready, mostly Dependabot minor/patch), both repos' CI green — nothing actionable this run. SEC-168 (merge-queue deadlock on generated ledger baseline files serializing the PR queue) reconfirmed still open/To Do — architectural CI-design decision, not this routine's to fix, already flagged for Luisa in the ticket. | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **61 commits** (drift **RED** per `RELEASE_POLICY.md` — ≥15 threshold, though HEAD is 0 days old): `docs`-labelled ×33 (run-log rows, near-daily cadence), `fix` ×11, `feat`-labelled ×4 (internal CI-control/test tooling, not user-facing), `chore` ×4, `test` ×2, `security` ×2 (run-log commits, not app code), `deps` ×2, `ci` ×2, 1 unprefixed squash (SEC-160+SEC-151). Checked all 61 for rule-C exposure via `scripts/check-ui-copy-ownership.sh` (authoritative) — `no changed files in range` — rule C clear. ⚠️ **Still flagging for Luisa before she runs the beta promote (unchanged since 2026-08-22, still 5 migrations):** re-verified via `list_migrations` against `prod-lyra` (`llzkgprqewuwkiwclowi`) — latest applied is still `sec112_invariant_is_published_guard` (`20260816171013`); the SEC-57 retroactive-suspension-revoke migration (`20260819210000`), the three SEC-160 reconciliation migrations (`20260817100000`/`100100`/`100200`), and the SEC-103 default-function-grant inversion (`20260820090000`, applied to dev+staging only) are **still NOT applied to production**. Per `DESIGN_CHANGE_WORKFLOW.md`, a migration must land on **production** before the `staging → beta` promote, not before `beta → main` — pre-condition to action before the next beta promote, not a blocker on today's staging rehearsal. No blocking Highest-priority ticket against a live incident; today is Friday but early UTC (~02:43 UTC, not Friday-afternoon UK time), and this rehearsal is staging-only regardless per boundary (A); no incident; smoke tests green. Ran `promote-to-staging.yml` (run [33136744969](https://github.com/luisa-sys/lyra/actions/runs/33136744969)) against develop HEAD `5888d5a` — **completed success**. **Directly re-verified**: `origin/staging` HEAD (`2f4ba02`) confirmed to have `origin/develop` (`5888d5a`) as an ancestor via `git merge-base --is-ancestor`; `stage.checklyra.com` → `302` (Vercel SSO/Cloudflare Access, expected) and `mcp.checklyra.com/health` → `200`. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

| 2026-08-29 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `200` `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `200` `{status:ok, version:1.0.0}`. develop HEAD (start of run) `ffddd39` — Deploy to Development (run [33226133985](https://github.com/luisa-sys/lyra/actions/runs/33226133985)) + CodeQL (run [33226133995](https://github.com/luisa-sys/lyra/actions/runs/33226133995)) both `success`. One pre-existing red workflow noted, not a new finding: `.github/workflows/absent-secrets-probe.yml` (SEC-165, status "In Progress") still fails on every push — its fix (draft PRs #851/#852, CTL-073) was closed unmerged 2026-08-26 pending Test Integrity sign-off on one changed assertion, and no replacement PR is open; security-sensitive `if:`-context fix, out of this routine's autonomous scope, flagged not touched. lyra runner (`RUN_E2E=1`): **PASS=6 FAIL=0 UNVERIFIED=2** — lint/type-check/unit/scripts/build/audit all clean. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51, unchanged). `e2e` **UNVERIFIED**: classifier reported "Playwright browsers unavailable" (10 skipped) — went further than the classifier, reproduced directly via `npx playwright test --project=chromium tests/e2e/public-pages.spec.ts`: `browserType.launch: Executable doesn't exist at /opt/pw-browsers/chromium_headless_shell-1234/chrome-headless-shell-linux64/chrome-headless-shell`, confirmed this sandbox's pre-baked rev is `1194` against the committed `@playwright/test@^1.62.0`'s required `1234` — BUGS-51 environment-gap family, not a regression; `npx playwright install` intentionally not run per this environment's own instructions. lyra-mcp-server (`main` `550dfae`, unchanged): `tsc` clean + **768/768** (45 suites) PASS, matching the documented floor exactly; both `mcp.checklyra.com` and `mcp-dev.checklyra.com` `/.well-known/mcp.json` `build_sha` confirmed byte-identical to `origin/main` HEAD; CI (`Test` + `Post-merge deploy smoke`) green on that HEAD. Two incidental side-effects noted and reverted, not committed: `npm run build` regenerated `AGENTS.md`'s Next.js-managed agent-rules block locally — discarded via `git checkout --` (same handling as the 2026-08-26/28 precedent); a transient `tests/unit/__ctl039_probe__.test.js` file appeared mid-run (a test's own create/delete probe cycle) and had already vanished by the time it was inspected — no action needed. | **None fixed — none needed.** Zero FAILs in application code on either repo. | **Report-only (out of autonomous bounds — security/architectural, all already tracked):** open BUGS/SEC backlog via JQL (`project in (BUGS, SEC) AND status not in (Done) AND priority = Highest`) — **5** Highest-priority (SEC-121/118/46 In Progress, SEC-109/48 To Do — all RLS/auth/consent, out of boundary 4), unchanged from prior runs; zero new BUGS/SEC tickets in the last 24h (`created >= -1d`, confirmed 0 results); zero open Highest-priority BUGS. TODO/FIXME scan: 1 match in `lyra` `src/` (`candidate-sourcing.ts`, pre-existing KAN-184-ticketed stub, unchanged); 0 in `lyra-mcp-server`. Skip/only scan: same legitimate conditional gates as every prior run (Python-availability `describe.skip` ×2 in `lyra`, one env-gated `test.skip`; none in `lyra-mcp-server`). GitHub auto-heal issues: 5 open (`#306`/`#298`/`#296`/`#295`/`#293`, all May–June 2026, pre-existing, tracked under SEC-156). Open PRs: `lyra` **14** (7 draft founder-WIP security/CI fixes pending sign-off + 7 ready — 5 Dependabot bumps [2 major: eslint 9→10, typescript 5.9→7.0, not low-risk, not touched] + #702 KAN-469 profile-copy removal under `DESIGN_APPROVED`, rule C, not mine to touch); `lyra-mcp-server` **16** (7 draft, 9 ready, mostly Dependabot minor/patch), both repos' CI green — nothing actionable this run. | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **64 commits** (drift **RED** per `RELEASE_POLICY.md` — ≥15 threshold, though HEAD is 0 days old): `docs`-labelled ×36 (run-log rows, near-daily cadence), `fix` ×11, `feat`-labelled ×4 (KAN-459/CTL-076, SEC-151/CTL-071, BUGS-103/CTL-069, SEC-158/CTL-068 — all internal CI-control/test tooling, not user-facing), `chore` ×4, `test` ×2, `security` ×2 (run-log commits, not app code), `deps` ×2, `ci` ×2, 1 unprefixed squash (SEC-160+SEC-151, #842). Checked all 64 for rule-C exposure via `scripts/check-ui-copy-ownership.sh` (authoritative) — `no changed files in range` — rule C clear. ⚠️ **Still flagging for Luisa before she runs the beta promote (unchanged since 2026-08-22, still 5 migrations):** re-verified via `list_migrations` against `prod-lyra` (`llzkgprqewuwkiwclowi`) — latest applied is still `sec112_invariant_is_published_guard` (`20260816171013`); the SEC-57 retroactive-suspension-revoke migration (`20260819210000`), the three SEC-160 reconciliation migrations (`20260817100000`/`100100`/`100200`), and the SEC-103 default-function-grant inversion (`20260820090000`, applied to dev+staging only) are **still NOT applied to production**. Per `DESIGN_CHANGE_WORKFLOW.md`, a migration must land on **production** before the `staging → beta` promote, not before `beta → main` — pre-condition to action before the next beta promote, not a blocker on today's staging rehearsal. No blocking Highest-priority ticket against a live incident; today is Saturday (not Friday-UK-afternoon), and this rehearsal is staging-only regardless per boundary (A); no incident; smoke tests green. Ran `promote-to-staging.yml` (run [33229692010](https://github.com/luisa-sys/lyra/actions/runs/33229692010)) against develop HEAD `ffddd39` — **completed success**. **Directly re-verified**: `origin/staging` HEAD (`d149c93`) confirmed to have `origin/develop` (`ffddd39`) as an ancestor via `git merge-base --is-ancestor`; `stage.checklyra.com` → `302` (Vercel SSO/Cloudflare Access, expected) and `mcp.checklyra.com/health` → `200`. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

| 2026-08-30 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `200` `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `200` `{status:ok, version:1.0.0}`. develop HEAD (start of run) `314368a` — Deploy to Development + CodeQL both `success`. One pre-existing red workflow noted, not a new finding: `.github/workflows/absent-secrets-probe.yml` (SEC-165, status "In Progress") still fails on every push — no replacement PR open since #851/#852 closed unmerged 2026-08-26; security-sensitive `if:`-context fix, out of this routine's autonomous scope, flagged not touched. lyra runner (`RUN_E2E=1`): **PASS=6 FAIL=0 UNVERIFIED=2** — lint/type-check/unit/scripts/build/audit all clean. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51, unchanged). `e2e` **UNVERIFIED**: classifier reported "Playwright browsers unavailable" (10 skipped) — went further than the classifier, reproduced directly via `npx playwright test --project=chromium tests/e2e/public-pages.spec.ts`: `browserType.launch: Executable doesn't exist at /opt/pw-browsers/chromium_headless_shell-1234/chrome-headless-shell-linux64/chrome-headless-shell`, confirmed this sandbox's pre-baked rev is `1194` against the committed `@playwright/test@^1.62.0`'s required `1234` — BUGS-51 environment-gap family, not a regression; `npx playwright install` intentionally not run per this environment's own instructions. lyra-mcp-server (`main` `550dfae`, unchanged): `tsc` clean + **768/768** (45 suites) PASS, matching the documented floor exactly; both `mcp.checklyra.com` and `mcp-dev.checklyra.com` `/.well-known/mcp.json` `build_sha` confirmed byte-identical to `origin/main` HEAD. No incidental local side-effects this run (`git status` clean after the full suite). | **None fixed — none needed.** Zero FAILs in application code on either repo. | **Report-only (out of autonomous bounds — security/architectural, all already tracked):** open BUGS/SEC backlog via JQL (`project in (BUGS, SEC) AND status not in (Done) AND priority = Highest`) — **5** Highest-priority (SEC-121/118/46 In Progress, SEC-109/48 To Do — all RLS/auth/consent, out of boundary 4), unchanged from prior runs; zero new BUGS/SEC tickets in the last 24h (`created >= -1d`, confirmed 0 results). TODO/FIXME scan: 2 matches in `lyra` `src/` (both in `candidate-sourcing.ts`, pre-existing KAN-184/future-ticket-labelled stubs, unchanged); 0 in `lyra-mcp-server`. Skip/only scan: same legitimate conditional gates as every prior run (Python-availability `describe.skip` ×2 in `lyra`, one env-gated `test.skip` in `profile-redesign.spec.ts`; none in `lyra-mcp-server`). GitHub auto-heal issues: same 5 open (`#306`/`#298`/`#296`/`#295`/`#293`), all pre-existing (May–June 2026), nothing new. Open PRs: `lyra` **14** (7 draft founder-WIP security/CI fixes pending sign-off + 7 ready — 6 Dependabot bumps [2 major: eslint 9→10, typescript 5.9→7.0, not low-risk, not touched] + #702 KAN-469 profile-copy removal under `DESIGN_APPROVED`, rule C, not mine to touch); `lyra-mcp-server` **16** (7 draft + 9 ready, mostly Dependabot minor/patch), both repos' CI green — nothing actionable this run. SEC-168 (merge-queue deadlock — every open PR conflicts on generated ledger baseline files, serializing the queue) reconfirmed still open/To Do — architectural CI-design decision requiring a merge-queue or ledger-split fix, not this routine's to build, already flagged for Luisa in the ticket. | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **67 commits** (drift **RED** per `RELEASE_POLICY.md` — ≥15 threshold, though HEAD is 0 days old): `docs`-labelled ×39 (run-log rows, near-daily cadence), `fix` ×11, `feat`-labelled ×4 (internal CI-control/test tooling — KAN-459/CTL-076/077, SEC-119/CTL-026 — not user-facing), `chore` ×4, `test` ×2, `security` ×2 (run-log commits, not app code), `deps` ×2, `ci` ×2, 1 unprefixed squash (SEC-160+SEC-151, #842). Checked all 67 for rule-C exposure via `scripts/check-ui-copy-ownership.sh` (authoritative, `BASE_REF=origin/main HEAD_REF=origin/develop`) — `no founder-owned UI/copy paths changed` — rule C clear; the one `.tsx` touched (`src/app/admin/reports/[id]/page.tsx`, SEC-118) is the explicitly carved-out `src/app/admin/**` path. ⚠️ **Still flagging for Luisa before she runs the beta promote (unchanged since 2026-08-22, still 5 migrations):** re-verified via `list_migrations` against `prod-lyra` (`llzkgprqewuwkiwclowi`) — latest applied is still `sec112_invariant_is_published_guard` (`20260816171013`); the SEC-57 retroactive-suspension-revoke migration (`20260819210000`), the three SEC-160 reconciliation migrations (`20260817100000`/`100100`/`100200`), and the SEC-103 default-function-grant inversion (`20260820090000`, applied to dev+staging only) are **still NOT applied to production**. Per `DESIGN_CHANGE_WORKFLOW.md`, a migration must land on **production** before the `staging → beta` promote, not before `beta → main` — pre-condition to action before the next beta promote, not a blocker on today's staging rehearsal. No blocking Highest-priority ticket against a live incident; today is Sunday, not Friday-UK-afternoon, and this rehearsal is staging-only regardless per boundary (A); no incident; smoke tests green. Ran `promote-to-staging.yml` (run [33288777988](https://github.com/luisa-sys/lyra/actions/runs/33288777988)) against develop HEAD `314368a` — **completed success**. **Directly re-verified**: `origin/staging` HEAD (`275f9bb`) tree confirmed byte-identical to `origin/develop` (`314368a`, `git diff` empty); `stage.checklyra.com` → `302` (Vercel SSO/Cloudflare Access, expected) and `mcp.checklyra.com/health` → `200`. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

| 2026-08-31 | Weekly Health+Regression (Claude Code, remote) | Live health **GREEN**: `checklyra.com/api/health` → `200` `{ok:true, vercelEnv:production}`; `mcp.checklyra.com/health` → `200` `{status:ok, version:1.0.0}`. develop HEAD (start of run) `f171002` — Deploy to Development (run [33347305984](https://github.com/luisa-sys/lyra/actions/runs/33347305984)) + CodeQL (run [33347305983](https://github.com/luisa-sys/lyra/actions/runs/33347305983)) both `success`. **Positive finding, not previously reported**: `.github/workflows/absent-secrets-probe.yml` (SEC-165) — reported red every run since 2026-08-11 — was actually fixed on develop by `3e27f8a` (2026-08-27, "land SEC-106/CTL-073 + SEC-165"), but the workflow only fires on its Monday 07:00 UTC schedule (or manual dispatch) so no run had exercised the fix. Manually dispatched it against develop HEAD to check: run [33351422355](https://github.com/luisa-sys/lyra/actions/runs/33351422355) — **success**. SEC-165 can be closed. lyra runner (`RUN_E2E=1`): **PASS=6 FAIL=0 UNVERIFIED=2** — lint/type-check/unit/scripts/build/audit all clean. `integration` **UNVERIFIED** (no `tests/integration/` dir, BUGS-51, unchanged). `e2e` **UNVERIFIED**: classifier reported "Playwright browsers unavailable" (10 skipped) — reproduced directly via `npx playwright test --project=chromium tests/e2e/public-pages.spec.ts`: `browserType.launch: Executable doesn't exist at /opt/pw-browsers/chromium_headless_shell-1234/…`, confirmed this sandbox's pre-baked rev is `1194` against the committed `@playwright/test@^1.62.0`'s required `1234` — BUGS-51 environment-gap family, not a regression; real E2E confirmed green in CI on this exact HEAD (`E2E Tests (PR gate)` run [33347163141](https://github.com/luisa-sys/lyra/actions/runs/33347163141), `success`); `npx playwright install` intentionally not run per this environment's own instructions. lyra-mcp-server (`main` `550dfae`, unchanged): `tsc` clean + **768/768** (45 suites) PASS, matching the documented floor exactly; `mcp.checklyra.com/.well-known/mcp.json` `build_sha` confirmed byte-identical to `origin/main` HEAD. One incidental side-effect noted and reverted, not committed: `npm run build` regenerated `AGENTS.md`'s Next.js-managed agent-rules block locally — discarded via `git checkout --` (same handling as the 2026-08-26/28/29 precedent). Isolation note: this session's assigned branch `claude/hopeful-bohr-i8xjc2` was found locally rooted on `main` (hundreds of stale promote-merge commits, ~1 real commit ahead of `main`, matching the BUGS-19 branch-topology shape) with no remote copy and no PR ever opened against it — reset to `origin/develop` and pushed to establish tracking before any work began; no work was lost. | **None fixed in application code — none needed.** Zero FAILs on either repo. SEC-165 confirmed resolved (see suite result column) — recommend closing the ticket; not closed here as this routine reports rather than transitions tickets outside its own run-log ownership. | **Report-only (out of autonomous bounds — security/architectural, all already tracked):** open BUGS/SEC backlog via JQL (`project in (BUGS, SEC) AND status not in (Done) AND priority = Highest`) — **5** Highest-priority (SEC-121/118/46 In Progress, SEC-109/48 To Do — all RLS/auth/consent, out of boundary 4), unchanged from prior runs; **1** BUGS/SEC ticket created in the last 24h (`created >= -1d`) — BUGS-111 (stuck draft PR #834, filed by today's earlier Daily Security routine run, not new from this run). TODO/FIXME scan: 2 matches in `lyra` `src/` (both in `candidate-sourcing.ts`, pre-existing KAN-184/future-ticket-labelled stubs, unchanged); 0 in `lyra-mcp-server`. Skip/only scan: same legitimate conditional gates as every prior run (Python-availability `describe.skip` ×2 in `lyra`, one env-gated `test.skip` in `profile-redesign.spec.ts`; none in `lyra-mcp-server`). GitHub auto-heal issues: same 5 open (`#306`/`#298`/`#296`/`#295`/`#293`), all pre-existing (May–June 2026), nothing new. Open PRs: `lyra` **14** (7 draft founder-WIP security/CI fixes pending sign-off + 7 ready — 6 Dependabot bumps [2 major: eslint 9→10, typescript 5.9→7.0, not low-risk, not touched] + #702 KAN-469 profile-copy removal under `DESIGN_APPROVED`, rule C, not mine to touch); `lyra-mcp-server` **16** (7 draft + 9 ready, mostly Dependabot minor/patch), both repos' CI green — nothing actionable this run. SEC-168 (merge-queue deadlock — every open PR conflicts on generated ledger baseline files, serializing the queue) reconfirmed still open/To Do — architectural CI-design decision requiring a merge-queue or ledger-split fix, not this routine's to build, already flagged for Luisa in the ticket. | **YES — release rehearsed to staging.** `origin/main..origin/develop` = **70 commits** (drift **RED** per `RELEASE_POLICY.md` — ≥15 threshold, though HEAD is 0 days old): `docs`-labelled ×42 (run-log rows, near-daily cadence), `fix` ×11, `feat`-labelled ×4 (internal CI-control/test tooling — KAN-459/CTL-076, SEC-151/CTL-071, BUGS-103/CTL-069, SEC-158/CTL-068 — not user-facing), `chore` ×4, `test` ×2, `security` ×2 (run-log commits, not app code), `deps` ×2, `ci` ×2, 1 unprefixed squash (SEC-160+SEC-151, #842). Checked all 70 for rule-C exposure via `scripts/check-ui-copy-ownership.sh` (authoritative, `BASE_REF=origin/main HEAD_REF=origin/develop`) — `no founder-owned UI/copy paths changed` — rule C clear; the one `.tsx` touched (`src/app/admin/reports/[id]/page.tsx`, SEC-118) is the explicitly carved-out `src/app/admin/**` path. ⚠️ **Still flagging for Luisa before she runs the beta promote (unchanged since 2026-08-22, still 5 migrations):** re-verified via `list_migrations` against `prod-lyra` (`llzkgprqewuwkiwclowi`) — latest applied is still `sec112_invariant_is_published_guard` (`20260816171013`); the SEC-57 retroactive-suspension-revoke migration (`20260819210000`), the three SEC-160 reconciliation migrations (`20260817100000`/`100100`/`100200`), and the SEC-103 default-function-grant inversion (`20260820090000`, applied to dev+staging only) are **still NOT applied to production**. Per `DESIGN_CHANGE_WORKFLOW.md`, a migration must land on **production** before the `staging → beta` promote, not before `beta → main` — pre-condition to action before the next beta promote, not a blocker on today's staging rehearsal. No blocking Highest-priority ticket against a live incident; today is Monday, not Friday-UK-afternoon, and this rehearsal is staging-only regardless per boundary (A); no incident; smoke tests green. Ran `promote-to-staging.yml` (run [33351539384](https://github.com/luisa-sys/lyra/actions/runs/33351539384)) against develop HEAD `f171002` — **completed success**. **Directly re-verified**: `origin/staging` HEAD (`a7bd402`) tree confirmed byte-identical to `origin/develop` (`f171002`, `git diff` empty); `stage.checklyra.com` → `302` (Vercel SSO/Cloudflare Access, expected) and `mcp.checklyra.com/health` → `200`. Did **not** run `promote-staging-to-beta.yml` or `promote-to-production.yml` per boundary (A). Luisa's commands when ready: `gh workflow run promote-staging-to-beta.yml -f confirm=promote` → verify → `gh workflow run promote-to-production.yml -f confirm=PRODUCTION`. |

## Self-update log

- **2026-07-22** — Added an `audit` phase to `scripts/weekly-health-regression.sh` (default phase set) that runs the SAME `npm audit --audit-level=high` gate as `pr-checks.yml` + every `deploy-*.yml`. Motivation: this run found the deploy/PR audit gate RED (SEC-91) **only by running the command by hand** — the deterministic runner had no visibility into the one gate that blocks all PRs and the whole deploy chain. A registry-unreachable audit is classified **UNVERIFIED** (env gap), a genuine high/critical advisory is a **FAIL** (+2 net-new tests, no existing test modified). Proposed via `claude/nice-meitner-invjio` PR — behaviour change to the routine, so opened for review, **not self-merged**.
- **2026-07-25** — Corrected this doc's own Hard boundary #1, which still described the fix-only auto-promote-to-production exception as active nearly two days after Luisa withdrew it (2026-07-23). Motivation: the task instruction for this run explicitly stated the withdrawal, but nothing in the written docs said so — a future run reading only the docs (not a live task instruction) could have concluded the exception was still authorised. Also corrected the same drift in `CLAUDE.md` and `docs/RELEASE_POLICY.md` (three spots). Filed as **SEC-95**, proposed via draft PR **#562** — governance/policy-accuracy doc, opened for review per this routine's convention, **not self-merged**.
- **2026-07-27** — This doc's "🚦 Hard boundaries" list and embedded routine-prompt template described only two absolute rules (A/B), but the live scheduled task prompt already carried a third — (C) "LOOK AND TEXT BELONGS TO LUISA", the UI/copy-ownership carve-out backed by `scripts/check-ui-copy-ownership.sh`. Motivation: same drift class as the 2026-07-25 fix above (doc vs. live-prompt mismatch), this time an omission of a newer rule rather than a stale statement of a withdrawn one — a future run reading only this doc could conclude the carve-out doesn't exist and edit a Luisa-owned page/component/stylesheet. Filed as **KAN-430**, proposed via draft PR **#588** — doc-only, opened for review per this routine's convention, **not self-merged**.
- **2026-07-28** — SEC-95's 2026-07-25 fix corrected boundary #1 but missed two other spots in this same file: the "How the run is split" Layer table's Release row, and step 5 of the embedded routine prompt — both still described an active fix-only auto-promote path (including the literal `promote_mode=auto-fix-only` invocation), directly contradicting boundary #1 immediately above them. Motivation: my own task instructions this run already reflect the withdrawal and a newer rule (C, UI/copy ownership) that this doc's embedded prompt copy doesn't mention at all — a sign the mirror drifts each time the live prompt changes, not just once. Rewrote both spots to match the current rule (prepare + report only, never promote past staging). Proposed via draft PR **#617** — opened for review per this routine's convention, **not self-merged**.
- **2026-07-29** — Step 5's drift-classification instruction (`git log --oneline origin/main..origin/develop`) doesn't tell the agent to fetch `origin/main` first, and the sandbox's initial clone can carry a stale local `origin/main` from whenever the container snapshot was taken — this run computed drift as 51 commits, then caught (via an unrelated `git fetch --prune` surfacing unexpected old tags) that the true count against a freshly-fetched `origin/main` was 25. The routine already explicitly fetches `develop`/`staging`/`beta`; `main` was the one branch only ever read implicitly. Recommend adding an explicit `git fetch origin main` immediately before the step-5 drift computation, every run — cheap, and removes a whole class of stale-ref false readings. Not yet applied to the routine prompt/script itself (this is a doc-log note only); proposing via **claude/correct-drift-count-20260729**, opened for review, **not self-merged**.
- **2026-08-06** — This run's pre-provisioned session branch (`claude/hopeful-bohr-vfc23q`) was rooted on `origin/main` instead of `origin/develop` — identical to main's tip with zero unique commits, reading as 343 "commits ahead of develop" purely from promote-merge topology (the exact BUGS-19 trap, CLAUDE.md gotcha #1/#20). The routine's "FIRST run" step only checks out `develop` and verifies the script exists — it never checks the *designated fix branch's* base. Recommend adding one cheap check right after the initial `git fetch origin develop`: if the designated `claude/*` branch already exists (locally or on the remote) and `git merge-base --is-ancestor origin/develop <branch>` fails, re-root it onto `origin/develop` before doing any other work, the same fix applied by hand this run. Not yet applied to the routine prompt/doc itself (this is a doc-log note only); proposing via this run's own PR.
- **2026-08-09** — `scripts/weekly-health-regression.sh`'s FAIL detail used a bare `tail -n 3` on the phase log, which can land entirely inside jest's `Snapshots:`/`Time:`/`Ran all test suites` boilerplate and never show WHICH suite failed — noted as a rough edge in the 2026-08-08 row above, then hit again for real this run (had to manually re-run `npm run test:unit` to find the version-drift failure). Added a `fail_detail()` helper that greps for jest's own `FAIL <path>` and `Tests:`/`Test Suites:` lines when present, falling back to the old tail-based behaviour for non-jest phases. Mutation-proven: reverted the script, confirmed the new test failed exactly as expected; restored, confirmed green. Proposed via PR **[#720](https://github.com/luisa-sys/lyra/pull/720)** — behaviour change to the runner, opened for review per this routine's convention. CI green (PR Quality Gate, CodeQL, Playwright E2E all passed) and low-risk (script + test only, no app code), so merged same run per the routine's own "CI green + low-risk → may merge" authority — merged `816054f`.
- **2026-08-10** — This run's first unit-test pass reported a 28-test FAIL entirely inside `tests/scripts/check-extraction-dod.test.js`, which on inspection was 100% caused by the `gh` CLI being completely absent from this sandbox (the script's own `[out-of-repo]` mcp-repo-backreference check fails closed with `gh is not available…`, by design — see gotcha table above, exit 2 "cannot verify"). `apt-get install -y gh` fixed it instantly (38/38 pass after). This is a new instance of the same BUGS-51 environment-gap family as the missing `tests/integration/` dir and the missing Playwright browser revisions/webkit, but it hadn't been hit or logged before — and unlike those two, it doesn't show up as a whole-phase UNVERIFIED in the deterministic runner's classifier (`scripts/weekly-health-regression.sh`'s `run_phase()` only pattern-matches Playwright/no-tests/npm-registry messages), so it read as a genuine 28-test regression on first sight and could easily be mistaken for one by a future run or a human skimming this log. Recommend: (1) add `apt-get install -y gh` (or equivalent) as an explicit early setup step in this routine's own instructions, alongside `npm ci`, so the gap is closed before phase 2 rather than discovered mid-run; (2) consider teaching `run_phase()`'s UNVERIFIED classifier to also recognise `gh is not available` in a jest failure log, the same way it already recognises Playwright/registry gaps, since this check is jest-based rather than a whole-command failure and so slips past the existing patterns. Not applied to the routine script itself this run (this is a doc-log note only, and the actual fix — installing `gh` — is sandbox-local and doesn't touch the repo); proposing via **claude/weekly-health-regression-20260810**, opened for review, **not self-merged**.
- **2026-08-15** — This run's session was isolated via `EnterWorktree` (per this repo's own CLAUDE.md "Parallel Claude sessions" policy, since another session's draft PRs (#801, #806) were visibly active against the backlog at the same time). That tool nests the new worktree at `<repo>/.claude/worktrees/<name>` — a *different* convention from the one this repo documents (`git worktree add ../lyra-<branch> origin/develop`, a sibling directory) — and `jest.config.js`'s `testPathIgnorePatterns` (`/\.claude/`, added for KAN-180's own anti-double-run reason) matches the entire nested checkout. Result: `unit`/`scripts`/`integration` all silently reported `0 matches` instead of running 3,926 real tests — and the runner's existing "no tests matched" message is worded identically to the already-known, benign `tests/integration/`-missing gap, so the two are indistinguishable from the log alone. Worked around this run by re-running the affected phases from a **sibling** worktree instead (`git worktree add ../lyra-jest-check origin/develop`), confirming the true result (295/295 suites, 3926/3926 tests, matching the documented floor). Filed as **CLAUDE.md gotcha #33** (this run's own PR) so a future session — routine or otherwise — recognises the `0 matches` result as an environment-placement defect rather than either a real regression or the known integration-dir gap. Not a change to the routine's own script/prompt (the fix is "don't run tests from inside `.claude/`", which is a session-setup choice, not something `weekly-health-regression.sh` can detect from inside a jest subprocess without new instrumentation) — recording as a doc-log note plus the CLAUDE.md gotcha; proposing via this run's own PR, **not self-merged pending review** (docs-only, but touches project-wide guidance, not just this routine).
- **2026-08-16** — The 2026-07-29 recommendation below (explicit `git fetch origin main` before the drift computation) was never applied to the routine's own instructions, and it recurred: this run's sandbox carried a local `origin/main` ref stale by a whole promote (`19298ed`, 2026-08-09) against the real tip (`029896a`, that same morning), computing drift as **116 commits** — a false 🔴-RED reading that would have been reported as a genuine backlog spike had it not been caught by cross-checking the commit list against known recent history before writing it down. Confirmed the routine's stored task prompt still only says `git fetch origin develop && git checkout develop` as its FIRST step — `main` is never explicitly fetched anywhere in the prompt, exactly as the 2026-07-29 entry diagnosed. Recommend (again, more concretely this time): add `git fetch origin main` as a second explicit command in the routine's FIRST-run step, right alongside the existing `git fetch origin develop`, so the stale-ref class is closed at the source rather than relying on a future run's cross-check catching it by luck. Not applied to the stored prompt itself this run (this doc-log note plus a corrected reading in today's row is the artifact); proposing via this run's own PR, **not self-merged** (prompt/doc guidance, not app code).
- **2026-08-19** — Fixing a false E2E FAIL (missing CI dummy env vars, see today's row above) introduced a second, worse bug on the first attempt: quoting the defaulted values inside the command string returned by `cmd_for()`. `run_phase()` executes that string via unquoted `$c`, which tokenises into argv by word-splitting alone — word-splitting does not strip quote characters, only variable *expansion* does — so `env` received `NEXT_PUBLIC_SUPABASE_URL="https://e2e-dummy.supabase.co"` with the literal quote characters baked into the value, an invalid URL. This reproduced a 100%-repeatable Playwright `webServer` timeout through the real script while the identical values, exported normally and passed directly to `npx playwright test` (bypassing `env`'s word-splitting), passed every time — a discrepancy that would have been invisible to a review resting on the isolated/direct test alone. Caught only because this run's own convention (re-validate through the actual invocation path, not a bypass) was followed a second time after the first fix looked done. Recommend generalising the lesson: any future patch to `cmd_for()`'s generated command strings must be validated by running the *actual* `weekly-health-regression.sh` phase, not a hand-rolled equivalent — an isolated repro proves the destination command works, not that the generator produces it correctly. Fixed and shipped in the same PR as the original env-var fix (**BUGS-109**, PR **#854**) rather than as a separate follow-up, since both were caught and resolved before merge in this run — no further action needed on the routine's own instructions.
- **2026-08-22** — The `e2e` phase has now read UNVERIFIED-by-missing-browser three runs running (2026-08-20, 2026-08-21, today), always for the same reason: this remote session's sandbox pre-installs `chromium` only, while `playwright.config.ts`'s `mobile-safari` project needs `webkit`, which the script's own classifier correctly reports as an environment gap rather than a FAIL — but every prior run stopped there. This run went one step further: ran `npx playwright install webkit --with-deps` (in addition to the chromium install already covered by this environment's own setup) and re-ran `test:e2e` directly against the real suite. Result: genuinely green, 34 passed / 10 skipped / 0 failed — not a hypothetical fix, a verified one. Recommending this routine's Setup section note explicitly that BOTH `chromium` and `webkit` need installing in this remote-session sandbox (not just `--with-deps` with no target, which this environment's own guidance discourages running bare) — closing this the same way BUGS-109's env-var fix closed the e2e-UNVERIFIED-by-missing-vars class two runs ago. Not applied to the stored routine setup script itself this run (this doc-log note plus today's row is the artifact); proposing via this run's own PR, **not self-merged** (routine setup guidance, not app code, and the actual claude.ai routine's setup script — distinct from this remote session's own environment — may already differ from what's documented here).
