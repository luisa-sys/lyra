# Staging Soak — Claude Code cloud-routine setup

> **Ticket:** KAN-413 (this routine) · concern **staging-soak** in
> `docs/OPS_ROUTINES_CONTROL_ROOM.md`. Grounded in the official docs:
> [Claude Code on the web](https://code.claude.com/docs/en/claude-code-on-the-web) ·
> [Routines](https://code.claude.com/docs/en/routines).

This is the **automation** for a daily soak test of the **promoted staging
environment** (`stage.checklyra.com`, branch `staging`, Supabase
`uobmlkzrjkptwhttzmmi`). It runs as a **scheduled Claude Code routine** — a cloud
session that spins up on a timer, checks out the spec, exercises staging
end-to-end *as a user would*, compares the result to the release contract below,
files a deduped BUGS/SEC ticket for anything new, appends a heartbeat row, and
emails on FAIL.

It is modelled 1:1 on the **Daily Security Check** routine
(`docs/DAILY_SECURITY_CHECK_ROUTINE.md`) — same clone/checkout mechanics, same
connector-not-container-secrets model, same no-silent-skip honesty rules — so
read that doc first if anything here is unclear.

> **This routine is DETECT-AND-LOG ONLY.** It identifies an issue and **logs it
> as a bug** (a BUGS ticket; SEC for a genuine security/privacy finding) — then
> stops. It never edits code, never opens a fix PR, never applies a migration,
> and never writes to prod. Fixing a logged bug is a separate human / Backlog
> Autopilot follow-up. The only writes it makes are to the disposable **staging**
> DB via the soak harness, and the heartbeat/run-log PR from a `claude/` branch.

---

## Why a soak test, and what it is NOT

The existing staging coverage is **deploy-gated and point-in-time**:

- `health-check.yml` — shallow HTTP liveness, every 6h (**liveness owner**).
- `staging-tests.yml` — axe + Lighthouse against a *just-deployed* URL, nightly
  05:00 + post-deploy (**deploy-gate**).
- `e2e-authed.yml` / `e2e-tests.yml` — Playwright journeys, **dispatch/PR-gated**.

None of these repeatedly exercises the **live, already-promoted staging build**
over time. A soak does: it is the daily canary that catches drift, degradation,
and journey breakage that appear *between* deploys — config/secret drift, a
migration that landed on the DB but not the app, an invite queue that stops
draining, latency creep, error-budget burn, a published profile that stops
rendering. **staging-soak** is a *new, distinct owner concern* (KAN-361); it
**cites** the liveness/security/regression owners, it does not re-derive them.

**One thing this routine deliberately does NOT test: real user sign-up.** It
drives a **pre-provisioned persistent soak user** and resets it to the initial
account-setup state each run, so it exercises *everything after* account
creation without re-creating the account. Sign-up itself is a **separate,
un-skippable promotion gate** — see `docs/SIGNUP_SURFACE_GATE.md`. If a change
touches the sign-up surface, the promotion-to-staging gate forces a full
sign-up E2E that **cannot be skipped**; the daily soak assumes a valid account
already exists.

---

## The release contract — "what good looks like on staging" (UPDATE THIS WITH RELEASES)

> **This section is the whole point of keeping the spec in git.** When a release
> changes what staging should do, the **same PR updates this contract**, and the
> next soak run tests against the new expectation. A soak that passes against a
> stale contract is worthless — treat editing this section as part of the
> definition-of-done for any change to the routes, journeys, or health signals
> below. CI does not (cannot) prove the contract matches intent; the reviewer
> of the release PR does.

### C1 — Public/edge surface (anonymous, no bypass)
- `stage.checklyra.com` root → **401 or 403** (Vercel deployment protection is
  ON for staging). A **200** anonymous is a FAIL (protection is down).
- `/status` → **200** (public status page, exempt from the beta gate).
- `/api/health` → **200** JSON with `ok:true`. Values must match this env:
  `siteUrl` = `https://stage.checklyra.com`, `isBetaDeploy` = `false`,
  `vercelEnv` = `preview`. A mismatch is a **release-conformance** finding.
- `/robots.txt`, `/sitemap.xml`, `/.well-known/security.txt` → 200 or 403.
- **`C1-sitemap-fresh`** — `/sitemap.xml` must be rendered **per request**, not
  prerendered at build. A prerendered sitemap keeps a SUSPENDED member's slug
  advertised to crawlers until the next deploy while their profile page 404s
  (SEC-100). Classified by `scripts/classify-sitemap-freshness.sh` from
  `x-vercel-cache` (`miss`/`bypass` → PASS, `hit`/`stale`/`prerender`/
  `revalidated` → FAIL) with a non-zero `age` as the secondary FAIL signal;
  anything else is **UNVERIFIED**, because a proxy that strips Vercel's cache
  telemetry leaves the question genuinely unanswerable.

  ⚠️ **This clause used to classify on `Cache-Control`, and that instrument was
  informationless** (BUGS-103). `src/app/sitemap.ts` is a Next *metadata route*
  and Next's loader hardcodes the header to a constant. Measured on two real
  production builds of this repo: with `force-dynamic` the route table reads
  `ƒ /sitemap.xml` and with it removed `○ /sitemap.xml` — the defect genuinely
  reintroduced — and **both emit `public, max-age=0, must-revalidate`,
  byte-identical**. So the old probe was permanently red *and* could never have
  detected SEC-100. Do not reintroduce a `Cache-Control` assertion here; a
  regression test in `tests/scripts/classify-sitemap-freshness.test.js` fails
  the build if anyone does.

### C2 — Authenticated surface (via the grey-cloud alias)
- Against `e2e-stage.checklyra.com` (`STAGE_SITE`/`BASE_URL`), the key routes
  return **200** within the latency budget (C4): `/`, `/login`, `/signup`,
  `/status`, `/api/health`. `/join` is a reachability check (it 307-redirects to
  `/signup`). The C1 **gate** check stays on the real `stage.checklyra.com`.
- No route in the set returns **5xx** across the repeated soak passes (C4).

### C3 — Persistent-user journey (read-write, staging DB is disposable)
Driven against the soak user (`soak@seed.checklyra.com`), reset to initial first:
1. **Baseline** — after reset the user is *initial account setup*: name only,
   not published, 18+ declared, no items/affiliations/entitlements/gatherings.
2. **Build** — add intro → onboarding state advances `empty → drafted`.
3. **Publish** — publish the profile → state `published_activate`; the public
   `/<slug>` renders the published profile within the latency budget.
4. **Grow** — add a gift + a school affiliation → state `published_grow`; the
   Manual-of-Me edit round-trips to the public profile.
5. **Gather** *(when Convene is entitled on staging)* — create a gathering,
   add the soak user's second identity, then cancel it; no orphaned rows remain.
6. **Reset** — restore initial account setup (afterAll), and assert the baseline
   is clean. **If reset leaves rows behind, FAIL loudly** — the reset table-list
   in `tests/e2e/support/soak-user.ts` is stale and must be extended (this is
   part of the release contract).

### C4 — Soak signals (sustained, not a single hit)
- Each C1/C2 route is probed **N=5** times per run; report **p50 (median) + max**
  latency (`staging-soak.sh` computes p50/max, not a true p95 — the budget is
  enforced on the pass-count below, not a percentile).
- **Latency budget:** **2500 ms** for pages, **800 ms** for `/api/health`.
  **≥2 of the 5 passes over budget → a degradation FAIL.**
- A pass returning a **non-200** (a Cloudflare "Just a moment" 403, a redirect,
  or the bypass not honoured) is **UNVERIFIED — latency is NOT measured** on a
  challenge page (never a fast false-PASS). A **000** is UNVERIFIED (unreachable).

### C5 — Data / drift on the staging DB (via the Supabase connector)
> **Baseline (refreshed 2026-07-27, re-verified directly against
> `uobmlkzrjkptwhttzmmi`):** staging carries **10 security** + **~151
> performance** advisor lints and **0 NULL-token `auth.users` rows**. C5 flags
> only a **change vs. this baseline / the last run-log row**, never the standing
> counts, so a run does not open a 150+-finding storm.
>
> Two things changed since the 2026-07-25 commissioning baseline, both
> **improvements**, both already reported as non-findings by run 4:
> - **NULL-token rows: 8 → 0.** The 8 `seed.%@seed.checklyra.com` rows still
>   exist (`seed_rows_total = 8`) but their token/change columns are now
>   normalised, so the GoTrue-500 `listUsers` quirk is gone. **The new baseline
>   is 0** — meaning *any* NULL-token row is now a finding, seed or not. This is
>   strictly stronger than the old "ignore 8 seed rows" rule.
> - **Performance lints: 154 → ~151.** Do **not** treat the count as a fixed
>   number. 65 of them are `unused_index`, which is derived from live
>   `pg_stat_user_indexes` usage, so the total **drifts on its own** (154 on
>   07-25 → 152 on 07-27 04:16 → 151 later that day) with no code change at all.
>   **A downward move is never a finding**, and neither is a ±few wobble. C5
>   compares **lint identity** (name + target object), not totals.
- `get_advisors(type=security|performance)` on `uobmlkzrjkptwhttzmmi` → no **new**
  advisor **name+target** vs. the last run-log row (baseline 10 sec / ~151 perf;
  the perf total drifts — compare identities, not counts).
- **Invite queue** `public.gathering_invite_messages` is draining (no rows older
  than 24h still un-sent).
- **NULL-token `auth.users`** — baseline is now **0**, so flag **any** row (seed
  or real) whose `confirmation_token` / `recovery_token` / `email_change*` /
  `phone_change*` / `reauthentication_token` is NULL. Such a row re-breaks the
  GoTrue admin `listUsers` call with a 500 and takes C3 down with it (that is
  exactly how run 1 failed). The old "8 seed rows are expected" carve-out is
  **retired** — the seed rows were normalised on 2026-07-25/26.
  ⚠️ `scripts/seed-demo-profiles.sql` still inserts NULL token columns, so
  **re-seeding staging will re-introduce this** — normalise after any re-seed.
- No **stale `auth.one_time_tokens`** piling up beyond their TTL.
- **Migration parity:** DB head == latest in `supabase/migrations/` on `staging`
  (baseline DB head 2026-07-27: `20260727123657`). Repo-ahead or DB-ahead = a
  finding. Note the DB head is an **apply timestamp** and will not string-match
  the repo filename prefix; compare the *migration*, not the digits.

### C6 — Error budget (last 24h)
- Vercel/Sentry runtime errors on staging over the last 24h below threshold
  (no new *unhandled* error signature vs. the last run-log row). Cite Sentry;
  do not re-derive liveness (that is `health-check.yml`).

---

## How the run is split

| Layer | Driven by | Covers |
|---|---|---|
| Deterministic HTTP + latency probes | `scripts/staging-soak.sh` (curl) | C1, C2, C4 |
| Read-write persistent-user journey | Playwright `soak-journey` project (`tests/e2e/soak/journey.soak.spec.ts` + `tests/e2e/support/soak-user.ts`) | C3 |
| DB drift / advisors / migration parity | the **agent**, via the **Supabase connector** | C5 |
| Error budget + release conformance | the agent (Sentry/Vercel connector + `/api/health`) | C6, C1 |
| Triage + reporting | the agent | dedupe vs Jira, file tickets, heartbeat, email |

The deterministic script needs nothing but `curl`. The journey needs the staging
service-role key + bypass headers in the routine env (below). The DB/error
checks run through **connectors** (routed via Anthropic — no container tokens).

### Graceful degradation (no silent-skip)
The content + journey layers target the grey-cloud `e2e-stage` alias, which has
no Cloudflare in front of it, so they reach the origin directly. If a page is
nonetheless challenged/redirected (e.g. `STAGE_SITE` misconfigured back to the
CF-proxied host), the affected probe reports **UNVERIFIED** — never a green PASS
and never a false FAIL — naming the unblock (point `STAGE_SITE`/`BASE_URL` at
`e2e-stage.checklyra.com`). The C1-gate, C5 and C6 layers always run. UNVERIFIED
is a soft-FAIL for the heartbeat, not a pass.

---

## ⚠️ Prerequisite: the spec must be on the branch the routine clones

A routine **clones the repo's default branch** (`main`) each run. This spec +
`scripts/staging-soak.sh` + the soak Playwright files currently live on branch
`claude/staging-soak-routine` → PR into `develop`. Until they reach the clone,
the **first line of the routine prompt** must check out the branch that has
them:

```
git fetch origin develop --depth=1 && git checkout develop
```

Point it at `develop` once the PR merges (recommended); drop the line entirely
once the files ride `develop → staging → beta → main` to `main`. The GitHub
proxy only restricts **pushes**; fetching/checking out another branch to *read*
is fine. **If the script still isn't present after checkout, STOP and say the
checkout failed — do NOT improvise probes.**

---

## Setup (mirror the Daily Security routine)

1. **Setup script:** leave **EMPTY**. `curl`, `jq`, `git`, `node`, `npx`,
   `ripgrep`, `bash` are pre-installed; the script is committed executable.
2. **Network access → Custom → Allowed domains:**
   ```
   checklyra.com
   *.checklyra.com
   stage.checklyra.com
   *.supabase.co
   api.resend.com
   ```
   Tick "Also include default list of common package managers" (Playwright needs
   npm). **`*.supabase.co` is MANDATORY for C3** — the journey's service-role
   admin client (`supabase-admin.ts`) and `mint-session`'s `generateLink` talk
   **directly** to `https://uobmlkzrjkptwhttzmmi.supabase.co`, not via the
   Supabase connector; without it the journey errors (worse than UNVERIFIED).
3. **Connectors:** **Supabase, GitHub, Atlassian** (+ **Sentry/Vercel** if
   available for C6). Remove every other connector — the connector list is a
   scope control.
4. **Permissions:** leave **"Allow unrestricted branch pushes" OFF** — the
   heartbeat/run-log change goes out as a PR from a `claude/` branch; the run
   cannot push to `staging`/`develop`/`main`.
5. **Env vars / secrets** (visible to anyone who can edit the environment — keep
   to these). **⚠️ These are the ROUTINE's OWN env store — SEPARATE from the
   repo's GitHub Actions secrets.** A secret existing in `gh secret list` does
   NOT make it available here; each must be set in the claude.ai routine env,
   and proved with a supervised "Run now" (test-plan A4).
   - `RESEND_API_KEY` — FAIL-alert email (reuses `scripts/security-alert-email.sh`).
   - `E2E_SUPABASE_URL` = `https://uobmlkzrjkptwhttzmmi.supabase.co`,
     `E2E_SUPABASE_SERVICE_ROLE_KEY` = the **staging** service-role key (the
     harness hard-guards against prod — `tests/e2e/support/supabase-admin.ts`).
     ⚠️ This is the **staging** key — a *different* value from the repo's
     `E2E_SUPABASE_*` GitHub secrets, which are **dev**-scoped (they back the
     `e2e-dev` signup-gate run). Do not cross them. **Requires `*.supabase.co`
     in the allowlist** (the harness hits Supabase directly).
   - `STAGE_SITE` = `https://e2e-stage.checklyra.com` and
     `BASE_URL` = `https://e2e-stage.checklyra.com` — point the **content** (C1
     pages, C2/C4 latency) and the **browser journey** (C3) at the DNS-only
     grey-cloud alias, which serves the same staging build with **no Cloudflare
     challenge** (`docs/E2E_AUTHED_CF_BYPASS.md`). The C1 protection **gate** stays
     on the real `stage.checklyra.com` via `GATE_SITE` (its default) — so we
     still confirm the user-facing host is gated. Validated 2026-07-25: all of
     C1/C2/C4 PASS against the alias.
   - `VERCEL_AUTOMATION_BYPASS` — **optional.** The grey-cloud alias serves 200
     without it, so it isn't required; harmless if present. **Do NOT set
     `E2E_CF_BYPASS_*`** — a Cloudflare WAF *skip* rule cannot except free-plan
     Bot Fight Mode (proven dead, `docs/E2E_AUTHED_CF_BYPASS.md`); the grey-cloud
     alias is the working substitute.
   - Optional `ALERT_TO` / `ALERT_FROM` (default `luisa@santos-stephens.com` /
     `security@checklyra.com`, a Resend-verified sender).
6. **Schedule:** **Daily ~04:12 UTC** (de-collided from the existing crons:
   01:00 daily-security, 03:20/09:20/… autopilot, 05:00 staging-tests). After
   the overnight jobs, before the nightly staging tests.

---

## The routine prompt

```
First, if scripts/staging-soak.sh is not present on the checked-out branch,
run: git fetch origin develop --depth=1 && git checkout develop
Then verify scripts/staging-soak.sh AND tests/e2e/soak/journey.soak.spec.ts
exist; if either does not, STOP and reply that the develop checkout failed — do
NOT improvise probes, read other docs, or proceed.

You are the Lyra Staging Soak (KAN-413). Target is STAGING ONLY
(stage.checklyra.com / Supabase uobmlkzrjkptwhttzmmi). Staging is disposable:
you MAY write to the staging DB via the soak harness. NEVER touch prod
(llzkgprqewuwkiwclowi) or dev. Read docs/STAGING_SOAK_ROUTINE.md — its "release
contract" section (C1–C6) is what you assert. Treat any UNVERIFIED as a
soft-FAIL, never a pass (Workflow & Backup Integrity Policy).

1. Run: bash scripts/staging-soak.sh   (C1/C2/C4 — capture every
   PASS/FAIL/UNVERIFIED line + the p50/p95 latencies).
2. Run the persistent-user journey (C3):
     E2E_AUTHED= SOAK_JOURNEY=1 BASE_URL=https://stage.checklyra.com \
     npx playwright test --project=soak-journey
   The spec ensures + resets the soak user to initial account setup, drives
   build→publish→grow→(gather), and resets to initial in afterAll. A reset that
   leaves rows behind FAILS — file that as a soak-reset finding.
   If a page is challenged/redirected (BASE_URL not pointed at the e2e-stage
   alias), record C3 as UNVERIFIED naming that unblock; do NOT mark it PASS/FAIL.
3. Via the Supabase connector on uobmlkzrjkptwhttzmmi (C5): get_advisors
   security+performance; invite-queue drain; NULL-token auth.users rows; stale
   one_time_tokens; migration parity (list vs supabase/migrations on staging).
   READ-ONLY except the soak harness's own writes.
4. C6: last-24h Vercel/Sentry error budget for staging; cite health-check.yml's
   last conclusion for liveness — do NOT re-curl the endpoint list.
5. Compare EVERY result to C1–C6 and the last run-log row in this doc. For any
   NEW finding not already covered by an open ticket (check Jira FIRST):
   - functional/reliability/perf/data → **BUGS** ticket (project BUGS, type
     Task): summary "[SOAK][<sev>] <short>", the 6-section standard, labels
     staging-soak + automated.
   - security/privacy → **SEC** ticket instead (per CLAUDE.md). Do NOT fix code
     or migrations; do NOT touch prod.
6. Append one run-log row (below) + one Ops-Control-Room heartbeat row as your
   FINAL checkpoint, via a PR from a claude/ branch (only that change).
7. If any FAIL: (a) email — pipe a one-paragraph summary into
   `bash scripts/security-alert-email.sh "Lyra staging soak: <N> FAIL"`; and
   (b) put a clear "PAGE:" line at the very top of your final reply.
   If clean: state "all green" with the PASS/UNVERIFIED counts; still write both
   log rows.
```

---

## Reporting

- **Tickets:** deduped against open Jira first. BUGS for functional/reliability/
  perf/data; SEC for security/privacy. Never a fix — the soak observes and
  reports; a human (or the Backlog Autopilot) fixes.
- **Heartbeat:** one row in the Confluence *Ops Routines Control Room*
  Heartbeat/Run-ledger every run, as the FINAL checkpoint (a run with no
  heartbeat is a **missed run** to the watchdog).
- **Email on FAIL:** reuses `scripts/security-alert-email.sh` → Resend. Fails
  loud if `RESEND_API_KEY` is missing or Resend returns non-2xx.
- The **watchdog** in the Daily Security routine covers this routine
  (`staging-soak|1740|<last-iso>|<outcome>` — 24h cadence + 5h grace).

## Why not (only) a GitHub Action?

`staging-tests.yml` already runs the deploy-gated axe/Lighthouse subset from
Actions. The value a routine adds is the parts an Action can't do well: driving
the Supabase/Sentry **connectors** with judgement (C5/C6), triaging against Jira,
and filing well-formed tickets. The Action remains the fallback for the
pure-HTTP subset.

---

## Verifying it works autonomously

Before this routine is trusted unattended, and after any change to
`staging-soak.sh`, the soak spec, or the C1–C6 contract, run the fault-injection
suite in **`docs/STAGING_SOAK_TEST_PLAN.md`**. It proves the routine *detects*
problems (not just that it runs green) and that its own liveness is watched.

## Run log (newest first)

| Date (UTC) | Runner | PASS/FAIL/UNVERIFIED | p50/max (page/health) | New tickets | Notes |
|---|---|---|---|---|---|
| 2026-09-02 04:13 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 225–524ms max 943ms / health p50 244ms max 1236ms | none (all identities unchanged) | **All green — no FAIL, no UNVERIFIED.** ⚠️ **Housekeeping first:** this run's designated branch (`claude/stoic-cerf-l3jjaj`) was found pinned at `origin/main`'s promote-chain tip (`db67ea8`, 534 commits of its own, all promote-chain history, none real, 50 behind `origin/develop`) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` (`fed8ce6`) before proceeding; the repo mirror run-log was already current as of that reset (last row 2026-09-01 — no backfill needed). C1/C2/C4: two transient UNVERIFIED blips across the first two `staging-soak.sh` passes (`C4-signup` 3/5 unreachable `000`, then on an immediate re-run `C4-login` 4/5 unreachable `000`) — a third pass came back clean 13/13 PASS with every route tight; treated as client-side egress noise (same pattern as prior transient blips, e.g. runs on 2026-08-06/08-11/08-15/08-19/08-29), not logged. C1 gate on the real host 302/gated, `/api/health` conformant (`siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match), **C1-sitemap-fresh PASSed** (`x-vercel-cache: miss`) on all three passes, 0/5 over budget on every C4 route in the clean pass except one `/api/health` outlier (1/5, 1236ms vs 800ms budget — below the ≥2/5 degradation threshold, not a finding). **C3 PASS** — cited from `soak-journey.yml`: latest run [33585299810](https://github.com/luisa-sys/lyra/actions/runs/33585299810) completed+success at 2026-09-02T02:59:43Z (~1.25h old, well within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — unchanged:** DB head still `20260820032946` (`sec103_invert_default_function_grant`); verified against `origin/staging` (`git ls-tree`) — the three SEC-160 reconciliation migrations (`20260817100000_sec160_reconcile_mcp_tool_call_log.sql`, `20260817100100_sec160_reconcile_content_moderation_flags.sql`, `20260817100200_sec160_reconcile_retention_jobs.sql`) remain unapplied on staging, no new file landed — not logged as a new finding since SEC-160 itself already tracks this. SEC-130, SEC-160, SEC-103 all reconfirmed still In Progress via Jira. C6: clean — zero runtime errors project-wide in the last 24h (Vercel `get_runtime_errors`, project `lyra`); liveness cited from `health-check.yml`: success 2026-09-02T03:51:49Z (run 549). Dedup check (Jira, `labels=staging-soak AND status != Done`): BUGS-103, BUGS-99, BUGS-88 all pre-existing and open (confirmed via `searchJiraIssuesUsingJql`), unaffected by this run — no new ticket filed. No alert email (all green — no FAIL, so the alert-email path was not exercised, sidestepping BUGS-99). |
| 2026-09-01 04:14 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 273–662ms max 772ms / health p50 250ms max 365ms | none (all identities unchanged) | **All green — no FAIL, no UNVERIFIED.** ⚠️ **Housekeeping first:** this run's designated branch (`claude/stoic-cerf-rmjgpb`) was found pinned at `origin/main`'s promote-chain tip (`db67ea8`, 534 commits of its own, all promote-chain history, none real) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` (`be5e51d`) before proceeding; the repo mirror run-log was already current as of that reset (last row 2026-08-31 — no backfill needed). C1/C2/C4: 13 PASS via `bash scripts/staging-soak.sh` against `STAGE_SITE=https://e2e-stage.checklyra.com`, one clean pass, no re-run needed — C1 gate on the real host 302/gated, `/api/health` conformant (`siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match), **C1-sitemap-fresh PASSed** (`x-vercel-cache: miss`), 0/5 over budget on every C4 route. **C3 PASS** — cited from `soak-journey.yml`: latest run [33464558268](https://github.com/luisa-sys/lyra/actions/runs/33464558268) completed+success at 2026-09-01T02:59:19Z (~1.3h old, well within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — unchanged:** DB head still `20260820032946` (`sec103_invert_default_function_grant`); verified against `origin/staging` (`git ls-tree`) — the three SEC-160 reconciliation migrations (`20260817100000_sec160_reconcile_mcp_tool_call_log.sql`, `20260817100100_sec160_reconcile_content_moderation_flags.sql`, `20260817100200_sec160_reconcile_retention_jobs.sql`) remain unapplied on staging, no new file landed — not logged as a new finding since SEC-160 itself already tracks this. C6: clean — zero runtime errors project-wide in the last 24h (Vercel `get_runtime_errors`, project `lyra`); liveness cited from `health-check.yml`: success 2026-08-31T22:40:36Z (run 544). Dedup check (Jira, `labels=staging-soak AND status != Done`): BUGS-103, BUGS-99, BUGS-88 all pre-existing and open (confirmed via `searchJiraIssuesUsingJql`), unaffected by this run — no new ticket filed. No alert email (all green — no FAIL, so the alert-email path was not exercised, sidestepping BUGS-99). |
| 2026-08-31 04:13 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 266–635ms max 747ms / health p50 232ms max 261ms | none (all identities unchanged) | **All green — no FAIL, no UNVERIFIED.** ⚠️ **Housekeeping first:** this run's designated branch (`claude/stoic-cerf-wegew1`) was found sitting on `origin/main`'s promote-chain tip (`db67ea8`, 534 commits of its own, all promote-chain history, none real) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` before proceeding; the repo mirror run-log was already current as of that reset (last row 2026-08-30 — no backfill needed). C1/C2/C4: 13 PASS via `bash scripts/staging-soak.sh` against `STAGE_SITE=https://e2e-stage.checklyra.com`, one clean pass, no re-run needed — C1 gate on the real host 302/gated, `/api/health` conformant (`siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match), **C1-sitemap-fresh PASSed** (`x-vercel-cache: miss`), 0/5 over budget on every C4 route. **C3 PASS** — cited from `soak-journey.yml`: latest run [33352071527](https://github.com/luisa-sys/lyra/actions/runs/33352071527) completed+success at 2026-08-31T02:53:26Z (~1.3h old, well within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — unchanged:** DB head still `20260820032946` (`sec103_invert_default_function_grant`); verified against `origin/staging` (`git ls-tree`) — the three SEC-160 reconciliation migrations (`20260817100000_sec160_reconcile_mcp_tool_call_log.sql`, `20260817100100_sec160_reconcile_content_moderation_flags.sql`, `20260817100200_sec160_reconcile_retention_jobs.sql`) remain unapplied on staging, no new file landed — not logged as a new finding since SEC-160 itself already tracks this. Both SEC-103 and SEC-160 reconfirmed still In Progress via Jira. C6: clean — zero runtime errors project-wide in the last 24h (Vercel `get_runtime_errors`, project `lyra`); liveness cited from `health-check.yml`: success 2026-08-30T20:49:18Z (run 541). Dedup check (Jira): SEC-130, SEC-160, SEC-103, BUGS-99, BUGS-88, BUGS-92, BUGS-103 all pre-existing and open (confirmed via `searchJiraIssuesUsingJql`), unaffected by this run — no new ticket filed. No alert email (all green — no FAIL, so the alert-email path was not exercised, sidestepping BUGS-99). |
| 2026-08-30 04:12 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 235–579ms max 795ms / health p50 239ms max 260ms | none (all identities unchanged) | **All green — no FAIL, no UNVERIFIED.** ⚠️ **Housekeeping first:** this run's designated branch (`claude/stoic-cerf-phbrw4`) was found sitting on `origin/main`'s promote-chain tip (`db67ea8`, 534 commits of its own, all promote-chain history, none real) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` before proceeding; the repo mirror run-log was already current as of that reset (last row 2026-08-29 — no backfill needed). C1/C2/C4: 13 PASS via `bash scripts/staging-soak.sh` against `STAGE_SITE=https://e2e-stage.checklyra.com`, one clean pass, no re-run needed — C1 gate on the real host 302/gated, `/api/health` conformant (`siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match), **C1-sitemap-fresh PASSed** (`x-vercel-cache: miss`), 0/5 over budget on every C4 route. **C3 PASS** — cited from `soak-journey.yml`: latest run [33289080740](https://github.com/luisa-sys/lyra/actions/runs/33289080740) completed+success at 2026-08-30T02:55:04Z (~1.3h old, well within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — unchanged:** DB head still `20260820032946` (`sec103_invert_default_function_grant`); verified against `origin/staging` (`git ls-tree`) — the three SEC-160 reconciliation migrations (`20260817100000_sec160_reconcile_mcp_tool_call_log.sql`, `20260817100100_sec160_reconcile_content_moderation_flags.sql`, `20260817100200_sec160_reconcile_retention_jobs.sql`) remain unapplied on staging, no new file landed — not logged as a new finding since SEC-160 itself already tracks this. Both SEC-103 and SEC-160 reconfirmed still In Progress via Jira. C6: clean — zero runtime errors project-wide in the last 24h (Vercel `get_runtime_errors`, project `lyra`); liveness cited from `health-check.yml`: success 2026-08-29T20:44:15Z (run 537). Dedup check (Jira): SEC-130, SEC-160, SEC-103, BUGS-99, BUGS-88, BUGS-98, BUGS-92, BUGS-103, BUGS-100 all pre-existing and open (confirmed via `searchJiraIssuesUsingJql`), unaffected by this run — no new ticket filed. No alert email (all green — no FAIL, so the alert-email path was not exercised, sidestepping BUGS-99). |
| 2026-08-29 04:14 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 203–556ms max 660ms / health p50 172ms max 179ms | none (all identities unchanged) | **All green — no FAIL, no UNVERIFIED.** ⚠️ **Housekeeping first:** this run's designated branch (`claude/stoic-cerf-3c6g01`) had no open PR and sat ~110 commits behind `origin/develop` with no unique work of its own — reset onto `origin/develop` (`d36a82e`) before proceeding; the repo mirror of this run log was already current as of that reset (last row 2026-08-28 — no backfill needed). C1/C2/C4: two transient UNVERIFIED blips on the first two `staging-soak.sh` passes (`C4-home` 4/5 unreachable `000`, then on the immediate re-run `C4-login` 3/5 unreachable `000`) — a third pass 15s later came back clean 13/13 PASS; treated as client-side egress noise (same pattern as prior transient blips, e.g. runs on 2026-08-06/08-11/08-15/08-19), not logged. C1 gate on the real host 302/gated, `/api/health` conformant (`siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match), **C1-sitemap-fresh PASSed** (`x-vercel-cache: miss`), 0/5 over budget on every C4 route in the clean pass. **C3 PASS** — cited from `soak-journey.yml`: latest run [33230037118](https://github.com/luisa-sys/lyra/actions/runs/33230037118) completed+success at 2026-08-29T02:53:09Z (~1.4h old, well within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — unchanged findings:** DB head still `20260820032946` (`sec103_invert_default_function_grant`); verified against `origin/staging` (`git ls-tree`) — the three SEC-160 reconciliation migrations (`20260817100000_sec160_reconcile_mcp_tool_call_log.sql`, `20260817100100_sec160_reconcile_content_moderation_flags.sql`, `20260817100200_sec160_reconcile_retention_jobs.sql`) remain unapplied on staging, no new file landed — not logged as a new finding since SEC-160 itself already tracks this. Both SEC-103 and SEC-160 reconfirmed still In Progress via Jira. C6: clean — zero runtime errors project-wide in the last 24h (Vercel `get_runtime_errors`, project `lyra`); liveness cited from `health-check.yml`: success 2026-08-28T22:00:51Z (run 534). Dedup check (Jira): SEC-130, SEC-160, SEC-103, BUGS-99, BUGS-88, BUGS-98, BUGS-92, BUGS-103, BUGS-100 all pre-existing and open, unaffected by this run — no new ticket filed. No alert email (all green — no FAIL, so the alert-email path was not exercised, sidestepping BUGS-99). |
| 2026-08-28 04:14 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 239–607ms max 711ms / health p50 265ms max 281ms | none (all identities unchanged) | **All green — no FAIL, no UNVERIFIED.** ⚠️ **Housekeeping first:** this run's designated branch (`claude/stoic-cerf-0xkct1`) was found sitting on `origin/main`'s promote-chain tip (`db67ea8`, 280 commits of its own, all promote-chain history, none real) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` (`9add9e7`) before proceeding; the repo mirror of this run log was already current as of that reset (last row 2026-08-27 — no backfill needed). C1/C2/C4: 13 PASS via `bash scripts/staging-soak.sh` against `STAGE_SITE=https://e2e-stage.checklyra.com`, one clean pass, no re-run needed — C1 gate on the real host 302/gated, `/api/health` conformant (`siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match), **C1-sitemap-fresh PASSed** (`x-vercel-cache: miss`), 0/5 over budget on every C4 route. **C3 PASS** — cited from `soak-journey.yml`: latest run [33137159596](https://github.com/luisa-sys/lyra/actions/runs/33137159596) completed+success at 2026-08-28T02:51:19Z (~1.4h old, well within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — unchanged findings, one correction to prior rows' citation:** DB head still `20260820032946` (`sec103_invert_default_function_grant`); verified against `origin/staging` (`git ls-tree`) — the three SEC-160 reconciliation migrations (`20260817100000_sec160_reconcile_mcp_tool_call_log.sql`, `20260817100100_sec160_reconcile_content_moderation_flags.sql`, `20260817100200_sec160_reconcile_retention_jobs.sql`) remain unapplied on staging, no new file landed. The prior three rows (08-25/26/27) described `sec103_invert_default_function_grant` as "absent from every branch's committed `supabase/migrations/` tree, traced to open draft PR #862" — that PR was closed superseded on 2026-08-26T23:47Z and its content landed on `develop` via PR #891 (merged as `0133a5e`, 2026-08-27T00:47Z), confirmed present today in both `origin/develop`'s and `origin/staging`'s trees as `20260820090000_sec103_invert_default_function_grant.sql`. So that half of the gap is now closed (repo and DB agree); not logged as a new finding since SEC-103 itself already tracks this (reconfirmed via its own comment history) and remains open pending the production leg only. Both SEC-103 and SEC-160 reconfirmed still In Progress via Jira. C6: clean — zero runtime errors project-wide in the last 24h (Vercel `get_runtime_errors`, project `lyra`); liveness cited from `health-check.yml`: success 2026-08-27T22:03:25Z (run 532). Dedup check (Jira): SEC-130, SEC-160, SEC-103, BUGS-99, BUGS-88, BUGS-98, BUGS-92, BUGS-103, BUGS-100 all pre-existing and open, unaffected by this run — no new ticket filed. No alert email (all green — no FAIL, so the alert-email path was not exercised, sidestepping BUGS-99). |
| 2026-08-27 04:15 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 262–649ms max 817ms / health p50 228ms max 277ms | none (all identities unchanged) | **All green — no FAIL, no UNVERIFIED.** ⚠️ **Housekeeping first:** this run's designated branch (`claude/stoic-cerf-a2552h`) was found sitting on `origin/main`'s promote-chain tip (`db67ea8`, 0 commits of its own) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` (`b115dd1`) before committing; the repo mirror of this run log was current as of that reset (last row 2026-08-26, matching `origin/develop` — no backfill needed). C1/C2/C4: 13 PASS via `bash scripts/staging-soak.sh` against `STAGE_SITE=https://e2e-stage.checklyra.com`, one clean pass, no re-run needed — C1 gate on the real host 302/gated, `/api/health` conformant (`siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match), **C1-sitemap-fresh PASSed** (`x-vercel-cache: miss`), 0/5 over budget on every C4 route. **C3 PASS** — cited from `soak-journey.yml`: latest run [33034777847](https://github.com/luisa-sys/lyra/actions/runs/33034777847) completed+success at 2026-08-27T02:55:22Z (~1.3h old, well within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — unchanged from 2026-08-26, not re-commented:** DB head still `20260820032946` (`sec103_invert_default_function_grant`); verified against `origin/staging` (`git ls-tree`) — the three SEC-160 reconciliation migrations (`20260817100000_sec160_reconcile_mcp_tool_call_log.sql`, `20260817100100_sec160_reconcile_content_moderation_flags.sql`, `20260817100200_sec160_reconcile_retention_jobs.sql`) remain unapplied on staging, no new file landed since yesterday; `sec103_invert_default_function_grant` remains applied to the staging DB but absent from every branch's committed `supabase/migrations/` tree, still traced to open draft PR [#862](https://github.com/luisa-sys/lyra/pull/862) (SEC-103, reconfirmed In Progress via Jira as of the last comment). Both SEC-103 and SEC-160 unchanged from yesterday's row — no new comment on either. C6: clean — zero runtime errors project-wide in the last 24h (Vercel `get_runtime_errors`, project `lyra`); liveness cited from `health-check.yml`: success 2026-08-26T19:52:41Z (run 530). Dedup check (Jira, by prior-row citation — no new finding to re-check): SEC-130, SEC-160, SEC-103, BUGS-99, BUGS-88, BUGS-98, BUGS-103, BUGS-92 all pre-existing and open per yesterday's row, unaffected by this run — no new ticket filed. No alert email (all green — no FAIL, so the alert-email path was not exercised, sidestepping BUGS-99). |
| 2026-08-26 04:18 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 240–540ms max 720ms / health p50 237ms max 281ms | none (all identities unchanged) | **All green — no FAIL, no UNVERIFIED.** ⚠️ **Housekeeping first:** this run's designated branch (`claude/stoic-cerf-ppv06t`) was found pinned at `db67ea8` (2026-08-17, `origin/main`'s promote-chain tip) rather than current `origin/develop` — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology, this time caught only after an initial read of this file returned the (stale) 2026-08-16 row as "latest," which would have caused an unnecessary backfill attempt. Reset onto `origin/develop` (`3853dbc`) before re-reading; the repo mirror of this run log was already current as of that reset (last row 2026-08-25 — no backfill needed). C1/C2/C4: `bash scripts/staging-soak.sh` against `STAGE_SITE=https://e2e-stage.checklyra.com` — first pass showed a transient `C1-join` FAIL (`000000`, unreachable); direct `curl` retries against `/join` both returned clean `307`s within 2s, and an immediate full re-run came back 13/13 PASS — treated as client-side egress noise (same pattern as many prior runs), not logged. Clean run: C1 gate on the real host 302/gated, `/api/health` conformant (`siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match), **C1-sitemap-fresh PASSed** (`x-vercel-cache: miss`), 0/5 over budget on every C4 route except one `C4-health` sample on the noisy first pass (not counted). **C3 PASS** — cited from `soak-journey.yml`: latest run [32924751608](https://github.com/luisa-sys/lyra/actions/runs/32924751608) completed+success at 2026-08-26T02:59:27Z (~1.25h old, well within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — unchanged from 2026-08-25, not re-commented:** DB head still `20260820032946` (`sec103_invert_default_function_grant`); verified against `origin/staging` (`git ls-tree`) — the three SEC-160 reconciliation migrations remain unapplied on staging, no new file landed; `sec103_invert_default_function_grant` remains applied to the staging DB but absent from every branch's committed `supabase/migrations/` tree (still traced to open draft PR #862, SEC-103 reconfirmed In Progress via Jira). Both SEC-103 and SEC-160 reconfirmed still In Progress, state identical to yesterday — no new comment. C6: clean — zero runtime errors project-wide in the last 24h (Vercel `get_runtime_errors`, project `lyra`); liveness cited from `health-check.yml`: success 2026-08-26T01:39:37Z (run 527). Dedup check (Jira): SEC-130, SEC-160, SEC-103, BUGS-99, BUGS-88, BUGS-98, BUGS-103, BUGS-92 all pre-existing and open (statuses re-verified via JQL), unaffected by this run — no new ticket filed. No alert email (all green — no FAIL, so the alert-email path was not exercised, sidestepping BUGS-99). |
| 2026-08-25 04:13 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 235–623ms max 726ms / health p50 249ms max 261ms | none (all identities unchanged) | **All green — no FAIL, no UNVERIFIED.** ⚠️ **Housekeeping first:** this run's designated branch (`claude/stoic-cerf-5nk311`) was found sitting on `origin/main`'s promote-chain tip (`db67ea8`, 0 commits of its own) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` (`9fd7758`) before committing; the repo mirror of this run log was current as of that reset (last row 2026-08-24, matching `origin/develop` — no backfill needed). C1/C2/C4: 13 PASS via `scripts/staging-soak.sh` against `STAGE_SITE=https://e2e-stage.checklyra.com`, one clean pass, no re-run needed — C1 gate on the real host 302/gated, `/api/health` conformant (`siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match), **C1-sitemap-fresh PASSed** (`x-vercel-cache: miss`), 0/5 over budget on every C4 route. **C3 PASS** — cited from `soak-journey.yml`: latest run [32803009895](https://github.com/luisa-sys/lyra/actions/runs/32803009895) completed+success at 2026-08-25T02:52:05Z (~1.4h old, well within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — same state as 2026-08-24, unchanged, not re-commented:** DB head still `20260820032946` (`sec103_invert_default_function_grant`); verified against `origin/staging` (`git ls-tree`) — the three SEC-160 reconciliation migrations (`20260817100000_sec160_reconcile_mcp_tool_call_log.sql`, `20260817100100_sec160_reconcile_content_moderation_flags.sql`, `20260817100200_sec160_reconcile_retention_jobs.sql`) remain unapplied on staging, no new file landed since yesterday; `sec103_invert_default_function_grant` remains applied to the staging DB but absent from every branch's committed `supabase/migrations/` tree, still traced to open draft PR [#862](https://github.com/luisa-sys/lyra/pull/862) (SEC-103, reconfirmed In Progress via Jira). Both SEC-103 and SEC-160 reconfirmed still In Progress, state identical to yesterday's row — no new comment on either. C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`, project `lyra`); liveness cited from `health-check.yml`: success 2026-08-25T01:34:59Z. Dedup check (Jira): SEC-130, SEC-160, SEC-103, BUGS-99, BUGS-88, BUGS-98, BUGS-103, BUGS-92 all pre-existing and open (statuses re-verified via JQL), unaffected by this run — no new ticket filed. No alert email (all green — no FAIL, so the alert-email path was not exercised, sidestepping BUGS-99). |
| 2026-08-24 04:13 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 275–463ms max 708ms / health p50 213ms max 320ms | none (all identities unchanged) | **All green — no FAIL, no UNVERIFIED.** ⚠️ **Housekeeping first:** this run's designated branch (`claude/stoic-cerf-x5ditb`) was found sitting on `origin/main`'s promote-chain tip (`db67ea8`, 0 commits of its own) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` (`034a078`) before committing; the repo mirror of this run log was current as of that reset (last row 2026-08-23, matching `origin/develop` — no backfill needed). C1/C2/C4: 13 PASS via `scripts/staging-soak.sh` against `STAGE_SITE=https://e2e-stage.checklyra.com`, one clean pass, no re-run needed — C1 gate on the real host 302/gated, `/api/health` conformant (`siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match), **C1-sitemap-fresh PASSed** (`x-vercel-cache: miss`), 0/5 over budget on every C4 route. **C3 PASS** — cited from `soak-journey.yml`: latest run [32684407176](https://github.com/luisa-sys/lyra/actions/runs/32684407176) completed+success at 2026-08-24T02:50:58Z (~1.4h old, well within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — same state as 2026-08-23, unchanged, not re-commented:** DB head still `20260820032946` (`sec103_invert_default_function_grant`); verified against `origin/staging` (`git ls-tree`) — the three SEC-160 reconciliation migrations (`20260817100000_sec160_reconcile_mcp_tool_call_log.sql`, `20260817100100_sec160_reconcile_content_moderation_flags.sql`, `20260817100200_sec160_reconcile_retention_jobs.sql`) remain unapplied on staging, no new file landed since yesterday; `sec103_invert_default_function_grant` remains applied to the staging DB but absent from every branch's committed `supabase/migrations/` tree, still traced to open draft PR [#862](https://github.com/luisa-sys/lyra/pull/862) (SEC-103, reconfirmed In Progress via Jira). Both SEC-103 and SEC-160 reconfirmed still In Progress, state identical to yesterday's row — no new comment on either. C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`, project `lyra`); liveness cited from `health-check.yml`: success 2026-08-24T01:39:24Z. Dedup check (Jira): SEC-130, SEC-160, SEC-103, BUGS-99, BUGS-88, BUGS-98, BUGS-103, BUGS-92 all pre-existing and open (statuses re-verified via JQL), unaffected by this run — no new ticket filed. No alert email (all green — no FAIL, so the alert-email path was not exercised, sidestepping BUGS-99). |
| 2026-08-23 04:13 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 267–604ms max 688ms / health p50 286ms max 521ms | none (all identities unchanged) | **All green — no FAIL, no UNVERIFIED.** ⚠️ **Housekeeping first:** this run's designated branch (`claude/stoic-cerf-qi1nwt`) was found sitting on `origin/main`'s promote-chain tip (`db67ea8`, 0 commits of its own) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` (`bef33a7`) before committing; the repo mirror of this run log was current as of that reset (last row 2026-08-22, matching `origin/develop` — no backfill needed). C1/C2/C4: 13 PASS via `scripts/staging-soak.sh` against `STAGE_SITE=https://e2e-stage.checklyra.com`, one clean pass, no re-run needed — C1 gate on the real host 302/gated, `/api/health` conformant (`siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match), **C1-sitemap-fresh PASSed** (`x-vercel-cache: miss`), 0/5 over budget on every C4 route. **C3 PASS** — cited from `soak-journey.yml`: latest run [32613861325](https://github.com/luisa-sys/lyra/actions/runs/32613861325) completed+success at 2026-08-23T02:50:49Z (~1.4h old, well within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — same state as 2026-08-22, unchanged, not re-commented:** DB head still `20260820032946` (`sec103_invert_default_function_grant`); verified against `origin/staging` (`git ls-tree`) — the three SEC-160 reconciliation migrations remain unapplied on staging, no new file landed since yesterday; `sec103_invert_default_function_grant` remains applied to the staging DB but absent from every branch's committed `supabase/migrations/` tree, still traced to open draft PR [#862](https://github.com/luisa-sys/lyra/pull/862) (SEC-103, reconfirmed In Progress via Jira). Both SEC-103 and SEC-160 reconfirmed still In Progress, state identical to yesterday's row — no new comment on either. C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`, project `lyra`); liveness cited from `health-check.yml`: success 2026-08-23T01:41:36Z. Dedup check (Jira): SEC-130, SEC-160, SEC-103, BUGS-99, BUGS-88, BUGS-98, BUGS-103, BUGS-92 all pre-existing and open (statuses re-verified via JQL), unaffected by this run — no new ticket filed. No alert email (all green — no FAIL, so the alert-email path was not exercised, sidestepping BUGS-99). |
| 2026-08-22 04:12 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 292–618ms max 804ms / health p50 230ms max 268ms | none (all identities unchanged) | **All green — no FAIL, no UNVERIFIED.** ⚠️ **Housekeeping first:** this run's designated branch (`claude/stoic-cerf-op182s`) was found sitting on `origin/main`'s tip (`db67ea8`, 0 commits of its own, 534 commits "ahead" of `develop` via promote-chain merge history) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` (`38db9a7`) before committing; the repo mirror of this run log was current as of that reset (last row 2026-08-21, matching `origin/develop` — no backfill needed). C1/C2/C4: 13 PASS via `scripts/staging-soak.sh` against `STAGE_SITE=https://e2e-stage.checklyra.com`, one clean pass, no re-run needed — C1 gate on the real host 302/gated, `/api/health` conformant (`siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match), **C1-sitemap-fresh PASSed** (`x-vercel-cache: miss`), 0/5 over budget on every C4 route. **C3 PASS** — cited from `soak-journey.yml`: latest run [32547526077](https://github.com/luisa-sys/lyra/actions/runs/32547526077) completed+success at 2026-08-22T02:54:46Z (~1.3h old, well within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — same state as 2026-08-21, unchanged, not re-commented:** DB head still `20260820032946` (`sec103_invert_default_function_grant`); verified against `origin/staging` (`git ls-tree`) — the three SEC-160 reconciliation migrations (`20260817100000_sec160_reconcile_mcp_tool_call_log.sql`, `20260817100100_sec160_reconcile_content_moderation_flags.sql`, `20260817100200_sec160_reconcile_retention_jobs.sql`) remain unapplied on staging, no new file landed since yesterday; `sec103_invert_default_function_grant` remains applied to the staging DB but absent from every branch's committed `supabase/migrations/` tree, still traced to open draft PR [#862](https://github.com/luisa-sys/lyra/pull/862) (SEC-103, reconfirmed In Progress via Jira). Both SEC-103 and SEC-160 reconfirmed still In Progress, state identical to yesterday's row — no new comment on either. C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`, project `lyra`); liveness cited from `health-check.yml`: success 2026-08-22T01:29:25Z. Dedup check (Jira): SEC-130, SEC-160, SEC-103, BUGS-99, BUGS-88, BUGS-98, BUGS-103, BUGS-92 all pre-existing and open, unaffected by this run — no new ticket filed. No alert email (all green — no FAIL, so the alert-email path was not exercised, sidestepping BUGS-99). |
| 2026-08-21 04:13 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 243–596ms max 965ms / health p50 239ms max 353ms | none (SEC-103, SEC-160 unaffected, not re-commented) | **All green — no FAIL, no UNVERIFIED.** ⚠️ **Housekeeping first:** this run's designated branch (`claude/stoic-cerf-dy20rz`) was found sitting on `origin/main`'s tip (`db67ea8`, 0 commits of its own) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` (`087ffcc`) before committing; the repo mirror of this run log was current as of that reset (last row 2026-08-20, matching origin/develop — no backfill needed). C1/C2/C4: 13 PASS via `scripts/staging-soak.sh` against `STAGE_SITE=https://e2e-stage.checklyra.com`, one clean pass, no re-run needed — C1 gate on the real host 302/gated, `/api/health` conformant (`siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match), **C1-sitemap-fresh PASSed** (`x-vercel-cache: miss`), 0/5 over budget on every C4 route. **C3 PASS** — cited from `soak-journey.yml`: latest run [32441316546](https://github.com/luisa-sys/lyra/actions/runs/32441316546) completed+success at 2026-08-21T02:51:47Z (~1.4h old, well within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — same state as 2026-08-20, unchanged, not re-commented:** DB head still `20260820032946` (`sec103_invert_default_function_grant`); verified against `origin/staging` (`git ls-tree`) — the three SEC-160 reconciliation migrations (`20260817100000_sec160_reconcile_mcp_tool_call_log.sql`, `20260817100100_sec160_reconcile_content_moderation_flags.sql`, `20260817100200_sec160_reconcile_retention_jobs.sql`) remain unapplied on staging, no new file landed since yesterday; `sec103_invert_default_function_grant` remains applied to the staging DB but absent from every branch's committed `supabase/migrations/` tree, still traced to open draft PR [#862](https://github.com/luisa-sys/lyra/pull/862) (SEC-103, reconfirmed In Progress via Jira). Both SEC-103 and SEC-160 reconfirmed still In Progress, state identical to yesterday's row — no new comment on either. C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from `health-check.yml`: success 2026-08-21T01:38:51Z. Dedup check (Jira): SEC-130, SEC-160, SEC-103, BUGS-99, BUGS-88, BUGS-98, BUGS-103, BUGS-92 all pre-existing and open, unaffected by this run — no new ticket filed. No alert email (all green — no FAIL, so the alert-email path was not exercised, sidestepping BUGS-99). |
| 2026-08-20 04:13 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 235–659ms max 738ms / health p50 258ms max 336ms | none (SEC-103 commented) | **All green — no FAIL, no UNVERIFIED.** ⚠️ **Housekeeping first:** this run's designated branch (`claude/stoic-cerf-plvoxh`) was found sitting on `origin/main`'s tip (0 commits of its own, `db67ea8`) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` before committing. C1/C2/C4: 13 PASS via the `e2e-stage` alias, one clean pass (no re-run needed): `staging-soak.sh` against `STAGE_SITE=https://e2e-stage.checklyra.com` — C1 gate on the real host 302/gated, `/api/health` conformant (`siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match), **C1-sitemap-fresh PASSed** (`x-vercel-cache: miss`), 0/5 over budget on every C4 route. **C3 PASS** — cited from `soak-journey.yml`: latest run [32326688424](https://github.com/luisa-sys/lyra/actions/runs/32326688424) completed+success at 2026-08-20T03:00:44Z (~1.2h old, well within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — DB head advanced, traced and explained, no new ticket:** `list_migrations` shows head now `20260820032946` (`sec103_invert_default_function_grant`), up from yesterday's `20260816091238`. Traced to two applies: `sec57_revoke_credentials_on_suspend` (DB `20260819213204`, matches `origin/staging` file `20260819210000_sec57_revoke_credentials_on_suspend.sql`, apply-timestamp offset per baseline — no finding) and `sec103_invert_default_function_grant` (DB `20260820032946`), which is **not yet in any branch's `supabase/migrations/`** — traced to open draft PR [#862](https://github.com/luisa-sys/lyra/pull/862) (SEC-103, In Progress), whose body confirms it applied the migration to dev-lyra and stage-lyra directly via `apply_migration` ahead of merging. Commented on **SEC-103** (not filed as a separate ticket — this is the ticket's own active work, not an anomaly) flagging that the applied change has no committed record in any branch's tree while #862 stays open. The three SEC-160 reconciliation migrations remain unapplied on staging, unchanged from yesterday. C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from `health-check.yml`: success 2026-08-20T01:34:07Z. Dedup check (Jira): SEC-130, SEC-160, SEC-103, BUGS-99, BUGS-88, BUGS-98, BUGS-103, BUGS-92 all pre-existing and open, unaffected by this run beyond the SEC-103 comment above. No alert email (all green — no FAIL, so the alert-email path was not exercised, sidestepping BUGS-99). |
| 2026-08-19 04:13 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 276–650ms max 755ms / health p50 269ms max 289ms | none (SEC-130, SEC-160 unaffected, not re-commented) | **All green — no FAIL, no UNVERIFIED.** C1/C2/C4: a first `staging-soak.sh` pass showed a transient `C1-sectxt` FAIL (`security.txt` unreachable, `000000`); an immediate re-run 29s later came back clean 13/13 PASS, `security.txt` reachable (200) on two direct follow-up `curl`s — treated as client-side egress noise (same pattern as prior transient blips, e.g. runs on 2026-08-06/08-11/08-15), not logged. **C1-sitemap-fresh PASSed for a third consecutive day** (`x-vercel-cache: miss`), continuing to build against the streak of FAILs already tracked by **SEC-130** (still In Progress, confirmed via Jira) — not treated as resolved, no ticket action taken. **C3 PASS** — cited from `soak-journey.yml`: latest run [32100191480](https://github.com/luisa-sys/lyra/actions/runs/32100191480) completed+success at 2026-08-18T04:43:42Z (~23.5h old, within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — same gap as 2026-08-18, unchanged, not re-commented:** DB head still `20260816091238` (`sec112_invariant_is_published_guard`); `origin/staging` still carries the same three unapplied SEC-160 reconciliation migrations (`20260817100000_sec160_reconcile_mcp_tool_call_log.sql`, `20260817100100_sec160_reconcile_content_moderation_flags.sql`, `20260817100200_sec160_reconcile_retention_jobs.sql`), verified via `git ls-tree origin/staging` — no new file landed since yesterday. **SEC-160 reconfirmed still In Progress** via Jira before this run — no new comment (state identical to yesterday's). C6: clean — the only 24h runtime-error group (`Failed to find Server Action`/`missing 500.html` pair, `page: '/contact'`) traces via `get_deployment` to `dpl_Gymd8RiUQqAK2LXfse3W6v8og1cF` (`githubCommitRef=main`, `target=production`) — **prod-attributed, not staging**, the same long-running bot probe first seen 2026-06-25 — so no staging error-budget finding; liveness cited from `health-check.yml`: success 2026-08-19T01:35:07Z. Dedup check (Jira): SEC-130, SEC-160, BUGS-99, BUGS-88, BUGS-103 all pre-existing and open, unaffected by this run — no new ticket filed. No alert email (all green — no FAIL, so the alert-email path was not exercised, sidestepping BUGS-99). **Housekeeping:** this run's designated branch (`claude/stoic-cerf-mcas18`) was found sitting on `origin/main`'s tip (0 commits of its own, no merge-base found with `origin/develop` from a depth-1 fetch) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` before committing. |
| 2026-08-18 04:13 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 291–690ms max 811ms / health p50 318ms max 333ms | none (SEC-160 updated) | **All green — no FAIL, no UNVERIFIED.** C1/C2/C4: 13 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant: `siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match). **C1-sitemap-fresh PASSed for the second consecutive day** (`x-vercel-cache: miss`), continuing to build against the streak of FAILs already tracked by **SEC-130** (still In Progress) — not treated as resolved from two observations, no ticket action taken. **C3 PASS** — cited from `soak-journey.yml`: latest run [32001832897](https://github.com/luisa-sys/lyra/actions/runs/32001832897) completed+success at 2026-08-17T06:28:54Z (~21.7h old, within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — new finding, commented on SEC-160 rather than filed separately:** `origin/staging` gained three new migrations via the normal `develop → staging` promote — `20260817100000_sec160_reconcile_mcp_tool_call_log.sql`, `20260817100100_sec160_reconcile_content_moderation_flags.sql`, `20260817100200_sec160_reconcile_retention_jobs.sql` (SEC-160's own reconciliation migrations, recording schema/cron that already existed out-of-band on all three databases) — but staging DB head is still `20260816091238` (`sec112_invariant_is_published_guard`), so none have been applied via `apply_migration`. Read all three: each is guarded (`create table if not exists`, catalogue-guarded `create policy`, idempotent `cron.schedule`) specifically so applying them to a database where the objects already exist is a no-op — confirmed this is a repo-ahead parity gap, not a live break. SEC-160 itself (In Progress) already lists "apply to dev and staging" as implementation step 3, so this is the existing ticket's own outstanding work, not a new one — commented with today's status rather than filing a duplicate. C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from `health-check.yml`: success 2026-08-18T01:27:22Z. Dedup check: SEC-130, BUGS-99, BUGS-92, BUGS-88 all pre-existing and open, unaffected by this run (BUGS-99 not applicable — no FAIL this run, so the alert-email path was not exercised). No alert email (all green). **Housekeeping:** this run's designated branch (`claude/stoic-cerf-0dv1y5`) was found sitting on `origin/main`'s promote-chain tip (0 unique commits, ~534 commits "ahead" of `develop` via merge history) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` before committing. |
| 2026-08-17 04:17 | staging-soak routine (KAN-413) | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 322–643ms max 773ms / health p50 267ms max 311ms | none (SEC-130 + SEC-112 updated) | **All green — no FAIL, no UNVERIFIED.** C1/C2/C4: 13 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant: `siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match). **C1-sitemap-fresh PASSed for the first time** (`x-vercel-cache: miss`) — breaks the run of consecutive-day FAILs (2026-08-13 through 2026-08-16) already tracked by **SEC-130** (still In Progress); commented on the ticket with this evidence rather than treating it as resolved from a single observation. **C3 PASS** — cited from `soak-journey.yml`: latest run [31989292860](https://github.com/luisa-sys/lyra/actions/runs/31989292860) completed+success at 2026-08-17T02:51:44Z (~1.4h old, well within the 26h freshness window). C5 via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity now OK** — DB head advanced to `20260816091238` (`sec112_invariant_is_published_guard`), matching `origin/staging`'s three newest files by name (`sec46_oauth_resource_indicator`, `sec112_block_is_published_self_set`, `sec112_invariant_is_published_guard`) — resolves the gap the last two run-log rows tracked against **SEC-112** (itself independently confirmed Done in Jira). C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from `health-check.yml`: success 2026-08-17T01:37:36Z. Dedup check: BUGS-99, BUGS-92, BUGS-88 all pre-existing and open, unaffected by this run (BUGS-99 not applicable — no FAIL this run, so the alert-email path was not exercised). No alert email (all green). **Housekeeping:** this run's designated branch (`claude/stoic-cerf-rkpr2o`) was found sitting on `origin/main`'s promote-chain tip (0 unique commits, ~280 commits "ahead" of `develop` via merge history) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` before committing. |
| 2026-08-16 07:32 | staging-soak routine (KAN-413) | PASS=12 / FAIL=1 / UNVERIFIED=0 | pages p50 278–493ms max 541ms / health p50 246ms max 327ms | none (BUGS-99 updated) | **1 FAIL, known/recurring — not a new finding.** C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C1-sitemap-fresh FAILed for the fourth consecutive day** (`cache-control: public, max-age=0, must-revalidate`, no `no-store`/`no-cache`) — the already-tracked **SEC-130** regression (status In Progress, not yet promoted to staging). **C3 PASS** — cited from `soak-journey.yml`: latest run [31927305451](https://github.com/luisa-sys/lyra/actions/runs/31927305451) completed+success at 2026-08-16T04:43:35Z (~2.8h old, well within the 26h freshness window). C5: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — unchanged from 2026-08-15, not re-commented:** DB head still `20260811211646` (`sec104_invariant_public_profiles_view`); `origin/staging` still carries the same two unapplied SEC-112 migrations (`20260814150000_sec112_block_is_published_self_set.sql`, `20260814150500_sec112_invariant_is_published_guard.sql`) — already commented on **SEC-112** yesterday with the deploy-order precondition verified satisfied; state is identical today, so no new comment. C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from `health-check.yml`: success 2026-08-16T06:47:40Z. Dedup check: SEC-130, SEC-112, BUGS-99, BUGS-92, BUGS-88 all pre-existing and open, unaffected by this run beyond the comment below. **FAIL-alert email failed again** — fourth consecutive recurrence of **BUGS-99** (`api.resend.com` still `connect_rejected`/403 via the session egress proxy, `RESEND_API_KEY` present, length 36); commented on BUGS-99 with the new proxy-status evidence; escalated via push notification instead. |
| 2026-08-15 04:17 | staging-soak routine (KAN-413) | PASS=12 / FAIL=1 / UNVERIFIED=0 | pages p50 264–625ms max 780ms / health p50 265ms max 287ms | none (SEC-112 + BUGS-99 updated) | **1 FAIL, known/recurring — not a new finding.** C1/C2/C4: a first `staging-soak.sh` pass showed transient `C1-robots` FAIL (000000, malformed) and `C4-signup`/`C4-health` UNVERIFIED (000, unreachable) alongside the sitemap FAIL; an immediate re-run 61s later came back 12 PASS / 1 FAIL / 0 UNVERIFIED with every route clean — treated as client-side egress noise (same pattern as runs 12 and 14), not logged. **C1-sitemap-fresh FAILed for the third consecutive day** (`cache-control: public, max-age=0, must-revalidate`, no `no-store`/`no-cache`) — the already-tracked **SEC-130** regression (status In Progress, not yet promoted to staging), C1 gate on the real host otherwise 302/gated, `/api/health` conformant. **C3 PASS** — cited from `soak-journey.yml`: latest run [31860386191](https://github.com/luisa-sys/lyra/actions/runs/31860386191) completed+success at 2026-08-15T02:53:54Z (~1.4h old, well within the 26h freshness window). C5: security advisors 10, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors 157, identical breakdown to the last run-log row (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities. Invite queue (`gathering_invite_messages`): 0 stuck >24h, 0 total. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity — new finding, commented on SEC-112 rather than filed separately:** `origin/staging` gained two new migrations via the normal `develop → staging` promote — `20260814150000_sec112_block_is_published_self_set.sql` and `20260814150500_sec112_invariant_is_published_guard.sql` (the `is_published` self-write guard trigger + its INV-9 invariant) — but staging DB head is still `20260811211646` (`sec104_invariant_public_profiles_view`); neither has been applied via `apply_migration`. Checked the migration's own stated precondition (the paired `publishProfile()` service-role-client code change must already be live before the trigger, or the real publish flow breaks BUGS-60-style) — confirmed satisfied on `origin/staging` (`createServiceRoleClient()` already in place in `actions.ts`), so it's safe for a human/Backlog-Autopilot to proceed with applying the migration. Net effect: `is_published` is presumably still self-writable on staging via PostgREST today — the same pre-existing SEC-112 exposure as prod, not a new one. C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from `health-check.yml`: success 2026-08-15T01:31:41Z. Dedup check: SEC-130, BUGS-99, BUGS-88 all pre-existing and open, unaffected by this run beyond the comments above; SEC-112 pre-existing, updated with the migration-parity status. **FAIL-alert email failed again** — third consecutive recurrence of **BUGS-99** (`api.resend.com` still `connect_rejected`/403 via the session egress proxy, `RESEND_API_KEY` present); commented on BUGS-99 with the new proxy-status evidence; escalated via push notification instead. |
| 2026-08-14 04:18 | staging-soak routine (KAN-413) | PASS=12 / FAIL=1 / UNVERIFIED=0 | pages p50 432–793ms max 1074ms / health p50 363ms max 467ms | none (2 existing tickets updated) | **1 FAIL, root-caused — not a staging regression.** C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C1-sitemap-fresh FAILed**: `Cache-Control: public, max-age=0, must-revalidate` (no literal `no-store`/`no-cache`) trips the SEC-130 probe's string classifier. Follow-up `curl`s (3x) show `x-vercel-cache: MISS` + `age:0` on every request — staging is behaviourally fresh; likely a probe classifier gap, not a regression. **Bigger finding surfaced while diagnosing this**: `checklyra.com` (production, read-only public check) is serving a genuinely stale cached `/sitemap.xml` — `x-vercel-cache: HIT`, `age` ~189100s (~52.5h), immune to a cache-buster — because the production deploy pipeline is stuck: 3 consecutive `beta → production` promotes since 2026-08-11 20:23 UTC have failed to actually deploy (2 cancelled after 6–8.5h stuck on the SEC-106 approval gate, current HEAD `c8662fb` / run [31735040009](https://github.com/luisa-sys/lyra/actions/runs/31735040009) still `waiting` after ~9h at time of this run). Production has been running the pre-`a303db3` build for ~56h, so SEC-130/131/132 and everything merged since are not live. This recurs the exact mechanism **BUGS-97** already tracks (promote-to-production reports success while the deploy is still `waiting`). Dedup: commented on **SEC-130** and **BUGS-97** with full evidence rather than opening duplicates — both already open. **C3 PASS** — cited from `soak-journey.yml`: latest run [31734166731](https://github.com/luisa-sys/lyra/actions/runs/31734166731) completed+success at 2026-08-13T19:06:30Z (~9.3h old). C5: advisors 10 security (unchanged, same known set) / 157 performance (was ~151 — `unused_index` 64 (-1, expected drift), `auth_rls_initplan` 40 (+2), `multiple_permissive_policies` 39 (+5), `unindexed_foreign_keys` 13, `auth_db_connections_absolute` 1; the +7 spread across many pre-existing tables with no single alarming new target — treated as normal drift from ongoing schema/policy work, not flagged); invite queue 0 stuck >24h; 0 stale `one_time_tokens`; 0 NULL-token `auth.users` rows; migration parity OK — DB head `20260811211646` (`sec104_invariant_public_profiles_view`) = origin/staging head file `20260811210500_sec104_invariant_public_profiles_view.sql` (apply-timestamp offset, per baseline). C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from health-check.yml: success 2026-08-14T02:21:03Z. **FAIL-alert email did not send** — `api.resend.com` still blocked by the session egress policy (recurrence of **BUGS-99**, still To Do; confirmed via proxy status `connect_rejected`/403); escalated via the session's push-notification channel instead. |
| 2026-08-13 04:14 | staging-soak routine (KAN-413), run 21 | PASS=13 / FAIL=1 / UNVERIFIED=0 | pages p50 344–680ms max 1049ms / health p50 249ms max 461ms | BUGS-99 (new) | ⚠️ This row is a continuation, not sequential with the row below (run 5, 2026-07-28) — `develop`'s copy of this file has been stale since then per **BUGS-88** (runs 6–20's docs PRs — [#630](https://github.com/luisa-sys/lyra/pull/630) through [#763](https://github.com/luisa-sys/lyra/pull/763) — sit unmerged as drafts; this run's PR joins that queue, now 17 open). Continuity for runs 6–20 lives in each run's own PR body and the Confluence heartbeat, not in this file. **One genuine FAIL, but not a new finding:** C1-sitemap-fresh — `/sitemap.xml` on `e2e-stage` served `cache-control: public, max-age=0, must-revalidate` instead of `no-store`/`no-cache` (confirmed directly via `curl -D -`). This is the exact regression already tracked by **SEC-130** ("`/sitemap.xml` is prerendered at BUILD time … defeating SEC-100"), status In Progress, not yet promoted to staging — no new ticket filed. C1/C2/C4 otherwise 12/13 PASS via the `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C3 PASS** — cited from `soak-journey.yml`: latest run [31662196348](https://github.com/luisa-sys/lyra/actions/runs/31662196348) completed+success 2026-08-13T02:53:22Z (~1.4h old, well within the 26h freshness window). **C5** via the Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors **10**, unchanged (same known set — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`). Performance advisors **157** (`unused_index` 64 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — within the drift already tracked by **BUGS-92** (still To Do), no identity beyond that set. Invite queue (`gathering_invite_messages`): 0 stuck >24h. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. Migration parity: DB head matches `origin/staging`'s two newest files (`sec104_public_profiles_view` / `sec104_invariant_public_profiles_view`, apply-timestamp offset per baseline) — same in-flight SEC-104 state already tracked by **BUGS-96**, unchanged. **C6**: 0 runtime errors project-wide in the last 24h (Vercel `get_runtime_errors`); liveness cited from `health-check.yml`: success 2026-08-13T02:22:54Z. **New finding: BUGS-99** (Medium) — the routine's own FAIL-alert email (`scripts/security-alert-email.sh` → Resend) failed: the session's egress proxy rejected the CONNECT to `api.resend.com` with a 403 policy denial, even though `RESEND_API_KEY` was present and correctly read. Confirmed via the proxy's own status endpoint, not guessed. This is a routine-environment allowlist gap (external to this repo), not an application defect — logged per the Workflow & Backup Integrity Policy's no-silent-skip rule rather than silently absorbed. Dedup check: SEC-130, BUGS-92, BUGS-96, BUGS-88 all pre-existing and open, unaffected by this run; only BUGS-99 is new. Email: **attempted, failed** (see BUGS-99) — flagged with a PAGE: line in the routine's final reply since a normal-channel FAIL alert could not be confirmed delivered. |
| 2026-08-12 04:16 | staging-soak routine (KAN-413), run 20 | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 362–706ms max 1037ms / health p50 260ms max 329ms | BUGS-96 (new) | **All green** (no FAIL, no UNVERIFIED). ⚠️ **This row is a continuation, not sequential with run 5** — `develop`'s copy of this file was stale at run 5 (2026-07-28) per **BUGS-88** (runs 6–19's docs PRs sit unmerged as drafts); the Confluence Ops Routines Control Room heartbeat is the authoritative run history in the meantime (run numbering here follows that ledger). C1/C2/C4: 12 PASS via `scripts/staging-soak.sh` against the `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant: `siteUrl`/`isBetaDeploy=false`/`vercelEnv=preview` all match). **C3 PASS** — cited from `soak-journey.yml` (Playwright not run in-cloud per KAN-413's split): latest run [31531851573](https://github.com/luisa-sys/lyra/actions/runs/31531851573) completed+success at 2026-08-11T20:13:48Z (~8h old, within the 26h freshness window). C5 via Supabase connector on `uobmlkzrjkptwhttzmmi`: security advisors 10, identical name+target set to baseline (7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN) — no new. Performance advisors 158, matching the baseline established at run 18 (**BUGS-92**, still To Do) — breakdown unchanged (`unused_index` 65 / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1) — no new identities, no new ticket. Invite queue (`gathering_invite_messages`) 0 stuck >24h. NULL-token `auth.users` rows: 0. Stale `one_time_tokens`: 0. **Migration parity: one new finding** — staging DB has 2 SEC-104 migrations (`sec104_public_profiles_view`, `sec104_invariant_public_profiles_view`) applied that are not yet in `origin/staging`'s `supabase/migrations/` (only in `origin/develop`, applied directly to dev+staging ahead of the `develop → staging` promote) — this is new since run 19 (2026-08-11 04:14), which confirmed parity OK at `20260809093651`/`kan443_gift_redesign`. Verified not a live functional bug (no `origin/staging` code references `public_profiles` yet); traced to the open, in-progress SEC-104 ticket; filed as **BUGS-96** (Low), distinct from SEC-104 itself and from the now-resolved BUGS-84/BUGS-87 gap (opposite direction — that one was repo-ahead/DB-behind). C6: zero runtime errors project-wide in the last 24h (Vercel `get_runtime_errors`, project `lyra`); liveness cited from `health-check.yml`: success 2026-08-12T02:20:55Z. Dedup check: BUGS-73, BUGS-84, BUGS-87, BUGS-88, BUGS-92 remain open, pre-existing, unaffected by this run. No email (no FAIL). |
| 2026-08-11 04:14 | staging-soak routine (KAN-413), run 19 | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 333–645ms max 722ms / health p50 264ms max 384ms | none | **All green.** C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). One transient `C4-home` UNVERIFIED (5/5 unreachable, `000`) on the script's first pass; immediate re-run reproduced clean (200 x5, p50=645ms max=722ms) with no other route affected either pass — treated as a probe-side network blip, not a staging fault, and not logged. **C3 PASS** — cited from `soak-journey.yml`: latest run [31454414156](https://github.com/luisa-sys/lyra/actions/runs/31454414156) completed+success at 2026-08-11T03:06:41Z (~1h old, within the 26h freshness window). **C5**: security advisors 10 (unchanged — same 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN identities as baseline). Performance advisors **158** — matches the count already tracked by **BUGS-92** (filed 2026-08-10, To Do, not yet re-baselined); breakdown `unused_index` 65 (unchanged) / `auth_rls_initplan` 40 / `multiple_permissive_policies` 39 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1 — no identities beyond what BUGS-92 already covers, so no new ticket. Invite queue 0 stuck >24h; 0 stale `one_time_tokens`; 0 NULL-token `auth.users` rows. Migration parity **OK** — DB head `20260809093651` (`kan443_gift_redesign`) matches `origin/staging`'s newest file (`20260803170000_kan443_gift_redesign.sql`, apply-timestamp offset per baseline); the migration-lag gaps **BUGS-84**/**BUGS-87** tracked both appear resolved by this check (both tickets still To Do — flagging for human closure, not transitioning them from this routine). C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from `health-check.yml`: success 2026-08-11T02:03:46Z. Dedup check: BUGS-73 (open, pre-existing), BUGS-84/BUGS-87/BUGS-88/BUGS-92 (all open, soak-filed, none newly triggered by this run) — no new ticket filed. **Two process notes, not new tickets (already covered):** (1) this repo mirror of the run-log has been stale on `develop` since run 5 (2026-07-28) — BUGS-88 already tracks the 10+ unmerged daily-soak draft PRs (#630–#706, runs 6–15) responsible; (2) this run's designated branch (`claude/stoic-cerf-e15jw4`) was found rooted on `main`'s promote-chain history (464 commits ahead of `develop`, ~1 ahead of `main`) rather than `origin/develop` — the BUGS-19/20 branch-topology pattern — and was reset onto `origin/develop` before this commit, per CLAUDE.md's worktree/branch-isolation guidance. No email (no FAIL). |
| 2026-08-10 04:12 | staging-soak routine (KAN-413), run 18 | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 290–647ms max 1035ms / health p50 247ms max 339ms | BUGS-92 | **All green.** C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C3 PASS** — cited from `soak-journey.yml`: latest run [31351250980](https://github.com/luisa-sys/lyra/actions/runs/31351250980) completed+success at 2026-08-10T02:58:21Z (~1.25h old, within the 26h freshness window). C5: security advisors 10, unchanged (same 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN identities); invite queue 0 stuck >24h; 0 NULL-token `auth.users` rows; 0 stale `one_time_tokens`. **Migration parity now OK** — DB head advanced to `20260803170000_kan443_gift_redesign` (matches `origin/staging` latest file by name), resolving the gap runs 9–17 had tracked under BUGS-84/BUGS-87 (commented BUGS-87 accordingly, recommending it close once the new fields are smoke-tested — this soak's C3 journey doesn't exercise gift-dismissal/voucher-hint/custom-group/custom-prompt specifically). **New finding:** performance advisors jumped 151→158 (`unused_index` 65 unchanged / `auth_rls_initplan` 38→40 / `multiple_permissive_policies` 34→39 / `unindexed_foreign_keys` 13 unchanged / `auth_db_connections_absolute` 1 unchanged) — structural, not the usual self-correcting `unused_index` drift, and correlated with the same migrations that closed the parity gap (new tables' RLS policies, e.g. `gift_suggestion_dismissals`). Filed **BUGS-92** (Low) recommending triage + baseline refresh rather than silently absorbing it. C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from health-check.yml: success 2026-08-10T02:08:56Z (run [31348986551](https://github.com/luisa-sys/lyra/actions/runs/31348986551)). Dedup check: BUGS-73 (In Progress, pre-existing, unaffected) remains open; BUGS-88 (11 prior run-log PRs stuck as unmerged drafts, `docs/STAGING_SOAK_ROUTINE.md` on `develop` stale since run 5) is still open and unresolved by this run — this PR is opened the same way as its predecessors pending that ticket's human decision on a merge model, so the staleness this row is closing is only current as of this PR actually merging. No email (no FAIL). |
| 2026-08-09 04:13 | staging-soak routine (KAN-413), run 17 | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 314–530ms max 1045ms / health p50 265ms max 363ms | none | **All green.** C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C3 PASS** — cited from `soak-journey.yml`: latest run [31291719719](https://github.com/luisa-sys/lyra/actions/runs/31291719719) completed+success at 2026-08-09T03:05:40Z (~1.1h old, well within the 26h freshness window). C5 at baseline, no new: advisors 10 security (unchanged — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN, same known set) / 151 performance (matches the last run-log figure; breakdown `unused_index` 65 / `auth_rls_initplan` 38 / `multiple_permissive_policies` 34 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1); invite queue 0 stuck >24h; 0 stale `one_time_tokens`; 0 NULL-token `auth.users` rows. **Migration parity — same tracked gap as runs 9–16, unchanged:** DB head still `20260727123657`; `origin/staging` still carries the same four unapplied files (`20260730120000_bugs77_create_ensure_rls_event_trigger.sql`, `20260803114500_favourites_custom_group.sql`, `20260803160000_kan445_custom_conversation_prompts.sql`, `20260803170000_kan443_gift_redesign.sql`) — already tracked as **BUGS-84** and **BUGS-87** (both reconfirmed still "To Do" in Jira before this run, 4 days unaddressed for BUGS-87), no new ticket filed. C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from health-check.yml: success 2026-08-09T02:04:22Z (run [31289565452](https://github.com/luisa-sys/lyra/actions/runs/31289565452)). Dedup check: BUGS-73 (Manual-of-Me autosave, In Progress), BUGS-84 + BUGS-87 (migration-parity family, both To Do), and BUGS-88 (run-log PRs #630–#706 sitting unmerged as drafts, To Do) remain the only open soak-adjacent tickets — all pre-existing, unaffected by this run. No new ticket filed. No email (no FAIL). **Note:** this repo mirror was found stuck at run 5 (2026-07-28) per BUGS-88 — jumping straight to run 17 here rather than re-numbering; Confluence heartbeat 34275370 is the continuous authoritative record throughout the gap. This run's designated branch (`claude/stoic-cerf-kwpuv4`) was found sitting on `origin/main`'s tip (0 commits of its own) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` before committing. |
| 2026-08-08 04:12 | staging-soak routine (KAN-413), run 16 | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 284–599ms max 1030ms / health p50 251ms max 311ms | [BUGS-88](https://checklyra.atlassian.net/browse/BUGS-88) | **All green** on the C1–C6 release contract. C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant, 0/5 over budget on every route). **C3 PASS** — cited from `soak-journey.yml` run [31236490131](https://github.com/luisa-sys/lyra/actions/runs/31236490131), completed+success at 2026-08-08T03:06:54Z (~1h old, well within the 26h freshness window). C5 on `uobmlkzrjkptwhttzmmi`: advisors 10 security (unchanged — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN, same set) / 151 performance (unchanged breakdown: `unused_index` 65 / `auth_rls_initplan` 38 / `multiple_permissive_policies` 34 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1); invite queue `gathering_invite_messages` empty (0 rows, 0 stuck >24h); 0 NULL-token `auth.users` rows; 0 stale `one_time_tokens`. **Migration parity gap unchanged from runs 13–15** — DB head still `20260727123657`; `origin/staging` still 4 migration files ahead (`bugs77_create_ensure_rls_event_trigger`, `favourites_custom_group`, `kan445_custom_conversation_prompts`, `kan443_gift_redesign`) — verified directly (`information_schema`) that `profiles.gift_voucher_hint`, `gift_suggestion_dismissals`, `profile_items.group_label`, `profile_conversation_starters.custom_prompt` still don't exist on staging. Already tracked as [BUGS-84](https://checklyra.atlassian.net/browse/BUGS-84) and [BUGS-87](https://checklyra.atlassian.net/browse/BUGS-87), both still To Do — no new ticket for the drift itself. C6: zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from `health-check.yml`: success 2026-08-08T01:56:52Z (run [31233838582](https://github.com/luisa-sys/lyra/actions/runs/31233838582)). **New finding this run (meta, not C1–C6):** `docs/STAGING_SOAK_ROUTINE.md` on `develop` was still sitting at run 5 (2026-07-28) — runs 6–15 (PRs #630, #642, #652, #656, #664, #676, #690, #698, #701, #706) all opened successfully but none ever merged, all still open as drafts. Filed [BUGS-88](https://checklyra.atlassian.net/browse/BUGS-88); this row is appended on top of run 5 (the actual current `develop` state), so the table below still has the runs-6-through-15 gap until BUGS-88 is resolved — see the Confluence Ops Routines Control Room heartbeat (page 34275370) for the authoritative continuous history in the meantime. No email (no FAIL). |
| 2026-08-07 04:14 | staging-soak routine (KAN-413), run 15 | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 344–721ms max 988ms / health p50 319ms max 373ms | none | **All green.** C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C3 PASS** — cited from `soak-journey.yml`: latest run [31143758869](https://github.com/luisa-sys/lyra/actions/runs/31143758869) completed+success at 2026-08-07T03:14:48Z (~1h old, well within the 26h freshness window). C5 at baseline for security/perf/invite-queue/tokens, no new: advisors 10 security (unchanged, same known set) / 151 performance (matches the last run-log figure exactly; breakdown `unused_index` 65 / `auth_rls_initplan` 38 / `multiple_permissive_policies` 34 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1); invite queue 0 stuck >24h; 0 stale `one_time_tokens`; 0 NULL-token `auth.users` rows. **Migration parity — same tracked gap as runs 9–14, unchanged:** DB head still `20260727123657`; `origin/staging` still carries the same four unapplied files (verified via `git ls-tree origin/staging`) — no new file landed since run 14. [BUGS-84](https://checklyra.atlassian.net/browse/BUGS-84) and [BUGS-87](https://checklyra.atlassian.net/browse/BUGS-87) both reconfirmed still To Do via Jira before this run — no new ticket filed. C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from health-check.yml: success 2026-08-07T00:06:08Z. Dedup: BUGS-84, BUGS-87 (migration-parity family) and BUGS-73 (Manual-of-Me autosave, In Progress) remain the only open soak-adjacent tickets, all pre-existing and unaffected by this run — no new ticket filed. No email (no FAIL). |
| 2026-08-06 04:14 | staging-soak routine (KAN-413), run 14 | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 300–635ms max 782ms / health p50 286ms max 320ms | none | **All green.** C1/C2/C4: 12 PASS via the `e2e-stage` grey-cloud alias (C1 gate on the real host: 302, gated; `/api/health` conformant). Note: a first invocation of `staging-soak.sh` at 04:14:31Z transiently showed `C4-home`/`C4-login` UNVERIFIED (unreachable, HTTP 000) and `C4-status` FAIL (DEGRADED, 2/5 passes over budget incl. one 8330ms outlier); an immediate re-run 76s later came back clean 12/12 PASS with tight latencies (p50 286–635ms) across all five probes including the previously-failing ones. Treated as transient egress noise from this session's own network path (multiple unrelated routes glitching simultaneously then clearing completely reads as client-side, not server-side degradation) rather than a staging finding — not filed, but flagging the pattern here in case a future run reproduces it non-transiently. **C3 PASS** — cited from `soak-journey.yml`: latest run [31066823450](https://github.com/luisa-sys/lyra/actions/runs/31066823450) completed+success at 2026-08-06T02:52:07Z (~1.4h old, well within the 26h freshness window). C5 at baseline, no new: advisors 10 security (unchanged — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN, same known set incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`) / 151 performance (matches the last run-log figure exactly; breakdown `unused_index` 65 / `auth_rls_initplan` 38 / `multiple_permissive_policies` 34 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1); invite queue 0 stuck >24h; 0 stale `one_time_tokens`; 0 NULL-token `auth.users` rows. **Migration parity — same tracked gap as runs 9–13, unchanged:** DB head still `20260727123657` (`security_invariants_report_v2`); `origin/staging` still carries the same four unapplied files verified this run via `git ls-tree origin/staging` — `20260730120000_bugs77_create_ensure_rls_event_trigger.sql` (BUGS-84), `20260803114500_favourites_custom_group.sql`, `20260803160000_kan445_custom_conversation_prompts.sql`, `20260803170000_kan443_gift_redesign.sql` (all three BUGS-87) — no new file landed since run 13. Both tickets confirmed still open (Status: To Do) via Jira before this run — no new ticket filed. C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from health-check.yml: success 2026-08-06T02:56:26Z. Dedup check: searched Jira first — BUGS-84, BUGS-87 (migration-parity family, both To Do) and BUGS-73 (Manual-of-Me autosave, In Progress) remain the only open soak-adjacent tickets, all pre-existing and unaffected by this run. No email (no FAIL). |
| 2026-08-05 04:14 | staging-soak routine (KAN-413), run 13 | PASS=19 / FAIL=1 / UNVERIFIED=0 | pages p50 365–752ms max 1025ms / health p50 354ms max 370ms | [BUGS-87](https://checklyra.atlassian.net/browse/BUGS-87) | **C5 migration-parity FAIL — confirmed live break.** C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C3 PASS** — cited from `soak-journey.yml`: latest run [30970684806](https://github.com/luisa-sys/lyra/actions/runs/30970684806) completed+success at 2026-08-05T02:54:13Z (~1.3h old, within the 26h freshness window). C5 advisors/tokens/queue at baseline, no new: 10 security (unchanged, same known set) / 151 performance (matches the last run-log figure exactly; breakdown `unused_index` 65 / `auth_rls_initplan` 38 / `multiple_permissive_policies` 34 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1); invite queue 0 stuck >24h; 0 stale `one_time_tokens`; 0 NULL-token `auth.users` rows. **Migration parity FAIL:** DB head still `20260727123657`; `origin/staging` (promoted **today** 2026-08-05T02:45:38Z, commit `7ecfe26`, ~1.5h before this soak) carries 4 unapplied migrations. `20260730120000_bugs77_...` stays under pre-existing BUGS-84 (confirmed no-op, unchanged). The 3 landed since the prior run — `20260803114500_favourites_custom_group.sql` (KAN-444), `20260803160000_kan445_custom_conversation_prompts.sql` (KAN-445), `20260803170000_kan443_gift_redesign.sql` (KAN-443) — were individually verified this run: `profiles.gift_voucher_hint` and table `gift_suggestion_dismissals` confirmed absent from the DB via `information_schema`, while the already-deployed `src/app/dashboard/profile/actions.ts` does an unconditional, unguarded write to `gift_suggestion_dismissals` on gift-suggestion dismiss/un-dismiss — that control will hard-fail (`relation does not exist`) the first time anyone exercises it on staging. The `gift_voucher_hint`/`group_label`/`custom_prompt` column writes are defensively guarded in app code (key omitted from the PostgREST payload unless the member sets the value), so ordinary profile saves are unaffected, but setting any of those three fields specifically will PGRST204 the whole save. Searched Jira first — no ticket covered files #2–#4 before this run. Filed **BUGS-87** (Medium). C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from health-check.yml: success 2026-08-05T02:52:46Z. **Alert email FAILED to send:** `scripts/security-alert-email.sh` → Resend returned a proxy-level 403 (`gateway answered 403 to CONNECT`, host `api.resend.com:443`) — this session's network egress policy does not allow `api.resend.com`; not retriable per the proxy's own guidance. Surfaced instead via a direct push notification and a PAGE: line in the run's reply; the routine's network-access allowlist needs a human fix (session cannot change its own environment config). **Housekeeping:** this run's designated branch (`claude/stoic-cerf-pub9oc`) was found sitting on `origin/main`'s tip (0 commits of its own, no open PR) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` before committing. |
| 2026-08-04 04:14 | staging-soak routine (KAN-413), run 12 | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 356–472ms max 1009ms / health p50 312ms max 370ms | none (comment on BUGS-84) | **All green.** C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C3 PASS** — cited from `soak-journey.yml`: latest run [30873925028](https://github.com/luisa-sys/lyra/actions/runs/30873925028) completed+success at 2026-08-04T03:09:21Z (~1.1h old, well within the 26h freshness window). C5 at baseline for security/perf/invite-queue/tokens, no new: advisors 10 security (unchanged, same known set: 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`) / 151 performance (matches the last run-log figure exactly; breakdown `unused_index` 65 / `auth_rls_initplan` 38 / `multiple_permissive_policies` 34 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1); invite queue 0 stuck >24h; 0 stale `one_time_tokens`; 0 NULL-token `auth.users` rows. **Migration parity — same tracked gap as runs 9–11, WIDENED (not new — commented on BUGS-84, not re-filed):** DB head still `20260727123657` (`security_invariants_report_v2`); `origin/staging` now carries **four** unapplied files ahead of that head — the original `20260730120000_bugs77_create_ensure_rls_event_trigger.sql` plus three from since run 11's promote: `20260803114500_favourites_custom_group.sql` (KAN-444), `20260803160000_kan445_custom_conversation_prompts.sql` (KAN-445), `20260803170000_kan443_gift_redesign.sql` (KAN-443). Only the original file has been verified as a behaviour no-op; the three new ones have not been individually re-verified (detect-and-log scope) — flagged in the BUGS-84 comment for human/Backlog-Autopilot follow-up. C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from health-check.yml: success 2026-08-04T02:56:40Z. Dedup check: BUGS-73 (Manual-of-Me autosave, In Progress) and BUGS-84 (migration parity gap, To Do) remain the only open soak-adjacent tickets; both pre-existing, updated (BUGS-84 comment) rather than re-filed. No alert email (all green — a comment on an existing ticket is not a FAIL). **Housekeeping:** this run's designated branch (`claude/stoic-cerf-i2x17u`) was found sitting on `origin/main`'s tip (0 commits of its own, no open PR) — the recurring BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` before committing. |
| 2026-08-03 05:26 | staging-soak routine (KAN-413), run 11 | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 329–699ms max 1064ms / health p50 338ms max 378ms | none | **All green.** C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C3 PASS** — cited from `soak-journey.yml`: latest run [30748906060](https://github.com/luisa-sys/lyra/actions/runs/30748906060) completed+success at 2026-08-02T13:00:11Z (~16.4h old, within the 26h freshness window). C5 at baseline, no new: advisors 10 security (unchanged, same known set) / 151 performance (matches the last run-log figure exactly; breakdown `unused_index` 65 / `auth_rls_initplan` 38 / `multiple_permissive_policies` 34 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1); invite queue 0 stuck >24h; 0 stale `one_time_tokens`; 0 NULL-token `auth.users` rows. **Migration parity — same known gap as runs 9–10, not new:** DB head still `20260727123657` (`security_invariants_report_v2`); `origin/staging` still carries the newer, unapplied `20260730120000_bugs77_create_ensure_rls_event_trigger.sql` — already tracked as **BUGS-84** (To Do), not re-filed. C6: clean — the sole 24h runtime-error group (`Hu [AuthApiError]: Request rate limit reached`, `/middleware`, 30 occurrences) traces via `get_deployment` to `dpl_2gMNoNg3ukfcsJP5wUVVUiZqdFvo` (`githubCommitRef=develop`, aliased to `dev.checklyra.com`) — **dev-attributed, not staging** — so no staging error-budget finding; liveness cited from health-check.yml: success 2026-08-03T03:28:31Z. Dedup check: BUGS-73 (Manual-of-Me autosave, In Progress) and BUGS-84 (migration parity gap, To Do) remain the only open soak-adjacent tickets, both pre-existing and unaffected by this run — no new ticket filed. No email (no FAIL). Note: this repo mirror's log was last updated at run 5 (2026-07-28); the Confluence Heartbeat ledger (page 34275370) is current through run 10 (2026-08-02) — the wiki is definitive per KAN-363, this mirror entry picks up the numbering from there. |
| 2026-08-02 04:12 | staging-soak routine (KAN-413), run 10 | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 319–733ms max 1026ms / health p50 281ms max 315ms | none | **All green.** C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C3 PASS** — cited from `soak-journey.yml`: latest run [30729502858](https://github.com/luisa-sys/lyra/actions/runs/30729502858) completed+success at 2026-08-02T02:48:26Z (~1.4h old, well within the 26h freshness window). C5 at baseline, no new: advisors 10 security (unchanged — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN, same known set incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`) / 151 performance (matches the last run-log figure exactly; breakdown `unused_index` 65 / `auth_rls_initplan` 38 / `multiple_permissive_policies` 34 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1); invite queue 0 stuck >24h; 0 stale `one_time_tokens`; 0 NULL-token `auth.users` rows. **Migration parity — same known gap as run 9, not new:** DB head still `20260727123657` (`security_invariants_report_v2`); `origin/staging` still carries the newer, unapplied `20260730120000_bugs77_create_ensure_rls_event_trigger.sql` — already tracked as **BUGS-84** (To Do), not re-filed. C6: clean — the sole 24h runtime-error group (`Hu [AuthApiError]: Request rate limit reached`, `/middleware`, 4 occurrences) traces via `get_deployment` to `dpl_Brfq7DW4oKc18tEEMDQqmMBD3ZPr` (`githubCommitRef=develop`, aliased to `dev.checklyra.com`) — **dev-attributed, not staging** — so no staging error-budget finding; liveness cited from health-check.yml: success 2026-08-02T03:26:23Z. Dedup check: BUGS-73 (Manual-of-Me autosave, In Progress) and BUGS-84 (migration parity gap, To Do) remain the only open soak-adjacent tickets, both pre-existing and unaffected by this run — no new ticket filed. No email (no FAIL). **Housekeeping:** this run's designated branch (`claude/stoic-cerf-670pr5`) was found sitting on `origin/main`'s tip (0 commits of its own, no open PR) — the BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` before committing. |
| 2026-08-01 04:13 | staging-soak routine (KAN-413), run 9 | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 348–666ms max 1192ms / health p50 297ms max 356ms | BUGS-84 | **All green on C1–C4/C3; one new C5 finding.** C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C3 PASS** — cited from `soak-journey.yml`: latest run [30665269816](https://github.com/luisa-sys/lyra/actions/runs/30665269816) completed+success at 2026-07-31T21:04:38Z (~7.1h old, within the 26h freshness window). C5: advisors at baseline, no new — 10 security (7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN, same known set) / 151 performance (matches the last run-log figure exactly; breakdown `unused_index` 65 / `auth_rls_initplan` 38 / `multiple_permissive_policies` 34 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1); invite queue 0 stuck >24h; 0 stale `one_time_tokens`; 0 NULL-token `auth.users` rows. **Migration parity — NEW finding:** DB head on staging is still `20260727123657` (`security_invariants_report_v2`), but `origin/staging` gained a newer file, `20260730120000_bugs77_create_ensure_rls_event_trigger.sql` (landed via the 2026-07-31T20:52:48Z develop→staging promote, i.e. after run 8's heartbeat), that has never been run against staging via `apply_migration` — a repo-ahead gap. Verified this is not a live bug: `select * from pg_event_trigger where evtname='ensure_rls'` confirms the trigger the migration records already exists and fires on staging (the file is written as an idempotent lineage-capture no-op). Filed **BUGS-84** (Low; dedup-checked — distinct from open BUGS-77, which covers `rls_auto_enable()` non-existence in a fresh replay, not this file). C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from health-check.yml: success 2026-08-01T03:26:42Z. Dedup check: BUGS-73 remains the only other open soak-adjacent ticket (In Progress), unaffected by this run. No alert email (no hard FAIL — migration parity is a C5 finding, not a probe FAIL). **Housekeeping:** this run's designated branch (`claude/stoic-cerf-24lfjk`) was found identical to `origin/main` (0 commits of its own, no open PR) — the BUGS-19/gotcha-#20 stale branch-off-main topology — reset onto `origin/develop` before committing. |
| 2026-07-31 04:13 | staging-soak routine (KAN-413), run 8 | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 369–510ms max 1004ms / health p50 284ms max 309ms | none | **All green.** C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C3 PASS** — cited from `soak-journey.yml`: latest run [30520075639](https://github.com/luisa-sys/lyra/actions/runs/30520075639) completed+success at 2026-07-30T06:35:40Z (~21.7h old, within the 26h freshness window). C5 at baseline, no new: advisors 10 security (unchanged — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN, same known set) / 151 performance (matches the last run-log figure exactly; breakdown `unused_index` 65 / `auth_rls_initplan` 38 / `multiple_permissive_policies` 34 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1); invite queue 0 stuck >24h; 0 stale `one_time_tokens`; 0 NULL-token `auth.users` rows; migration parity OK — DB head `20260727123657` = origin/staging head file `security_invariants_report_v2` (unchanged since run 5). C6: `get_runtime_errors` (project-wide, no branch filter available) shows 2 error groups in the last 24h — both traced via `get_deployment` to `dpl_H9RmnLuu6TvvGeykTUvrnoef729M`, `githubCommitRef=main`, `target=production` — i.e. **prod-attributed, not staging**, and both are a long-running bot probe (`page: '/index.php'` triggering a Next.js "Failed to find Server Action" / missing `/500.html` pair, first seen 2026-06-25) rather than a new regression. No staging-attributed error found. Liveness cited from health-check.yml: success 2026-07-31T03:27:39Z. Dedup check (Jira): BUGS-73 remains the only open soak-adjacent ticket (In Progress), unaffected by this run; BUGS-74 stayed Done. No new ticket filed. No email (no FAIL). **Housekeeping:** this run's designated branch (`claude/stoic-cerf-4f6i00`) was found identical to `origin/main` (0 commits ahead, 406 behind `develop` via a stale merge-base) with no open PR and no real unmerged work — same BUGS-19/gotcha-#20 topology as prior runs — reset onto `origin/develop` before committing. Also note: PRs [#630](https://github.com/luisa-sys/lyra/pull/630) (run 6) and [#642](https://github.com/luisa-sys/lyra/pull/642) (run 7) are still open/unmerged, so this row is appended against the same base they used — expect a merge conflict to resolve manually, not a soak defect. |
| 2026-07-30 04:12 | staging-soak routine (KAN-413), run 7 | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 318–581ms max 1000ms / health p50 259ms max 377ms | none | **All green.** C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C3 PASS** — cited from `soak-journey.yml`: latest run [30428947479](https://github.com/luisa-sys/lyra/actions/runs/30428947479) completed+success at 2026-07-29T06:40:38Z (~21.5h old, within the 26h freshness window). C5 at baseline, no new: advisors 10 security (unchanged, same known set) / 151 performance (matches the last run-log figure exactly; breakdown `unused_index` 65 / `auth_rls_initplan` 38 / `multiple_permissive_policies` 34 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1); invite queue 0 stuck >24h; 0 stale `one_time_tokens`; 0 NULL-token `auth.users` rows; migration parity OK — DB head `20260727123657` = origin/staging head file `security_invariants_report_v2` (unchanged since run 6). C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from health-check.yml: success 2026-07-30T02:45:02Z. Dedup check: BUGS-73 remains the only open soak-adjacent ticket (In Progress), unaffected by this run — no new ticket filed. No email (no FAIL). Note: run 6's docs run-log PR ([#630](https://github.com/luisa-sys/lyra/pull/630)) had not yet merged at the start of this run, so this row was appended directly on top of run 5's in the develop-branch copy of this file; a human should verify ordering once both PRs land. |
| 2026-07-29 04:12 | staging-soak routine (KAN-413), run 6 | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 333–516ms max 1076ms / health p50 321ms max 334ms | none | **All green.** C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C3 PASS** — cited from `soak-journey.yml`: latest run [30335300718](https://github.com/luisa-sys/lyra/actions/runs/30335300718) completed+success at 2026-07-28T06:35:07Z (~21.6h old, within the 26h freshness window). C5 at baseline, no new: advisors 10 security (unchanged — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN, same known set incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`) / 151 performance (matches the last run-log figure exactly; breakdown `unused_index` 65 / `auth_rls_initplan` 38 / `multiple_permissive_policies` 34 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1); invite queue 0 stuck >24h; 0 stale `one_time_tokens`; 0 NULL-token `auth.users` rows; migration parity OK — DB head `20260727123657` = origin/staging head file `20260727120000_security_invariants_report_v2.sql` (apply-timestamp offset, per baseline; same migration). C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from health-check.yml: success 2026-07-29T03:12:40Z. Dedup check: BUGS-73 remains the only open soak-adjacent ticket (In Progress), pre-existing and unaffected by this run; BUGS-74 is now **Done** since the last run (closed, no longer open). No new ticket filed. No email (no FAIL). Note: this run's Claude Code branch (`claude/stoic-cerf-rqf6ls`) was found freshly cut but 404 commits behind `origin/develop` / topologically off an old point (the BUGS-19/gotcha-#20 stale-branch pattern) with no open PR and no real unmerged work — reset onto `origin/develop` before committing this row, per CLAUDE.md "Always branch from `origin/develop`". |
| 2026-07-28 04:13 | staging-soak routine (KAN-413), run 5 | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 310–688ms max 1052ms / health p50 296ms max 358ms | none | **All green.** C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C3 PASS** — cited from `soak-journey.yml`: latest run [30247079029](https://github.com/luisa-sys/lyra/actions/runs/30247079029) completed+success at 2026-07-27T07:41:52Z (~20.5h old, within the 26h freshness window). C5 at baseline, no new: advisors 10 security (unchanged — 7 `rls_enabled_no_policy` INFO + 3 `authenticated_security_definer_function_executable` WARN, same known set incl. `admin_list_users`/`admin_filter_profile_ids`/`search_by_contact_hash`) / 151 performance (matches the last run-log figure; breakdown `unused_index` 65 / `auth_rls_initplan` 38 / `multiple_permissive_policies` 34 / `unindexed_foreign_keys` 13 / `auth_db_connections_absolute` 1); invite queue 0 stuck >24h; 0 stale `one_time_tokens`; 0 NULL-token `auth.users` rows; migration parity OK — DB head `20260727123657` = origin/staging head file `20260727120000_security_invariants_report_v2.sql` (apply-timestamp offset, per baseline; same migration). C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from health-check.yml: success 2026-07-28T02:53:10Z. Dedup check: BUGS-73 and BUGS-74 remain the only open soak-adjacent tickets, both pre-existing and unaffected by this run — no new ticket filed. No email (no FAIL). |
| 2026-07-27 (post-run-4) | C5 baseline refresh (human/Claude, KAN-413) | BASELINE | n/a | none | **Accepted baseline REFRESHED** after run 4 flagged the old one as stale, re-verified directly against `uobmlkzrjkptwhttzmmi`: **10 security** advisors (unchanged); **~151 performance** advisors (was 154 — 65 are `unused_index`, derived from live usage stats, so the total drifts on its own: 154 → 152 → 151 with no code change; **C5 now compares lint name+target, not totals, and a downward move is never a finding**); **NULL-token `auth.users` = 0** (was 8 seed rows; the 8 `seed.%@seed.checklyra.com` rows still exist but are normalised, so the GoTrue-500 `listUsers` quirk that took C3 down in run 1 is gone). **The "8 seed rows are expected" carve-out is RETIRED — any NULL-token row is now a finding**, which is strictly stronger. ⚠️ `scripts/seed-demo-profiles.sql` still inserts NULL token columns, so re-seeding staging re-introduces the quirk; normalise after any re-seed. Migration head baseline moved `20260724231235` → `20260727123657` (apply timestamp, not the repo filename prefix). Also registered `staging-soak|1740|<iso>|<outcome>` in the routine watchdog (`docs/OPS_ROUTINES_CONTROL_ROOM.md`), mutation-verified fresh→PASS / stale→OVERDUE-FAIL(exit 2) / missing→UNVERIFIED(exit 1). |
| 2026-07-27 04:16 | staging-soak routine (KAN-413), run 4 | PASS=13 / FAIL=0 / UNVERIFIED=0 | pages p50 345–641ms max 1051ms / health p50 307ms max 331ms | none | **All green.** C1/C2/C4: 12 PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C3 PASS** — cited from `soak-journey.yml` (owns the Playwright journey per KAN-413's split): latest run [30219132924](https://github.com/luisa-sys/lyra/actions/runs/30219132924) completed+success at 2026-07-26T20:30:38Z (~7.7h old, within the 26h freshness window). C5 at baseline, no new: advisors 10 security (unchanged) / 152 performance (2 fewer than the 154 baseline — improvement, not a finding); invite queue 0 stuck >24h; 0 stale `one_time_tokens`; migration parity OK — DB head `20260724231235` = origin/staging head file `20260725000500_bugs69_…` (apply-timestamp offset, per baseline). **Note (not a finding):** NULL-token `auth.users` rows are now **0** (0 seed, 0 non-seed), down from the 8 baselined seed rows — the GoTrue-500 seed-row normalisation flagged in run 1 appears to have landed since; no new non-seed rows, so no C5 finding, but the accepted-baseline row above is now stale and worth a human refresh. C6: clean — zero runtime errors project-wide in the last 24h (`get_runtime_errors`); liveness cited from health-check.yml: success 2026-07-27T03:36:48Z. Dedup check: two open tickets already track prior soak findings (BUGS-73 Manual-of-Me autosave-on-blur timing, BUGS-74 profile-editor field-loss) — both pre-existing, still In Progress, no new ticket needed this run. No email (no FAIL). |
| 2026-07-25 18:14 | staging-soak routine (KAN-413), run 1 | PASS=12 / FAIL=0 / UNVERIFIED=1 (C3) | pages p50 355–653ms max 813ms / health p50 285ms max 354ms | none | C1/C2/C4 all PASS via `e2e-stage` alias (C1 gate on real host: 302, gated; `/api/health` conformant). **C3 UNVERIFIED — harness, not a staging bug**: `ensureSoakUser()` fails before any browser launch — GoTrue `/auth/v1/admin/users` (admin listUsers) returns 500 `Database error finding users`; root cause is the *baselined* GoTrue-500 quirk (the 8 NULL-token `seed.%@seed.checklyra.com` rows break admin listUsers pagination). Fix: normalise the 8 seed rows' NULL `confirmation_token`/`recovery_token` to `''` on staging, or drop the `listUsers` dependency in `tests/e2e/support/soak-user.ts` (e.g. look up by ID); soak user `soak@seed.checklyra.com` not yet created. Logged on KAN-413. C5 fully at baseline: advisors 10 security / 154 performance (no new); NULL-token rows 8 (0 non-seed); invite queue 0 stuck >24h; 0 stale one_time_tokens; migration parity OK — DB head `20260724231235` = `bugs69_revoke_global_feature_switch_trigger_exec`, matching origin/staging head file `20260725000500_bugs69_…` (apply-timestamp offset, per baseline). C6: staging error budget clean — the only 24h runtime-error group (`get_metrics_for_window` permission denied, `/admin/monitoring`) is **prod**-attributed and already tracked as BUGS-71 (Done, awaiting promotion); liveness cited from health-check.yml: success 2026-07-25 13:45 UTC. No email (no hard FAIL). |
| 2026-07-25 | commissioning baseline (human) | BASELINE | n/a | none | **Accepted baseline for C5 dedup**: advisors 10 security / 154 performance; 8 known NULL-token `seed.%@seed.checklyra.com` rows; invite queue `gathering_invite_messages` 0 stuck; DB migration head `20260724231235`. C3 pending CF-bypass/`e2e-stage` alias. First real run compares against this row. |
